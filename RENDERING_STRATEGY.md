Default rendering path

Use Quarto as the primary rendering backend.

Optional CLI support

Allow Typst CLI-specific checks where useful.

Goal

The user should not need to care about backend details in normal usage.
