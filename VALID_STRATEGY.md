# NA

typstR should validate the most common user mistakes before render.

Checks to implement Quarto installed Typst available through Quarto or
CLI required files exist logo path valid bibliography exists if
citations are used manuscript metadata fields are well formed selected
format is supported Functions check_quarto() check_typst()
validate_manuscript()
