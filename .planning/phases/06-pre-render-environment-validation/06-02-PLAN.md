---
phase: 06-pre-render-environment-validation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - R/render.R
  - tests/testthat/test-render-guards.R
  - tests/testthat/test-validation-environment.R
  - tests/testthat/test-yaml-integration.R
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - "render_pub() preflight behavior is driven by the same shared environment validator used by standalone validation (D-07)."
    - "For equivalent failing environments, standalone validation and render wrappers emit the same stable diagnostic codes and class contract (D-02, D-06, D-07)."
    - "Legacy user guidance strings remain compatible while tests prioritize class+payload assertions (D-08)."
  artifacts:
    - path: "R/render.R"
      provides: "Render preflight refactored to call shared environment validation primitive"
    - path: "tests/testthat/test-render-guards.R"
      provides: "Contract tests asserting render path reuses shared validator output"
    - path: "tests/testthat/test-yaml-integration.R"
      provides: "Quarto-present guarded checks for reported versions/floor readiness without breaking no-Quarto safety"
  key_links:
    - from: "R/render.R"
      to: "R/validation_environment.R"
      via: "render_pub() preflight invocation before resolve_input() and quarto_render()"
      pattern: "validate_render_environment"
    - from: "tests/testthat/test-render-guards.R"
      to: "R/render.R"
      via: "mocked no-Quarto assertions on shared validator-driven payload"
      pattern: "typstR_diagnostics_error|DIAG-ENV-"
    - from: "tests/testthat/test-validation-environment.R"
      to: "R/render.R"
      via: "equivalence assertions for standalone vs render preflight diagnostics"
      pattern: "render_pub|validate_render_environment"
---

<objective>
Cut over render preflight to the new shared environment validator and verify both call paths stay behaviorally identical.

Purpose: Enforce one concept/one representation for environment readiness so users cannot receive conflicting preflight outcomes between standalone checks and render wrappers.
Output: Refactored render preflight wiring plus guard/integration tests that prove contract consistency and no-Quarto safety.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-pre-render-environment-validation/06-CONTEXT.md
@.planning/phases/06-pre-render-environment-validation/06-RESEARCH.md
@.planning/phases/06-pre-render-environment-validation/06-VALIDATION.md
@.planning/phases/06-pre-render-environment-validation/06-01-PLAN.md
@R/render.R
@R/validation_environment.R
@tests/testthat/test-render-guards.R
@tests/testthat/test-validation-environment.R
@tests/testthat/test-yaml-integration.R

<interfaces>
From R/validation_environment.R (from 06-01):
```r
validate_render_environment <- function(path = ".")
```

From R/render.R:
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE, open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE, open = interactive())
```

From tests/testthat/test-render-guards.R:
```r
expect_error(render_pub("paper.qmd", open = FALSE), class = "typstR_diagnostics_error")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor render preflight to consume shared validation primitive only</name>
  <files>
    R/render.R
    tests/testthat/test-render-guards.R
    tests/testthat/test-validation-environment.R
  </files>
  <read_first>
    R/render.R,
    R/validation_environment.R,
    R/utils.R,
    tests/testthat/test-render-guards.R,
    tests/testthat/test-validation-environment.R,
    .planning/phases/06-pre-render-environment-validation/06-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `render_pub()` calls `validate_render_environment()` before `resolve_input()` and never duplicates preflight logic inline (D-07).
    - Test 2: when environment is invalid, `render_pub()` emits the same diagnostic class/codes as standalone validation for equivalent conditions (D-02, D-06, D-07).
    - Test 3: `render_working_paper()` inherits identical behavior because it delegates to `render_pub()`.
    - Test 4: top-level compatibility message for Quarto-missing remains present in rendered error text (D-08).
  </behavior>
  <action>
Replace the existing inline Quarto-only preflight branch in `R/render.R` with a call to `validate_render_environment()` per D-07, and ensure the call happens before any render side effects (`resolve_input()`, `quarto_render()`, file opening).

Preserve the existing user guidance messaging behavior where possible (D-08), but do not maintain a second preflight implementation path. Update `tests/testthat/test-render-guards.R` to assert shared-path outcomes (class + stable codes first, message compatibility second). Add an equivalence assertion in `tests/testthat/test-validation-environment.R` that compares standalone and render-triggered diagnostics for a known failing environment.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-render-guards.R"); testthat::test_file("tests/testthat/test-validation-environment.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/render.R` contains a `validate_render_environment(...)` preflight call in `render_pub()`.
    - `R/render.R` no longer contains direct environment diagnostic construction branches that duplicate validation logic.
    - `tests/testthat/test-render-guards.R` asserts stable diagnostic code payloads from the shared path.
    - `tests/testthat/test-validation-environment.R` includes at least one standalone-vs-render diagnostic equivalence assertion.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-render-guards.R")'` exits successfully.
  </acceptance_criteria>
  <done>Render wrappers now trust the shared validator as the single source of environment preflight truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden Quarto-present and no-Quarto validation coverage across integration guardrails</name>
  <files>
    tests/testthat/test-validation-environment.R
    tests/testthat/test-yaml-integration.R
  </files>
  <read_first>
    tests/testthat/test-validation-environment.R,
    tests/testthat/test-yaml-integration.R,
    R/validation_environment.R,
    .planning/phases/06-pre-render-environment-validation/06-VALIDATION.md,
    .planning/phases/06-pre-render-environment-validation/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: in Quarto-present environments (guarded), successful validation report contains detected Quarto and Typst version evidence plus floor compatibility status (D-03, D-04).
    - Test 2: in no-Quarto environments, validation tests still run and assert structured diagnostics without false positives from missing external CLI (D-02, D-05).
    - Test 3: extension-missing scenarios produce dedicated stable diagnostic codes and actionable hints before render attempts (D-04, D-06).
  </behavior>
  <action>
Expand validation-focused tests to cover both runtime envelopes specified in 06-VALIDATION: no-Quarto-safe contract checks and Quarto-present guarded evidence checks. Keep Quarto-present assertions wrapped in explicit availability guards (`skip_if_not(...)`) to preserve CRAN-safe posture.

Update `tests/testthat/test-yaml-integration.R` only where needed to assert that environment validation can be invoked as a preflight gate in integration contexts without changing existing render smoke semantics. Ensure extension-presence failures are covered with deterministic diagnostics and that test expectations remain payload-first.

This task closes D-04 coverage completeness and D-05 collect-all behavior confirmation at integration boundaries.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R"); testthat::test_file("tests/testthat/test-render-guards.R"); testthat::test_file("tests/testthat/test-yaml-integration.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-validation-environment.R` includes guarded assertions for Quarto/Typst version evidence on success.
    - `tests/testthat/test-validation-environment.R` includes explicit extension-missing diagnostic code assertions.
    - `tests/testthat/test-yaml-integration.R` preserves Quarto skip guards and still passes/skips deterministically in no-Quarto environments.
    - Combined verification command exits successfully with expected skips only for unavailable Quarto-dependent checks.
  </acceptance_criteria>
  <done>Validation behavior is proven consistent across standalone, render, and guarded integration contexts for VAL-01.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-render-guards.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R")'`
- `Rscript -e 'testthat::test_local(".")'`
</verification>

<success_criteria>
- `render_pub()` and standalone validation use one shared environment-validation path.
- Equivalent failure environments produce equivalent stable diagnostics payloads across call sites.
- Quarto-present success evidence and no-Quarto-safe diagnostics behavior are both test-covered without regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-pre-render-environment-validation/06-02-SUMMARY.md`
</output>
