---
phase: 06-pre-render-environment-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/diagnostics.R
  - R/validation_environment.R
  - NAMESPACE
  - tests/testthat/helper-validation.R
  - tests/testthat/test-validation-environment.R
  - tests/testthat/test-diagnostics-codebook.R
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - "User can invoke a standalone pre-render validator and receive a clear pass/fail result before calling render functions (D-01)."
    - "Validation failures emit typstR_diagnostics_error payloads with stable, deterministic codes for each environment check class rather than prose-only aborts (D-02, D-06)."
    - "A successful validation call returns a structured report object containing Quarto and Typst evidence that callers can inspect programmatically (D-03)."
  artifacts:
    - path: "R/validation_environment.R"
      provides: "Shared environment-validation probes, aggregation, and public validation entrypoint"
    - path: "R/diagnostics.R"
      provides: "Extended immutable codebook entries for environment validation issue classes"
    - path: "tests/testthat/test-validation-environment.R"
      provides: "Payload-first contract tests for pass/fail validation behavior"
    - path: "tests/testthat/helper-validation.R"
      provides: "Reusable source helpers for running validation tests via test_file"
  key_links:
    - from: "R/validation_environment.R"
      to: "R/diagnostics.R"
      via: "new_diagnostic() + emit_diagnostics_error() for failed checks"
      pattern: "diagnostics_codebook\(\).*emit_diagnostics_error"
    - from: "R/validation_environment.R"
      to: "inst/quarto/extensions/typstR/_extension.yml"
      via: "Quarto version-floor derivation from quarto-required"
      pattern: "quarto-required"
    - from: "tests/testthat/test-validation-environment.R"
      to: "R/validation_environment.R"
      via: "class+payload assertions for standalone validation entrypoint"
      pattern: "typstR_diagnostics_error|typstR_validation_report"
---

<objective>
Create the standalone pre-render environment validation contract and implementation substrate for Phase 6.

Purpose: Deliver VAL-01 truthfulness at the validation boundary first, so render integration in the next plan can reuse one canonical primitive without divergence.
Output: New shared environment validation module, stable diagnostics codebook extensions, and contract tests proving pass/fail/report semantics.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-pre-render-environment-validation/06-CONTEXT.md
@.planning/phases/06-pre-render-environment-validation/06-RESEARCH.md
@.planning/phases/06-pre-render-environment-validation/06-VALIDATION.md
@.planning/phases/05-structured-diagnostics-foundation/05-01-SUMMARY.md
@.planning/phases/05-structured-diagnostics-foundation/05-02-SUMMARY.md
@R/diagnostics.R
@R/render.R
@inst/quarto/extensions/typstR/_extension.yml
@tests/testthat/test-diagnostics-codebook.R

<interfaces>
From R/diagnostics.R:
```r
diagnostics_codebook <- function()
new_diagnostic <- function(code, severity, location, hint, message = NULL, details = NULL)
sort_diagnostics <- function(diagnostics)
emit_diagnostics_error <- function(diagnostics, message = "Validation failed.", hint = "Review diagnostics and retry the command.")
```

From R/render.R:
```r
quarto_available <- function()
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE, open = interactive())
```

From inst/quarto/extensions/typstR/_extension.yml:
```yaml
quarto-required: ">=1.4.11"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend diagnostics codebook and validation test harness for environment issue classes</name>
  <files>
    R/diagnostics.R
    tests/testthat/test-diagnostics-codebook.R
    tests/testthat/helper-validation.R
    tests/testthat/test-validation-environment.R
  </files>
  <read_first>
    R/diagnostics.R,
    tests/testthat/helper-diagnostics.R,
    tests/testthat/test-diagnostics-codebook.R,
    .planning/phases/06-pre-render-environment-validation/06-CONTEXT.md,
    .planning/phases/06-pre-render-environment-validation/06-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: codebook includes stable environment keys for Quarto unavailable, Typst unavailable, Quarto floor incompatible, and extension missing, each matching DIAG-<AREA>-<NNN> (D-04, D-06).
    - Test 2: codebook immutability test fails if any new environment key is remapped to a different code (D-06).
    - Test 3: validation test harness can source diagnostics + validation modules reliably when run with testthat::test_file (Wave 0 gap from 06-VALIDATION).
    - Test 4: diagnostics ordering of multi-issue environment payload remains deterministic through existing sort_diagnostics behavior (Phase 5 contract carry-forward).
  </behavior>
  <action>
Add environment-validation issue keys to `DIAGNOSTIC_CODEBOOK` in `R/diagnostics.R` with stable assignments per D-06 and keep them within the existing immutable key-reference guard pattern from Phase 5. Use an `ENV` area namespace to keep runtime/input/environment classes distinguishable without introducing a parallel registry.

Update `tests/testthat/test-diagnostics-codebook.R` to lock these new mappings and enforce that reassignment breaks tests. Create `tests/testthat/helper-validation.R` for source-path discovery of `R/validation_environment.R` and diagnostics modules so `test_file()` invocation remains deterministic (per 06-VALIDATION Wave 0). Seed `tests/testthat/test-validation-environment.R` with payload-first expectations that will drive implementation in Task 2.

Reference decisions directly in test naming/messages: D-04 minimum check classes and D-06 stable code namespace. Do not add prose-only assertions as primary checks.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-codebook.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/diagnostics.R` defines four new environment issue keys in `DIAGNOSTIC_CODEBOOK` and validates them via existing codebook guard logic.
    - `tests/testthat/test-diagnostics-codebook.R` asserts stable mappings for each new environment key.
    - `tests/testthat/helper-validation.R` exists and resolves source files robustly for direct `test_file()` runs.
    - `tests/testthat/test-validation-environment.R` exists with payload-first, class-first expectations for VAL-01 checks.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-codebook.R")'` exits successfully.
  </acceptance_criteria>
  <done>Environment diagnostics classes are contract-locked and test harness prerequisites for validation tests are in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement standalone validation entrypoint and collect-all environment probe engine</name>
  <files>
    R/validation_environment.R
    R/diagnostics.R
    NAMESPACE
    tests/testthat/helper-validation.R
    tests/testthat/test-validation-environment.R
  </files>
  <read_first>
    R/diagnostics.R,
    R/render.R,
    inst/quarto/extensions/typstR/_extension.yml,
    tests/testthat/test-render-guards.R,
    tests/testthat/test-validation-environment.R,
    .planning/phases/06-pre-render-environment-validation/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `validate_render_environment(path)` returns class `typstR_validation_report` with explicit pass state and check evidence when all required checks pass (D-01, D-03).
    - Test 2: when one or more checks fail, function raises `typstR_diagnostics_error` containing all discoverable blockers in deterministic order (D-02, D-05, D-06).
    - Test 3: report/check payload always includes Quarto availability + detected version, Typst availability + detected version/raw evidence, Quarto floor requirement + compatibility result, and extension-presence result (D-04).
    - Test 4: missing Quarto does not prevent extension-presence check from being evaluated in the same run (collect-all behavior per D-05).
  </behavior>
  <action>
Create `R/validation_environment.R` implementing one shared validation primitive and one public user entrypoint `validate_render_environment(path = ".")` per D-01. The function must run the four required checks from D-04 and aggregate failures before emission per D-05.

Implement probes with explicit evidence capture:
- Quarto availability/version via `requireNamespace("quarto", quietly = TRUE)`, `quarto::quarto_available()`, and guarded `quarto::quarto_version()` calls.
- Typst availability/version via guarded `system2(quarto::quarto_path(), c("typst", "--version"), ...)` only when Quarto is reachable.
- Quarto floor compatibility using the floor parsed from `_extension.yml` (`quarto-required`) and `quarto::quarto_available(min = floor)`.
- Extension presence by checking `_extensions/typstR/_extension.yml` under the target project path.

On failure, map each failed check to the new stable codebook keys and emit one aggregate `typstR_diagnostics_error` payload using existing diagnostics primitives (D-02, D-06). On success, return a structured report object containing per-check outcomes and detected values (D-03). Export `validate_render_environment` by updating `NAMESPACE` so users can run validation before `render_pub()`.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/validation_environment.R` exists and defines `validate_render_environment(path = ".")` with collect-all check behavior.
    - `NAMESPACE` includes `export(validate_render_environment)`.
    - `tests/testthat/test-validation-environment.R` asserts both success-report structure and aggregate diagnostic payload behavior.
    - Validation failure payloads use only codebook-backed environment codes (no ad hoc strings).
    - `Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R")'` exits successfully.
  </acceptance_criteria>
  <done>A user-invokable, payload-first standalone environment validator now satisfies the VAL-01 contract outside render flow.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-codebook.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R")'`
</verification>

<success_criteria>
- Users have a callable `validate_render_environment()` entrypoint that runs before render and returns clear pass/fail semantics.
- Environment failures emit deterministic, stable-code diagnostics via `typstR_diagnostics_error`.
- Success-path reports contain explicit Quarto/Typst/floor/extension evidence for programmatic consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/06-pre-render-environment-validation/06-01-SUMMARY.md`
</output>
