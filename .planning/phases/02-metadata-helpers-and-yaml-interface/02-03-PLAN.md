---
phase: 02-metadata-helpers-and-yaml-interface
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - inst/quarto/extensions/typstR/typst-show.typ
  - inst/quarto/extensions/typstR/typst-template.typ
  - inst/templates/workingpaper/template.qmd
autonomous: true
requirements:
  - YAML-01
  - YAML-02
  - YAML-03

must_haves:
  truths:
    - "typstR: namespace fields in YAML front matter survive the Pandoc pipeline and appear as Typst template arguments"
    - "All typstR: fields (keywords, jel, acknowledgements, report-number, funding, data-availability, code-availability) are wired through typst-show.typ"
    - "Author email, ORCID, and corresponding marker pass through typst-show.typ to the template"
    - "Affiliations with id, name, and department pass through typst-show.typ"
    - "Standard Quarto fields (toc, number-sections) continue to work alongside typstR: fields"
    - "Existing Phase 1 renders (title + authors + abstract only) still work without regression"
  artifacts:
    - path: "inst/quarto/extensions/typstR/typst-show.typ"
      provides: "Full Pandoc variable bridge for author details, affiliations, and all typstR: namespace fields"
      contains: "typstR.keywords"
    - path: "inst/quarto/extensions/typstR/typst-template.typ"
      provides: "Extended working-paper function accepting keywords, jel, acknowledgements, report-number, funding, data-availability, code-availability"
      contains: "keywords: ()"
    - path: "inst/templates/workingpaper/template.qmd"
      provides: "Updated scaffold template demonstrating typstR: namespace usage"
      contains: "typstR:"
  key_links:
    - from: "inst/quarto/extensions/typstR/typst-show.typ"
      to: "inst/quarto/extensions/typstR/typst-template.typ"
      via: "Pandoc variable interpolation passes YAML values as Typst function arguments"
      pattern: "working-paper\\.with"
    - from: "inst/templates/workingpaper/template.qmd"
      to: "inst/quarto/extensions/typstR/typst-show.typ"
      via: "YAML front matter fields consumed by Pandoc template engine"
      pattern: "typstR:"
---

<objective>
Wire all YAML metadata fields through typst-show.typ into the Typst template and update the scaffold template.

Purpose: This is the critical integration layer. R helpers produce YAML; this plan ensures that YAML reaches the Typst template. The research flagged hyphenated keys and Quarto 1.6 behavior as risks that must be validated with an actual render test.

Output: Extended typst-show.typ, extended typst-template.typ, updated template.qmd.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-CONTEXT.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-RESEARCH.md

<interfaces>
<!-- Current typst-show.typ (Phase 1 baseline): -->
```typst
#import "typst-template.typ": working-paper

#show: working-paper.with(
  $if(title)$
  title: "$title$",
  $endif$
  $if(author)$
  authors: (
    $for(author)$
    (name: "$it.name.literal$"),
    $endfor$
  ),
  $endif$
  $if(abstract)$
  abstract: [$abstract$],
  $endif$
)
```

<!-- Current typst-template.typ function signature: -->
```typst
#let working-paper(
  title: none,
  authors: (),
  abstract: none,
  body,
) = { ... }
```

<!-- Target typst-show.typ from 02-RESEARCH.md Pattern 5 -->
<!-- Target typst-template.typ signature from 02-RESEARCH.md Pattern 6 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend typst-show.typ and typst-template.typ with all metadata fields</name>
  <files>inst/quarto/extensions/typstR/typst-show.typ, inst/quarto/extensions/typstR/typst-template.typ</files>
  <read_first>
    - inst/quarto/extensions/typstR/typst-show.typ (current Pandoc variable bridge — must extend, not replace working parts)
    - inst/quarto/extensions/typstR/typst-template.typ (current monolithic template — must extend function signature)
    - .planning/phases/02-metadata-helpers-and-yaml-interface/02-RESEARCH.md (Pattern 5: exact typst-show.typ extension; Pattern 6: exact template signature; Pitfall 4: use content blocks [...] for free-text fields; Pitfall 6: hyphenated keys; Open Question 3: author.name.literal vs author.name)
  </read_first>
  <action>
    **typst-show.typ** — Replace the file with the extended version that wires all fields:

    1. Keep the `#import "typst-template.typ": working-paper` import.
    2. Extend the `#show: working-paper.with(...)` call to pass:
       - title (unchanged)
       - authors — extended per author to include email, orcid, corresponding fields using `$if(it.email)$email: "$it.email$",$endif$` etc. For name, use defensive fallback: `$if(it.name.literal)$name: "$it.name.literal$",$else$$if(it.name)$name: "$it.name$",$endif$$endif$` to handle both Quarto-normalized and direct YAML author entries.
       - affiliations — NEW block: `$if(affiliations)$` with `$for(affiliations)$` iterating over id, name, department fields.
       - abstract (unchanged, content block)
       - typstR namespace fields — all wrapped in `$if(typstR)$` outer conditional:
         - keywords: `$for(typstR.keywords)$"$it$",$endfor$` (array)
         - jel: same array pattern
         - acknowledgements: content block `[$typstR.acknowledgements$]`
         - report-number: string `"$typstR.report-number$"` — NOTE: if hyphenated key fails in render test, change to `$typstR.report_number$` or `$typstR.reportnumber$` and document the mapping
         - funding: content block `[$typstR.funding$]`
         - data-availability: content block `[$typstR.data-availability$]`
         - code-availability: content block `[$typstR.code-availability$]`

    **typst-template.typ** — Extend the `working-paper` function:

    1. Add new parameters to function signature (all with safe defaults):
       ```
       affiliations: ()
       keywords: ()
       jel: ()
       acknowledgements: none
       report-number: none
       funding: none
       data-availability: none
       code-availability: none
       ```
    2. Extend the authors rendering section to show email/ORCID/corresponding when present. For each author, check if `.at("email", default: none)` exists. Show affiliations linked by ID.
    3. After the abstract block, add conditional rendering for:
       - Keywords: if keywords is not empty, render "Keywords: " followed by comma-joined list
       - JEL: if jel is not empty, render "JEL Classification: " followed by comma-joined list
    4. After body, add conditional sections for acknowledgements, funding, data-availability, code-availability (rendered as paragraphs with bold headings).
    5. All new parameters default to `none` or `()` — existing Phase 1 renders MUST NOT break.
    6. Keep the Phase 1 comment: `// Phase 1: monolithic template -- refactored to modular in Phase 3`
    7. Use Typst syntax compatible with Quarto >= 1.4.11 bundled Typst version.

    **CRITICAL**: After writing both files, run a smoke-test render to validate:
    ```bash
    cd /tmp && mkdir -p typstr-test && cd typstr-test
    # Create a minimal .qmd with all typstR: fields populated
    # Copy the extension from the package
    # Run quarto render and check for errors
    ```
    If hyphenated keys (`report-number`, `data-availability`, `code-availability`) fail, fall back to underscore variants and document the mapping.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e '
      withr::with_tempdir({
        # Scaffold a project
        typstR::create_working_paper("smoke-test")

        # Verify extension files were copied with new content
        show_content <- readLines("smoke-test/_extensions/typstR/typst-show.typ")
        stopifnot(any(grepl("typstR", show_content)))
        stopifnot(any(grepl("affiliations", show_content)))

        tmpl_content <- readLines("smoke-test/_extensions/typstR/typst-template.typ")
        stopifnot(any(grepl("keywords", tmpl_content)))
        stopifnot(any(grepl("acknowledgements", tmpl_content)))

        cat("Extension files validated successfully\n")
      })
    '
    </automated>
  </verify>
  <acceptance_criteria>
    - inst/quarto/extensions/typstR/typst-show.typ contains `$if(typstR)$`
    - inst/quarto/extensions/typstR/typst-show.typ contains `$for(typstR.keywords)$`
    - inst/quarto/extensions/typstR/typst-show.typ contains `$for(typstR.jel)$`
    - inst/quarto/extensions/typstR/typst-show.typ contains `typstR.acknowledgements`
    - inst/quarto/extensions/typstR/typst-show.typ contains `typstR.funding`
    - inst/quarto/extensions/typstR/typst-show.typ contains `$if(affiliations)$`
    - inst/quarto/extensions/typstR/typst-show.typ contains `it.email` (author email passthrough)
    - inst/quarto/extensions/typstR/typst-show.typ contains `it.orcid` (author ORCID passthrough)
    - inst/quarto/extensions/typstR/typst-template.typ contains `keywords: ()`
    - inst/quarto/extensions/typstR/typst-template.typ contains `jel: ()`
    - inst/quarto/extensions/typstR/typst-template.typ contains `acknowledgements: none`
    - inst/quarto/extensions/typstR/typst-template.typ contains `funding: none`
    - inst/quarto/extensions/typstR/typst-template.typ contains `affiliations: ()`
    - Existing Phase 1 template still compiles (title + author name + abstract only)
  </acceptance_criteria>
  <done>typst-show.typ wires all typstR: namespace fields plus extended author/affiliation details to the template. typst-template.typ accepts and renders all new parameters with safe defaults. No regression on Phase 1 functionality.</done>
</task>

<task type="auto">
  <name>Task 2: Update scaffold template.qmd with typstR: namespace example</name>
  <files>inst/templates/workingpaper/template.qmd</files>
  <read_first>
    - inst/templates/workingpaper/template.qmd (current template — extend YAML, keep body structure)
    - BLUEPRINT.md section 8 (suggested YAML interface example)
    - .planning/phases/02-metadata-helpers-and-yaml-interface/02-RESEARCH.md (YAML serialization examples showing correct structure)
  </read_first>
  <action>
    Update inst/templates/workingpaper/template.qmd YAML front matter to demonstrate the full metadata model:

    ```yaml
    ---
    title: "Working Paper Title"
    author:
      - name: "Author One"
        email: "author.one@example.org"
        corresponding: true
        affiliation:
          - "1"
      - name: "Author Two"
        affiliation:
          - "1"
          - "2"
    affiliations:
      - id: "1"
        name: "Institute Name"
      - id: "2"
        name: "University Name"
    abstract: |
      Replace this placeholder with your abstract. This paper examines...
    bibliography: references.bib
    format:
      typstR-workingpaper: default
    typstR:
      keywords:
        - keyword one
        - keyword two
      jel:
        - C10
        - F13
      acknowledgements: |
        The authors thank...
      report-number: "WP 001"
      funding: |
        This research was supported by...
    ---
    ```

    Keep the existing document body sections (Introduction, Data and Methods, Results, Conclusion, References) unchanged. The template should be a useful starting point — use placeholder text that makes it obvious what to replace.

    Do NOT add `data-availability` or `code-availability` to the scaffold (they are optional and less common; keep the template focused).
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e '
      lines <- readLines("inst/templates/workingpaper/template.qmd")
      stopifnot(any(grepl("typstR:", lines)))
      stopifnot(any(grepl("keywords:", lines)))
      stopifnot(any(grepl("jel:", lines)))
      stopifnot(any(grepl("affiliations:", lines)))
      stopifnot(any(grepl("report-number:", lines)))
      stopifnot(any(grepl("email:", lines)))
      cat("template.qmd validated successfully\n")
    '
    </automated>
  </verify>
  <acceptance_criteria>
    - inst/templates/workingpaper/template.qmd contains `typstR:` (namespace block)
    - inst/templates/workingpaper/template.qmd contains `keywords:` under typstR block
    - inst/templates/workingpaper/template.qmd contains `jel:` under typstR block
    - inst/templates/workingpaper/template.qmd contains `acknowledgements:` under typstR block
    - inst/templates/workingpaper/template.qmd contains `report-number:` under typstR block
    - inst/templates/workingpaper/template.qmd contains `affiliations:` with at least 2 entries
    - inst/templates/workingpaper/template.qmd contains `email:` in author block
    - inst/templates/workingpaper/template.qmd contains `corresponding: true`
    - inst/templates/workingpaper/template.qmd still contains `format:` and `typstR-workingpaper`
    - inst/templates/workingpaper/template.qmd still contains `bibliography: references.bib`
  </acceptance_criteria>
  <done>template.qmd YAML demonstrates the full metadata model including authors with affiliations/email, typstR: namespace with keywords, JEL, acknowledgements, report number, and funding.</done>
</task>

</tasks>

<verification>
- Extension files contain all expected Pandoc variable interpolations
- Template.qmd demonstrates the full metadata schema
- Scaffolded project gets the updated extension files via create_working_paper()
- `Rscript -e 'devtools::document(); devtools::check(args = "--no-tests")'` passes
</verification>

<success_criteria>
- typst-show.typ wires all typstR: namespace fields to template arguments
- typst-template.typ accepts keywords, jel, acknowledgements, report-number, funding, data-availability, code-availability with safe defaults
- Author email, ORCID, corresponding pass through to template
- Affiliations pass through with id, name, department
- template.qmd shows full metadata model as a useful starting point
- No Phase 1 regression
</success_criteria>

<output>
After completion, create `.planning/phases/02-metadata-helpers-and-yaml-interface/02-03-SUMMARY.md`
</output>
