---
phase: 02-metadata-helpers-and-yaml-interface
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - tests/testthat/test-yaml-integration.R
autonomous: false
requirements:
  - YAML-01
  - YAML-02
  - YAML-03

must_haves:
  truths:
    - "A .qmd with typstR: namespace fields renders to PDF without errors"
    - "Keywords, JEL codes, acknowledgements, and report number appear in the rendered PDF"
    - "Author email, ORCID, and affiliation details appear in the rendered PDF"
    - "Standard Quarto fields (toc, number-sections) work alongside typstR: fields"
  artifacts:
    - path: "tests/testthat/test-yaml-integration.R"
      provides: "Integration tests for full YAML-to-PDF pipeline"
      contains: "skip_if_not"
  key_links:
    - from: "tests/testthat/test-yaml-integration.R"
      to: "inst/quarto/extensions/typstR/typst-show.typ"
      via: "Render test validates Pandoc variable passthrough"
      pattern: "quarto::quarto_render"
---

<objective>
Create integration tests that validate the full YAML-to-PDF pipeline and verify the rendered output with a human checkpoint.

Purpose: The research flagged that unit tests for R helpers are necessary but not sufficient. This plan validates the end-to-end pipeline: R metadata -> YAML -> Pandoc -> typst-show.typ -> typst-template.typ -> PDF. It also validates the hyphenated key behavior and Quarto version compatibility.

Output: tests/testthat/test-yaml-integration.R with guarded integration tests; human verification of rendered PDF.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-CONTEXT.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-RESEARCH.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-01-SUMMARY.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-02-SUMMARY.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 (metadata_helpers.R) -->
```r
author(name, affiliation = NULL, email = NULL, orcid = NULL, corresponding = FALSE)
affiliation(id, name, department = NULL, address = NULL, country = NULL)
manuscript_meta(authors, affiliations = list(), keywords = NULL, jel = NULL,
                acknowledgements = NULL, report_number = NULL, funding = NULL,
                data_availability = NULL, code_availability = NULL)
```

<!-- From Plan 02 (pub_helpers.R) -->
```r
keywords(...)
jel_codes(...)
report_number(text)
funding(text)
```

<!-- From Plan 03 (typst-show.typ) — Pandoc variable patterns -->
<!-- $typstR.keywords$, $typstR.jel$, $typstR.acknowledgements$, $typstR.report-number$, etc. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for YAML-to-PDF pipeline</name>
  <files>tests/testthat/test-yaml-integration.R</files>
  <read_first>
    - inst/quarto/extensions/typstR/typst-show.typ (verify the Pandoc variable names used)
    - inst/quarto/extensions/typstR/typst-template.typ (verify the template function signature)
    - inst/templates/workingpaper/template.qmd (verify the current scaffold YAML)
    - R/render.R (render_pub and render_working_paper signatures)
    - R/metadata_helpers.R (verify actual function signatures from Plan 01)
    - R/pub_helpers.R (verify actual function signatures from Plan 02)
  </read_first>
  <action>
    Create tests/testthat/test-yaml-integration.R with integration tests guarded by `skip_if_not(quarto::quarto_available())`:

    **Test 1: "scaffold renders without errors"**
    - Use withr::with_tempdir to create an isolated environment
    - Call typstR::create_working_paper("test-project")
    - Call typstR::render_working_paper("test-project", quiet = TRUE, open = FALSE)
    - Verify: PDF file exists at test-project/template.pdf
    - This validates that the updated template.qmd (with all typstR: fields) renders clean

    **Test 2: "typstR namespace fields reach the PDF"**
    - Use withr::with_tempdir
    - Create a minimal .qmd file with all typstR: namespace fields populated:
      ```yaml
      typstR:
        keywords: [trade, policy]
        jel: [F10, L52]
        acknowledgements: "We thank the reviewers."
        report-number: "WP 001"
        funding: "ERC Grant"
      ```
    - Copy the extension from inst/quarto/extensions/typstR/ into _extensions/typstR/
    - Render with quarto::quarto_render(quiet = TRUE)
    - Verify: PDF exists (render succeeded = fields did not cause errors)
    - NOTE: Cannot easily verify PDF text content without pdftools, but a successful render proves the Pandoc variable wiring is syntactically correct

    **Test 3: "standard Quarto fields work alongside typstR fields"**
    - Same setup as Test 2 but add `toc: true` and `number-sections: true` under `format: typstR-workingpaper:`
    - Verify: render succeeds without errors (proves no conflict)

    **Test 4: "hyphenated key validation"**
    - Specifically test that `report-number`, `data-availability`, `code-availability` pass through
    - If any fail, this test documents the failure and the fallback approach

    All tests use `skip_if_not(quarto::quarto_available())` as the first line so they are safe for CRAN (TEST-04).
    Use `withr::with_tempdir()` for isolation.
    Set `open = FALSE` on all render calls.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'devtools::test(filter = "integration")'</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-yaml-integration.R contains `skip_if_not(quarto::quarto_available())`
    - tests/testthat/test-yaml-integration.R contains `withr::with_tempdir`
    - tests/testthat/test-yaml-integration.R contains `typstR::create_working_paper` or `create_working_paper`
    - tests/testthat/test-yaml-integration.R contains `quarto::quarto_render` or `render_working_paper` or `render_pub`
    - tests/testthat/test-yaml-integration.R contains `expect_true(fs::file_exists` or `expect_true(file.exists` (PDF existence check)
    - tests/testthat/test-yaml-integration.R contains `typstR:` or `typstR` in a YAML string (namespace field test)
    - tests/testthat/test-yaml-integration.R contains `report-number` (hyphenated key test)
    - All integration tests pass (or skip gracefully if Quarto unavailable)
  </acceptance_criteria>
  <done>Integration tests validate that the full pipeline works: scaffolded project renders, typstR: namespace fields do not cause errors, standard Quarto fields coexist, and hyphenated keys are validated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify rendered PDF shows all metadata fields</name>
  <files>none</files>
  <action>
    Human visually inspects the rendered PDF to confirm all metadata fields appear correctly. The executor scaffolds a test project and renders it, then pauses for human review.

    Steps the executor takes before pausing:
    1. Run `Rscript -e 'library(typstR); create_working_paper("/tmp/meta-test", title = "Metadata Test Paper"); render_working_paper("/tmp/meta-test", open = FALSE, quiet = FALSE)'`
    2. Report the path to the PDF: /tmp/meta-test/template.pdf
    3. Pause for human review
  </action>
  <verify>Human confirms all fields visible in PDF</verify>
  <done>Human approves that title, authors with email/affiliation, affiliations, abstract, keywords, JEL codes, acknowledgements, report number, and funding all appear in the rendered PDF.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'devtools::test()'` — full test suite passes (unit + integration)
- `Rscript -e 'devtools::document(); devtools::check(args = "--no-tests")'` — R CMD check clean
- Human confirms rendered PDF shows all metadata fields
</verification>

<success_criteria>
- Integration tests pass with Quarto available
- Integration tests skip gracefully without Quarto (CRAN-safe)
- Scaffold renders to PDF with all metadata fields visible
- Standard Quarto fields work alongside typstR: fields
- Hyphenated key behavior validated (report-number, data-availability, code-availability)
- Human approves the rendered output
</success_criteria>

<output>
After completion, create `.planning/phases/02-metadata-helpers-and-yaml-interface/02-04-SUMMARY.md`
</output>
