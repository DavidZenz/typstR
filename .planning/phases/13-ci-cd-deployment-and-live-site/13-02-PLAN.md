---
phase: 13-ci-cd-deployment-and-live-site
plan: 13-02
type: execute
wave: 2
depends_on: [13-01]
files_modified: []
autonomous: false
requirements: [SITE-01, SHIP-03]

must_haves:
  truths:
    - "GitHub Actions pkgdown run is successful"
    - "gh-pages branch exists and is populated"
    - "Live site loads at https://davidzenz.github.io/typstR/"
    - "Nav links, articles, and changelog are functional"
  artifacts:
    - path: "gh-pages (branch)"
      provides: "Remote deployment source"
---

<objective>
Trigger the first automated deployment, configure GitHub Pages, and perform final smoke testing of the live site.

Purpose: Final validation of the public-facing documentation.
Output: Live public site.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ci-cd-deployment-and-live-site/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push changes and monitor deployment</name>
  <files></files>
  <action>
    1. Commit and push all pending changes (from Phase 11, 12, and 13-01) to the `main` branch.
    2. Monitor the GitHub Actions tab for the "pkgdown" workflow run.
    3. Verify that the run completes successfully and pushes to a `gh-pages` branch.
  </action>
  <verify>
    <automated>git ls-remote --heads origin gh-pages</automated>
  </verify>
  <done>Changes pushed and gh-pages branch updated by GHA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Automated deployment workflow and gh-pages branch.</what-built>
  <how-to-verify>
    1. Go to your GitHub repository: https://github.com/davidzenz/typstR
    2. Go to **Settings -> Pages**.
    3. Ensure "Build and deployment" -> "Source" is set to "Deploy from a branch".
    4. Ensure "Branch" is set to `gh-pages` and folder is `/(root)`.
    5. Save if necessary.
    6. Wait for the "github-pages" deployment job to finish in the Actions tab.
  </how-to-verify>
  <resume-signal>Settings verified and site is live</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Automated Live Site Check</name>
  <files></files>
  <action>
    Perform a basic reachability check on the live site URL and key endpoints.
  </action>
  <verify>
    <automated>
      curl -sL --fail https://davidzenz.github.io/typstR/ && \
      curl -sL --fail https://davidzenz.github.io/typstR/articles/getting-started.html && \
      curl -sL --fail https://davidzenz.github.io/typstR/reference/index.html && \
      curl -sL --fail https://davidzenz.github.io/typstR/news/index.html
    </automated>
  </verify>
  <done>Live site endpoints are reachable (200 OK).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Live pkgdown documentation site.</what-built>
  <how-to-verify>
    Visit https://davidzenz.github.io/typstR/ and perform final smoke tests:
    - [ ] Homepage loads correctly with "typstR" title.
    - [ ] "Get Started" nav link works and leads to the article.
    - [ ] Reference index is grouped into sections.
    - [ ] All 3 articles (Getting Started, Working Papers, Customizing Branding) are readable.
    - [ ] Changelog (News) page is present and shows v1.0, v1.1, v1.2 entries.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA → GitHub Pages | Internal GitHub deployment mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Information Disclosure | Live Site | accept | Content is public documentation by design. No secrets or PII are included in the build. |
</threat_model>

<verification>
Live site reachability confirmed via curl and human review.
</verification>

<success_criteria>
Live site 100% functional at the public URL.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ci-cd-deployment-and-live-site/13-02-SUMMARY.md`
</output>
