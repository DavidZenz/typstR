---
phase: 13-ci-cd-deployment-and-live-site
plan: 13-01
type: execute
wave: 1
depends_on: []
files_modified: [.github/workflows/pkgdown.yaml, .Rbuildignore]
autonomous: true
requirements: [SHIP-03]

must_haves:
  truths:
    - ".github/workflows/pkgdown.yaml exists"
    - "Workflow includes permissions: contents: write"
    - ".github is not in .Rbuildignore (per user request)"
  artifacts:
    - path: ".github/workflows/pkgdown.yaml"
      provides: "GitHub Actions deployment workflow"
---

<objective>
Generate the GitHub Actions workflow for automatic pkgdown deployment and ensure build-ignore settings match user requirements.

Purpose: Automated deployment is the final piece of the v1.2 milestone.
Output: .github/workflows/pkgdown.yaml
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate pkgdown workflow</name>
  <files>.github/workflows/pkgdown.yaml</files>
  <action>
    Run `Rscript -e 'usethis::use_pkgdown_github_pages()'` to generate the standard pkgdown deployment workflow.
    This will create `.github/workflows/pkgdown.yaml`.
  </action>
  <verify>
    <automated>ls .github/workflows/pkgdown.yaml</automated>
  </verify>
  <done>Workflow file generated.</done>
</task>

<task type="auto">
  <name>Task 2: Configure workflow permissions and Rbuildignore</name>
  <files>.github/workflows/pkgdown.yaml, .Rbuildignore</files>
  <action>
    1. Verify `.github/workflows/pkgdown.yaml` includes `permissions: contents: write`. Add it if missing (though `usethis` usually includes it now).
    2. Per user instructions: Check `.Rbuildignore` and ensure `.github` is NOT ignored. 
       - If `^\.github$` or similar exists in `.Rbuildignore`, remove it.
       - Note: While standard for CRAN packages to ignore .github, we are following specific user request for this phase.
  </action>
  <verify>
    <automated>grep "contents: write" .github/workflows/pkgdown.yaml && ! grep "^\.github$" .Rbuildignore</automated>
  </verify>
  <done>Permissions verified and .Rbuildignore updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main branch → GHA | Pushes to main trigger elevated GHA permissions |
| GHA → gh-pages | Workflow writes built site to gh-pages branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Elevation of Privilege | GHA Workflow | mitigate | Use standard `r-lib/actions/setup-r` and restricted `contents: write` permission rather than full `repo` scope |
</threat_model>

<verification>
Check that `.github/workflows/pkgdown.yaml` exists and `.Rbuildignore` does not contain `.github`.
</verification>

<success_criteria>
Workflow file created with correct permissions.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ci-cd-deployment-and-live-site/13-01-SUMMARY.md`
</output>
