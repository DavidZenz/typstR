---
phase: 04-tests-documentation-and-cran-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-metadata-helpers.R
  - tests/testthat/test-pub-helpers.R
  - tests/testthat/test-notes-helpers.R
  - tests/testthat/test-yaml-integration.R
  - tests/testthat/test-render-guards.R
  - tests/testthat/test-scaffolding.R
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "Pure-R helper tests run without Quarto and cover metadata, publication, and notes helpers."
    - "Project creation for working paper, article, and policy brief is verified in isolated temp directories."
    - "Quarto-dependent integration tests skip cleanly when Quarto is unavailable and exercise render paths when it is available."
  artifacts:
    - path: "tests/testthat/test-metadata-helpers.R"
      provides: "Expanded helper validation coverage for author, affiliation, and manuscript metadata"
    - path: "tests/testthat/test-render-guards.R"
      provides: "No-Quarto-safe tests for render wrapper guard behavior"
    - path: "tests/testthat/test-scaffolding.R"
      provides: "Filesystem checks for all three scaffolding functions"
  key_links:
    - from: "tests/testthat/test-scaffolding.R"
      to: "R/create_working_paper.R"
      via: "create_working_paper() tempdir assertions"
      pattern: "create_working_paper"
    - from: "tests/testthat/test-render-guards.R"
      to: "R/render.R"
      via: "mocked quarto::quarto_available() guard assertions"
      pattern: "render_pub|render_working_paper"
    - from: "tests/testthat/test-yaml-integration.R"
      to: "quarto::quarto_available()"
      via: "skip_if_not guard on render-dependent tests"
      pattern: "skip_if_not\\(quarto::quarto_available\\(\\)\\)"
---

<objective>
Raise the Phase 4 test baseline to cover pure helper logic, no-Quarto-safe render guards, and scaffolding for all three formats.

Purpose: Lock in CRAN-safe verification before documentation and final hardening work land.
Output: Expanded testthat coverage with explicit Quarto guards and dedicated scaffolding tests.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-RESEARCH.md
@.planning/phases/02-metadata-helpers-and-yaml-interface/02-04-SUMMARY.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-05-SUMMARY.md

<interfaces>
From [NAMESPACE](/Users/davidzenz/R/typstR/NAMESPACE):
```r
export(affiliation)
export(appendix_title)
export(author)
export(code_availability)
export(create_article)
export(create_policy_brief)
export(create_working_paper)
export(data_availability)
export(fig_note)
export(funding)
export(jel_codes)
export(keywords)
export(manuscript_meta)
export(render_pub)
export(render_working_paper)
export(report_number)
export(tab_note)
```

From [R/render.R](/Users/davidzenz/R/typstR/R/render.R):
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE,
                       open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE,
                                 open = interactive())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand pure-R helper coverage per D-09 and D-10</name>
  <files>
    tests/testthat/test-metadata-helpers.R
    tests/testthat/test-pub-helpers.R
    tests/testthat/test-notes-helpers.R
    tests/testthat/test-render-guards.R
  </files>
  <read_first>
    tests/testthat/test-metadata-helpers.R,
    tests/testthat/test-pub-helpers.R,
    tests/testthat/test-notes-helpers.R,
    R/metadata_helpers.R,
    R/pub_helpers.R,
    R/notes_helpers.R,
    R/render.R,
    .planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: invalid scalar inputs for `author()`, `affiliation()`, `report_number()`, `funding()`, `data_availability()`, and `code_availability()` raise the documented cli error patterns.
    - Test 2: `manuscript_meta()` preserves hyphenated `typstR` keys and omits the `typstR` block entirely when no optional fields are supplied.
    - Test 3: `appendix_title()` validates `level`, and `render_pub()` / `render_working_paper()` abort cleanly when `quarto::quarto_available()` is `FALSE`.
  </behavior>
  <action>
Extend the existing helper test files instead of rewriting them, per D-10. Add concrete missing cases for scalar validation, omitted optional fields, and serialized `typstR` key names in `tests/testthat/test-metadata-helpers.R`, `tests/testthat/test-pub-helpers.R`, and `tests/testthat/test-notes-helpers.R`.

Create `tests/testthat/test-render-guards.R` for TEST-02. In that file, assert the no-Quarto path in `render_pub()` and `render_working_paper()` without invoking a real render. Use a temporary/mock approach that forces `quarto::quarto_available()` to return `FALSE`, and check for the exact current abort message fragment `Quarto is not installed or not on PATH.` so the tests exercise the intended CRAN-safe guard.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-metadata-helpers.R"); testthat::test_file("tests/testthat/test-pub-helpers.R"); testthat::test_file("tests/testthat/test-notes-helpers.R"); testthat::test_file("tests/testthat/test-render-guards.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-render-guards.R` exists and contains both `render_pub` and `render_working_paper`.
    - `tests/testthat/test-render-guards.R` contains the string `Quarto is not installed or not on PATH.`.
    - `tests/testthat/test-metadata-helpers.R` contains assertions for `report-number` and `code-availability`.
    - `tests/testthat/test-pub-helpers.R` contains tests for `data_availability()` and `code_availability()` error handling.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-render-guards.R")'` exits successfully.
  </acceptance_criteria>
  <done>Helper and no-Quarto guard coverage explicitly exercise the current exported behavior without requiring Quarto or network access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scaffolding and guarded render smoke tests per D-07, D-09, and D-10</name>
  <files>
    tests/testthat/test-scaffolding.R
    tests/testthat/test-yaml-integration.R
  </files>
  <read_first>
    tests/testthat/test-yaml-integration.R,
    R/create_working_paper.R,
    R/create_article.R,
    R/create_policy_brief.R,
    inst/templates/workingpaper/template.qmd,
    inst/templates/article/template.qmd,
    inst/templates/policy-brief/template.qmd,
    .planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: each scaffolding function creates `template.qmd`, `_quarto.yml`, `references.bib`, and `_extensions/typstR/` in an isolated tempdir.
    - Test 2: each scaffolding function writes the expected format-specific placeholder title and format-variant YAML markers.
    - Test 3: render-dependent smoke tests remain guarded with `skip_if_not(quarto::quarto_available())` and include article and policy-brief render paths in addition to the working paper path.
  </behavior>
  <action>
Create `tests/testthat/test-scaffolding.R` for TEST-03. Cover `create_working_paper()`, `create_article()`, and `create_policy_brief()` in temp directories with `open = FALSE`. Assert exact file creation, extension copy, placeholder title replacement when `title` is supplied, and the format-specific YAML markers already present in each starter `template.qmd`.

Then extend `tests/testthat/test-yaml-integration.R` so TEST-04 covers all three formats. Preserve the existing `skip_if_not(quarto::quarto_available())` pattern as the first executable line in every render-dependent test, per D-09. Add concise article and policy-brief smoke renders using the existing bundled templates rather than inventing new fixtures.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R"); testthat::test_file("tests/testthat/test-yaml-integration.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-scaffolding.R` exists and contains `create_working_paper`, `create_article`, and `create_policy_brief`.
    - `tests/testthat/test-scaffolding.R` contains checks for `template.qmd`, `_quarto.yml`, `references.bib`, and `_extensions/typstR`.
    - `tests/testthat/test-yaml-integration.R` contains at least one guarded smoke test for `format-variant: article`.
    - `tests/testthat/test-yaml-integration.R` contains at least one guarded smoke test for `format-variant: brief`.
    - `rg -n 'skip_if_not\\(quarto::quarto_available\\(\\)\\)' tests/testthat/test-yaml-integration.R` finds every render-dependent test block.
  </acceptance_criteria>
  <done>Scaffolding is explicitly tested for all formats, and all render-dependent smoke tests remain CRAN-safe and Quarto-guarded.</done>
</task>

</tasks>

<verification>
Focused testthat runs for helper coverage, render guards, scaffolding, and integration tests all pass. No test in this plan requires Quarto unless it is explicitly guarded with `skip_if_not(quarto::quarto_available())`.
</verification>

<success_criteria>
- `TEST-01` through `TEST-04` are covered by checked-in test files.
- Working-paper, article, and policy-brief scaffolding are all asserted in tempdir-based tests.
- No-Quarto runtime behavior is tested directly for render wrappers.
- Render smoke tests still skip cleanly on CRAN machines without Quarto.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-documentation-and-cran-hardening/04-01-SUMMARY.md`
</output>
