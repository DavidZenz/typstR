---
phase: 04-tests-documentation-and-cran-hardening
plan: 06
type: execute
wave: 5
depends_on: ["04-01", "04-05"]
files_modified:
  - DESCRIPTION
  - .Rbuildignore
  - NAMESPACE
  - man/affiliation.Rd
  - man/appendix_title.Rd
  - man/author.Rd
  - man/code_availability.Rd
  - man/create_article.Rd
  - man/create_policy_brief.Rd
  - man/create_working_paper.Rd
  - man/data_availability.Rd
  - man/fig_note.Rd
  - man/funding.Rd
  - man/jel_codes.Rd
  - man/keywords.Rd
  - man/manuscript_meta.Rd
  - man/render_pub.Rd
  - man/render_working_paper.Rd
  - man/report_number.Rd
  - man/tab_note.Rd
  - man/typstR-package.Rd
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "The package passes `R CMD check --as-cran` with zero errors, warnings, and notes on a machine without Quarto."
    - "The final CRAN pass only requires packaging or generated-doc repairs, not reopening the whole phase."
    - "A faster automated gate runs before the full CRAN check so failures surface earlier than the slowest command."
  artifacts:
    - path: "typstR_*.tar.gz"
      provides: "Source package tarball produced by the regeneration plan"
    - path: "NAMESPACE"
      provides: "Generated namespace after any final packaging/doc repair"
    - path: "man/render_pub.Rd"
      provides: "Generated Rd file aligned with final package check state"
  key_links:
    - from: "R CMD check --as-cran"
      to: "DESCRIPTION, .Rbuildignore, NAMESPACE, man/"
      via: "bounded repair loop for packaging and generated-doc failures"
      pattern: "check"
    - from: "Rscript -e 'testthat::test_dir(...)'"
      to: "typstR_*.tar.gz"
      via: "fast gate before slow CRAN check"
      pattern: "testthat"
---

<objective>
Run the final package-level hardening loop with a bounded repair surface.

Purpose: Isolate the slowest verification step into its own plan and keep any last fixes constrained to packaging metadata and generated-doc artifacts instead of reopening README, vignette prose, tests, or source implementation wholesale.
Output: A clean `R CMD check --as-cran` result.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-RESEARCH.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-VALIDATION.md
@DESCRIPTION
@.Rbuildignore
@NAMESPACE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the fast package gate before the full CRAN check</name>
  <files>
    DESCRIPTION
    .Rbuildignore
    NAMESPACE
    man/affiliation.Rd
    man/appendix_title.Rd
    man/author.Rd
    man/code_availability.Rd
    man/create_article.Rd
    man/create_policy_brief.Rd
    man/create_working_paper.Rd
    man/data_availability.Rd
    man/fig_note.Rd
    man/funding.Rd
    man/jel_codes.Rd
    man/keywords.Rd
    man/manuscript_meta.Rd
    man/render_pub.Rd
    man/render_working_paper.Rd
    man/report_number.Rd
    man/tab_note.Rd
    man/typstR-package.Rd
  </files>
  <read_first>
    .planning/phases/04-tests-documentation-and-cran-hardening/04-VALIDATION.md,
    DESCRIPTION,
    .Rbuildignore,
    NAMESPACE,
    man/render_pub.Rd,
    man/create_working_paper.Rd,
    man/typstR-package.Rd
  </read_first>
  <action>
Run the faster preflight gate before the slow `R CMD check --as-cran`:
1. `Rscript -e 'testthat::test_dir("tests/testthat", reporter = "summary")'`
2. `Rscript -e 'tools::checkRdContents("man")'`

If this gate fails, fix only packaging metadata or generated-doc alignment problems in the files listed for this plan. If the failure clearly requires reopening tests, vignette prose, README, or R source implementation, stop and surface that the issue escaped the intended repair surface rather than silently expanding scope.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_dir("tests/testthat", reporter = "summary")' && Rscript -e 'tools::checkRdContents("man")'</automated>
  </verify>
  <acceptance_criteria>
    - `Rscript -e 'testthat::test_dir("tests/testthat", reporter = "summary")'` exits successfully.
    - `Rscript -e 'tools::checkRdContents("man")'` exits successfully.
    - any fixes made during this gate are limited to `DESCRIPTION`, `.Rbuildignore`, `NAMESPACE`, or `man/`.
  </acceptance_criteria>
  <done>The package clears a fast automated gate before attempting the slow CRAN check, reducing latency for obvious failures.</done>
</task>

<task type="auto">
  <name>Task 2: Run `R CMD check --as-cran` with a bounded packaging/doc repair loop</name>
  <files>
    DESCRIPTION
    .Rbuildignore
    NAMESPACE
    man/affiliation.Rd
    man/appendix_title.Rd
    man/author.Rd
    man/code_availability.Rd
    man/create_article.Rd
    man/create_policy_brief.Rd
    man/create_working_paper.Rd
    man/data_availability.Rd
    man/fig_note.Rd
    man/funding.Rd
    man/jel_codes.Rd
    man/keywords.Rd
    man/manuscript_meta.Rd
    man/render_pub.Rd
    man/render_working_paper.Rd
    man/report_number.Rd
    man/tab_note.Rd
    man/typstR-package.Rd
  </files>
  <read_first>
    .planning/phases/04-tests-documentation-and-cran-hardening/04-VALIDATION.md,
    DESCRIPTION,
    .Rbuildignore,
    NAMESPACE,
    man/render_pub.Rd,
    man/create_working_paper.Rd,
    man/typstR-package.Rd
  </read_first>
  <action>
Run `R CMD check --as-cran typstR_*.tar.gz`.

If `R CMD check` reports issues, only repair packaging metadata or generated-doc problems inside `DESCRIPTION`, `.Rbuildignore`, `NAMESPACE`, or `man/`, then rerun the fast gate from Task 1 and the full check. Examples of allowed fixes here: stale Rd aliases, generated-doc mismatches, missing vignette metadata in `DESCRIPTION`, or hidden-file/build-ignore notes. Do not reopen README, vignette prose, tests, or `R/` source files in this plan.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && R CMD check --as-cran typstR_*.tar.gz</automated>
  </verify>
  <acceptance_criteria>
    - `R CMD check --as-cran typstR_*.tar.gz` completes successfully.
    - the final check result reports zero errors, zero warnings, and zero notes.
    - any edits made during the repair loop stay within `DESCRIPTION`, `.Rbuildignore`, `NAMESPACE`, and `man/`.
  </acceptance_criteria>
  <done>The package passes the final CRAN check without broadening the repair loop beyond packaging metadata and generated documentation artifacts.</done>
</task>

</tasks>

<verification>
Run the fast gate first, then the full CRAN check. The final plan is only complete when the full check is clean and the repair surface stayed bounded.
</verification>

<success_criteria>
- `R CMD check --as-cran` passes with zero errors, warnings, and notes.
- The final hardening loop does not reopen README, vignette prose, tests, or source implementation files.
- Nyquist feedback latency is improved by the fast pre-check gate before the slowest command.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-documentation-and-cran-hardening/04-06-SUMMARY.md`
</output>
