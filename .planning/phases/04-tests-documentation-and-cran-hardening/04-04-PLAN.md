---
phase: 04-tests-documentation-and-cran-hardening
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - DESCRIPTION
  - R/create_working_paper.R
  - R/create_article.R
  - R/create_policy_brief.R
  - R/render.R
  - R/metadata_helpers.R
  - R/pub_helpers.R
  - R/notes_helpers.R
  - R/typstR-package.R
autonomous: true
requirements:
  - DOCS-05
must_haves:
  truths:
    - "Every exported function has source-level roxygen with at least one example."
    - "Guarded examples are used for scaffold/render paths that would require Quarto or filesystem side effects."
    - "The package has a meaningful package-level documentation source before regeneration."
  artifacts:
    - path: "DESCRIPTION"
      provides: "Vignette build metadata if needed for the new Rmd vignettes"
    - path: "R/render.R"
      provides: "Guarded render examples"
    - path: "R/metadata_helpers.R"
      provides: "Runnable helper examples for metadata workflows"
    - path: "R/typstR-package.R"
      provides: "Package-level roxygen source"
  key_links:
    - from: "NAMESPACE"
      to: "R/*.R"
      via: "each export must have a corresponding source roxygen block"
      pattern: "export\\("
    - from: "R/render.R"
      to: "DESCRIPTION"
      via: "guarded examples respect no-Quarto check behavior"
      pattern: "quarto"
---

<objective>
Complete source-level roxygen and example coverage for the full exported API before regeneration and package checks.

Purpose: Give DOCS-05 a dedicated owner with concrete source-level acceptance criteria before any generated files or final hardening loop.
Output: Updated roxygen blocks in `R/` sources plus any `DESCRIPTION` vignette metadata needed for later regeneration.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-RESEARCH.md
@DESCRIPTION
@NAMESPACE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete source roxygen coverage for every export per DOCS-05</name>
  <files>
    DESCRIPTION
    R/create_working_paper.R
    R/create_article.R
    R/create_policy_brief.R
    R/render.R
    R/metadata_helpers.R
    R/pub_helpers.R
    R/notes_helpers.R
    R/typstR-package.R
  </files>
  <read_first>
    DESCRIPTION,
    NAMESPACE,
    R/create_working_paper.R,
    R/create_article.R,
    R/create_policy_brief.R,
    R/render.R,
    R/metadata_helpers.R,
    R/pub_helpers.R,
    R/notes_helpers.R,
    R/typstR-package.R,
    .planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
  </read_first>
  <action>
Audit every export listed in `NAMESPACE` and ensure the corresponding roxygen block in `R/` includes a clear description, parameter docs, return value, and at least one example. Keep pure helpers runnable where feasible. Guard filesystem- or Quarto-dependent examples with `\\dontrun{}` or equivalent so they remain safe on machines without Quarto, per D-11.

Be explicit about current behavior:
- `create_*()` examples should show scaffold commands with `open = FALSE` and be guarded.
- `render_pub()` and `render_working_paper()` examples must be guarded because they require Quarto.
- `author()`, `affiliation()`, `manuscript_meta()`, `keywords()`, `jel_codes()`, `report_number()`, `funding()`, `data_availability()`, `code_availability()`, `fig_note()`, `tab_note()`, and `appendix_title()` must each have at least one source example.
- If `R/typstR-package.R` lacks a package-level roxygen block, add one.

If needed for the new `Rmd` vignettes, update `DESCRIPTION` with the vignette build metadata now so the later regeneration plan can run without guessing.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && rg -n "^#' @examples|create_working_paper <-|create_article <-|create_policy_brief <-|render_pub <-|render_working_paper <-|author <-|affiliation <-|manuscript_meta <-|keywords <-|jel_codes <-|report_number <-|funding <-|data_availability <-|code_availability <-|fig_note <-|tab_note <-|appendix_title <-" R/create_working_paper.R R/create_article.R R/create_policy_brief.R R/render.R R/metadata_helpers.R R/pub_helpers.R R/notes_helpers.R R/typstR-package.R</automated>
  </verify>
  <acceptance_criteria>
    - every export listed in `NAMESPACE` appears in a roxygen block in the `R/` sources.
    - `R/render.R` contains `@examples` for both `render_pub` and `render_working_paper`.
    - `R/create_working_paper.R`, `R/create_article.R`, and `R/create_policy_brief.R` each contain guarded examples with `open = FALSE`.
    - `R/metadata_helpers.R` contains examples for `author()`, `affiliation()`, and `manuscript_meta()`.
    - `R/pub_helpers.R` and `R/notes_helpers.R` each contain at least one `@examples` block covering every exported helper defined there.
    - `R/typstR-package.R` contains a package-level roxygen block if one was previously missing.
    - if vignette build metadata is required, `DESCRIPTION` contains it before the regeneration plan runs.
  </acceptance_criteria>
  <done>All exported functions are documented at the source level with examples appropriate to their runtime requirements, and the later regeneration plan has the metadata it needs.</done>
</task>

</tasks>

<verification>
Source roxygen coverage is complete before any generated docs are touched, and the plan leaves definitive Rd proof to the next regeneration/build plan.
</verification>

<success_criteria>
- `DOCS-05` is satisfied at the source level with concrete examples for every export.
- The subsequent regeneration plan can prove DOCS-05 definitively via generated Rd outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-documentation-and-cran-hardening/04-04-SUMMARY.md`
</output>
