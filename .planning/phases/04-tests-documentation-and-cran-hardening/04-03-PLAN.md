---
phase: 04-tests-documentation-and-cran-hardening
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - vignettes/getting-started.Rmd
  - vignettes/working-papers.Rmd
  - vignettes/customizing-branding.Rmd
  - R/create_working_paper.R
  - R/create_article.R
  - R/create_policy_brief.R
  - R/render.R
  - R/metadata_helpers.R
  - R/pub_helpers.R
  - R/notes_helpers.R
  - R/typstR-package.R
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
must_haves:
  truths:
    - "Users can read short task-oriented vignettes for getting started, working papers, and branding without needing Quarto at check time."
    - "Every exported function has roxygen documentation with at least one example."
    - "Render-dependent examples are guarded so the docs remain safe on machines without Quarto."
  artifacts:
    - path: "vignettes/getting-started.Rmd"
      provides: "Full scaffold-to-render walkthrough"
    - path: "vignettes/working-papers.Rmd"
      provides: "Focused working-paper guide"
    - path: "vignettes/customizing-branding.Rmd"
      provides: "Focused branding guide"
    - path: "R/metadata_helpers.R"
      provides: "Completed roxygen docs for metadata helpers"
  key_links:
    - from: "vignettes/getting-started.Rmd"
      to: "README.md"
      via: "same working-paper-first flow"
      pattern: "create_working_paper\\("
    - from: "R/render.R"
      to: "man/render_pub.Rd"
      via: "roxygen examples regenerated in final hardening plan"
      pattern: "@examples"
    - from: "R/create_article.R"
      to: "vignettes/working-papers.Rmd"
      via: "secondary format references"
      pattern: "create_article"
---

<objective>
Complete the package’s long-form documentation and roxygen examples without breaking CRAN-safe checks.

Purpose: Satisfy the vignette and exported-function documentation requirements while preserving the concise, working-paper-first tone locked in D-04 to D-06 and D-11.
Output: Three vignettes plus finished roxygen blocks for the exported API.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-RESEARCH.md
@README.md
@DOCUMENTATION_PLAN.md
@BLUEPRINT.md
@NAMESPACE

<interfaces>
Exported functions from [NAMESPACE](/Users/davidzenz/R/typstR/NAMESPACE):
```r
affiliation()
appendix_title()
author()
code_availability()
create_article()
create_policy_brief()
create_working_paper()
data_availability()
fig_note()
funding()
jel_codes()
keywords()
manuscript_meta()
render_pub()
render_working_paper()
report_number()
tab_note()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three concise vignettes per D-04, D-05, and D-06</name>
  <files>
    vignettes/getting-started.Rmd
    vignettes/working-papers.Rmd
    vignettes/customizing-branding.Rmd
  </files>
  <read_first>
    README.md,
    DOCUMENTATION_PLAN.md,
    inst/templates/workingpaper/template.qmd,
    inst/templates/article/template.qmd,
    inst/templates/policy-brief/template.qmd,
    .planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
  </read_first>
  <action>
Create the three missing vignettes as short task guides, not long tutorials, per D-04.

`vignettes/getting-started.Rmd` must narrate the full workflow from `create_working_paper()` through editing `template.qmd` to rendering with `render_working_paper()`, per D-05. Any chunk that would actually scaffold files or render must be guarded so the vignette remains CRAN-safe without Quarto.

`vignettes/working-papers.Rmd` must focus on the working paper format’s metadata surface: authors, affiliations, abstract, keywords, JEL codes, report number, acknowledgements, and bibliography workflow.

`vignettes/customizing-branding.Rmd` must focus on YAML-driven branding fields already supported in Phase 3: `logo`, `accent-color`, `primary-font`, `footer`, `margins`, and `disclaimer-page`.

Use concrete headings and short runnable code for pure-R helper examples. For render-dependent chunks, use chunk guards or non-evaluated examples so `R CMD check --as-cran` stays safe without Quarto, per D-11.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && test -f vignettes/getting-started.Rmd && test -f vignettes/working-papers.Rmd && test -f vignettes/customizing-branding.Rmd && rg -n 'create_working_paper\\(|render_working_paper\\(|accent-color|disclaimer-page|JEL|report number' vignettes/getting-started.Rmd vignettes/working-papers.Rmd vignettes/customizing-branding.Rmd</automated>
  </verify>
  <acceptance_criteria>
    - `vignettes/getting-started.Rmd` exists and contains both `create_working_paper(` and `render_working_paper(`.
    - `vignettes/working-papers.Rmd` exists and mentions `JEL` and `report number`.
    - `vignettes/customizing-branding.Rmd` exists and mentions `accent-color` and `disclaimer-page`.
    - render-dependent code in the vignettes is visibly guarded or marked non-evaluated rather than forced to run during check.
  </acceptance_criteria>
  <done>The package has the three required vignettes, all written in a concise task-oriented style and safe for machines without Quarto.</done>
</task>

<task type="auto">
  <name>Task 2: Complete roxygen documentation and examples for every export per DOCS-05</name>
  <files>
    R/create_working_paper.R
    R/create_article.R
    R/create_policy_brief.R
    R/render.R
    R/metadata_helpers.R
    R/pub_helpers.R
    R/notes_helpers.R
    R/typstR-package.R
  </files>
  <read_first>
    NAMESPACE,
    R/create_working_paper.R,
    R/create_article.R,
    R/create_policy_brief.R,
    R/render.R,
    R/metadata_helpers.R,
    R/pub_helpers.R,
    R/notes_helpers.R,
    R/typstR-package.R,
    .planning/phases/04-tests-documentation-and-cran-hardening/04-CONTEXT.md
  </read_first>
  <action>
Audit every function exported in `NAMESPACE` and ensure its roxygen block includes a clear description, complete parameter docs, return value, and at least one example. Keep pure helpers runnable where feasible. Guard filesystem- or Quarto-dependent examples with `\\dontrun{}` or equivalent so they do not break checks on machines without Quarto, per D-11.

Be explicit about the current user-facing behavior:
- `create_*()` examples should show scaffold commands with `open = FALSE` and stay guarded.
- `render_pub()` and `render_working_paper()` examples must stay guarded because they require Quarto.
- helper functions such as `author()`, `affiliation()`, `keywords()`, `jel_codes()`, `report_number()`, `funding()`, `data_availability()`, `code_availability()`, and `manuscript_meta()` should have real examples that match the package workflow.
- if `R/typstR-package.R` lacks a package-level roxygen block, add one so the package index page is meaningful once `man/` is regenerated.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'ns <- parseNamespaceFile("typstR", "."); exports <- getNamespaceExports("typstR"); files <- c("R/create_working_paper.R","R/create_article.R","R/create_policy_brief.R","R/render.R","R/metadata_helpers.R","R/pub_helpers.R","R/notes_helpers.R","R/typstR-package.R"); txt <- paste(vapply(files, paste, collapse = "\n", FUN.VALUE = character(1)), collapse = "\n"); missing <- exports[!vapply(exports, function(x) grepl(paste0("@examples[[:space:][:print:][:cntrl:]]*\\n#' .*", x), txt) || grepl(paste0("`", x, "`"), txt), logical(1))]; if (length(missing)) { stop(paste("Potentially undocumented exports:", paste(missing, collapse = ", "))) }'</automated>
  </verify>
  <acceptance_criteria>
    - every export listed in `NAMESPACE` appears in a roxygen block in the `R/` sources.
    - `R/render.R` contains `@examples` for both `render_pub` and `render_working_paper`.
    - `R/create_working_paper.R`, `R/create_article.R`, and `R/create_policy_brief.R` each contain guarded examples.
    - `R/metadata_helpers.R` contains runnable examples for `author()`, `affiliation()`, and `manuscript_meta()`.
    - `R/pub_helpers.R` and `R/notes_helpers.R` each contain `@examples` blocks for every exported helper defined there.
  </acceptance_criteria>
  <done>All exported functions are documented at the source level with examples appropriate to their runtime requirements.</done>
</task>

</tasks>

<verification>
The three vignette files exist with the required topics, and the R source roxygen blocks now cover the full exported surface with guarded examples where needed.
</verification>

<success_criteria>
- `DOCS-02`, `DOCS-03`, and `DOCS-04` are satisfied by checked-in vignettes.
- `DOCS-05` is satisfied at the source level before documentation generation.
- Documentation prose remains concise and task-oriented, with render-dependent steps guarded for no-Quarto machines.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tests-documentation-and-cran-hardening/04-03-SUMMARY.md`
</output>
