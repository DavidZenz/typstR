---
phase: 14-visual-branding-and-identity
plan: 14-01
type: execute
wave: 1
depends_on: []
files_modified: [man/figures/logo.svg, .Rbuildignore, pkgdown/favicon/]
autonomous: true
requirements: [BRAND-01]

must_haves:
  truths:
    - "Logo file exists in the canonical directory"
    - "Favicons are generated in the pkgdown directory"
    - "pkgdown directory is excluded from R build"
  artifacts:
    - path: "man/figures/logo.svg"
      provides: "Visual logo for the package"
    - path: ".Rbuildignore"
      provides: "Exclusion of pkgdown assets from CRAN package"
    - path: "pkgdown/favicon/favicon.ico"
      provides: "Browser tab icon"
  key_links:
    - from: "man/figures/logo.svg"
      to: "pkgdown::build_favicons()"
      via: "Source asset"
---

<objective>
Prepare visual assets for the typstR package branding.

Purpose: Create the logo and generate favicons for the documentation site.
Output: Logo SVG and favicon set.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-visual-branding-and-identity/14-CONTEXT.md
@.planning/phases/14-visual-branding-and-identity/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Logo SVG</name>
  <files>man/figures/logo.svg</files>
  <action>
    Create the man/figures directory if it doesn't exist.
    Write the following SVG content to man/figures/logo.svg:
    ```xml
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
      <rect x="20" y="20" width="60" height="10" fill="#002147"/>
      <rect x="45" y="30" width="10" height="50" fill="#002147"/>
      <rect x="25" y="75" width="50" height="5" fill="#002147" opacity="0.3"/>
    </svg>
    ```
  </action>
  <verify>
    <automated>test -f man/figures/logo.svg</automated>
  </verify>
  <done>Logo file exists with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Configure .Rbuildignore</name>
  <files>.Rbuildignore</files>
  <action>
    Add `^pkgdown/` to .Rbuildignore to ensure site assets are not included in the R package build.
  </action>
  <verify>
    <automated>grep "^pkgdown/" .Rbuildignore</automated>
  </verify>
  <done>pkgdown/ is listed in .Rbuildignore</done>
</task>

<task type="auto">
  <name>Task 3: Generate Favicons</name>
  <files>pkgdown/favicon/</files>
  <action>
    Run `Rscript -e 'pkgdown::build_favicons()'` to generate browser icons from the logo.
  </action>
  <verify>
    <automated>test -f pkgdown/favicon/favicon.ico</automated>
  </verify>
  <done>Favicon files generated in pkgdown/favicon/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| filesystem | Local file creation of SVG and favicons |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | logo.svg | mitigate | Verify SVG content after creation |
| T-14-01-02 | Info Disclosure | .Rbuildignore | mitigate | Ensure pkgdown/ is properly ignored to avoid shipping large assets to CRAN |
</threat_model>

<verification>
Check that man/figures/logo.svg and pkgdown/favicon/ exist.
Check .Rbuildignore for pkgdown/ entry.
</verification>

<success_criteria>
Logo and favicons are ready for site building.
</success_criteria>

<output>
After completion, create .planning/phases/14-visual-branding-and-identity/14-01-SUMMARY.md
</output>
