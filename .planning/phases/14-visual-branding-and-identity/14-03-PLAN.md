---
phase: 14-visual-branding-and-identity
plan: 14-03
type: execute
wave: 3
depends_on: [14-02]
files_modified: [docs/]
autonomous: false
requirements: [BRAND-01, BRAND-02]

must_haves:
  truths:
    - "pkgdown::check_pkgdown() passes without warnings"
    - "Local site build completes successfully"
    - "Visual inspection confirms logo and colors are applied"
  artifacts:
    - path: "docs/index.html"
      provides: "Rendered home page"
  key_links:
    - from: "docs/index.html"
      to: "man/figures/logo.svg"
      via: "HTML img tag"
---

<objective>
Validate the visual branding by building the site locally and performing a visual check.

Purpose: Ensure the configuration works as expected and looks professional.
Output: Rebuilt documentation site in docs/ directory.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pkgdown Check</name>
  <files></files>
  <action>
    Run `Rscript -e 'pkgdown::check_pkgdown()'` to verify the configuration and assets.
    Fix any warnings if they appear.
  </action>
  <verify>
    <automated>Rscript -e 'pkgdown::check_pkgdown()'</automated>
  </verify>
  <done>pkgdown check returns zero warnings</done>
</task>

<task type="auto">
  <name>Task 2: Build Site Locally</name>
  <files>docs/</files>
  <action>
    Run `Rscript -e 'pkgdown::build_site(preview = FALSE)'` to regenerate the documentation site with the new branding.
  </action>
  <verify>
    <automated>test -f docs/index.html</automated>
  </verify>
  <done>Site rebuilt successfully in docs/</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Branded pkgdown site</what-built>
  <how-to-verify>
    Open `docs/index.html` in a browser and verify:
    1. The Oxford Blue logo is visible in the top-left navbar.
    2. The browser tab shows the new favicon.
    3. Links and the 'light switch' reflect the Oxford Blue theme.
    4. Code blocks have the subtle blue left border.
  </how-to-verify>
  <resume-signal>Type "approved" if the branding looks correct.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| filesystem | Local site build |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | Rendered docs | mitigate | Manual visual verification checkpoint |
</threat_model>

<verification>
pkgdown::check_pkgdown() passes.
Visual confirmation of branding.
</verification>

<success_criteria>
Branding is fully applied and validated.
</success_criteria>

<output>
After completion, create .planning/phases/14-visual-branding-and-identity/14-03-SUMMARY.md
</output>
