---
phase: "18-cross-platform-test-hardening"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [".github/workflows/R-CMD-check.yaml"]
autonomous: true
requirements: ["CRAN-04"]
must_haves:
  truths:
    - "CI workflow definition exists for R-CMD-check"
  artifacts:
    - path: ".github/workflows/R-CMD-check.yaml"
      provides: "GitHub Actions workflow definition"
  key_links: []
---

<objective>
CI Workflow Scaffolding.

Purpose: Create the standard GitHub Actions R-CMD-check workflow to enable cross-platform CI testing (macOS, Windows, Ubuntu).
Output: `.github/workflows/R-CMD-check.yaml`
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate R-CMD-check workflow</name>
  <files>.github/workflows/R-CMD-check.yaml</files>
  <action>
    1. Run `Rscript -e 'usethis::use_github_action_check_standard()'` to generate `.github/workflows/R-CMD-check.yaml`.
    2. Verify the generated workflow does NOT contain `setup-quarto`. We deliberately omit Quarto on CI runners to simulate CRAN check environments.
  </action>
  <verify>
    <automated>test -f .github/workflows/R-CMD-check.yaml && ! grep -q "setup-quarto" .github/workflows/R-CMD-check.yaml</automated>
  </verify>
  <done>The standard GitHub Actions workflow file is created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Runner → External | Workflow execution runs on Github infrastructure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | CI Workflow | accept | Standard template generated by usethis, running in secure Github environment. |
</threat_model>

<verification>
Automated verification confirms the existence of the newly created workflow YAML file.
</verification>

<success_criteria>
`R-CMD-check.yaml` exists.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cross-platform-test-hardening/18-01-SUMMARY.md`
</output>