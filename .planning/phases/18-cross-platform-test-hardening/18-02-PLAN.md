---
phase: "18-cross-platform-test-hardening"
plan: "02"
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: ["tests/testthat/*.R"]
autonomous: false
requirements: ["CRAN-04"]
must_haves:
  truths:
    - "CI pipeline is green across all OS targets"
    - "Tests fail gracefully on environments without Quarto/Typst"
  artifacts: []
  key_links: []
---

<objective>
Cross-Platform Execution and Hardening.

Purpose: Ensure tests run reliably across platforms and skip Quarto/Typst-dependent tests when these tools are missing.
Output: Green CI builds on macOS, Windows, and Ubuntu.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>CI workflow execution trigger</what-built>
  <how-to-verify>
    1. Push changes to `main`.
    2. Check GitHub Actions log for the `R-CMD-check` workflow runs (macOS, Windows, Ubuntu).
    3. If any platform fails due to missing Quarto/Typst, patch the specific failing tests with `skip_if_not(quarto::quarto_available())` and push again.
    4. Verify all platforms show a green checkmark.
  </how-to-verify>
  <resume-signal>Type "approved" once all CI pipelines are green.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Github | Code pushed to remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering | Tests | accept | Tests are only skipping based on environment capabilities, no real risk. |
</threat_model>

<verification>
Human confirms that CI checks pass on Github.
</verification>

<success_criteria>
CI pipeline is green across all OS targets.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cross-platform-test-hardening/18-02-SUMMARY.md`
</output>