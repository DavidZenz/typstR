---
phase: 19-r-cmd-check-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.Rbuildignore]
autonomous: true
requirements: [CRAN-01]

must_haves:
  truths:
    - "Local R CMD check returns 0 errors, 0 warnings, 0 notes"
  artifacts:
    - path: ".Rbuildignore"
      provides: "Build exclusion rules"
  key_links:
    - from: ".Rbuildignore"
      to: "R CMD check"
      via: "exclusion"
---

<objective>
Align the .Rbuildignore file to ensure all non-package files are excluded from the build, resulting in a perfect (0/0/0) R CMD check locally.

Purpose: Eliminate the "Note" for init.json and prevent other non-package files from being included in the CRAN tarball.
Output: Updated .Rbuildignore and a clean local check report.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.Rbuildignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .Rbuildignore</name>
  <files>.Rbuildignore</files>
  <action>
    Update .Rbuildignore to include:
    - `^init\.json$`
    - `^\.Rhistory$`
    - `^\.DS_Store$`
    
    Ensure these patterns are added at the end of the file if not already present.
  </action>
  <verify>grep -E "^init\\\\.json\\$|\\\\.Rhistory\\$|\\\\.DS_Store\\$" .Rbuildignore</verify>
  <done>.Rbuildignore contains the required exclusion patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Run local R CMD check</name>
  <files></files>
  <action>
    Run `devtools::check(args = "--as-cran")` locally to verify that the changes result in a perfect check status.
  </action>
  <verify>Rscript -e 'check_res <- devtools::check(args = "--as-cran", quiet = TRUE); quit(status = if(length(check_res$errors) == 0 && length(check_res$warnings) == 0 && length(check_res$notes) == 0) 0 else 1)'</verify>
  <done>Local check returns 0 errors, 0 warnings, and 0 notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local build → CRAN submission | Build artifacts must exclude internal metadata |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | .Rbuildignore | mitigate | Explicitly exclude init.json and other metadata files |
</threat_model>

<verification>
Run `devtools::check(args = "--as-cran")` and confirm output shows:
0 errors ✔ | 0 warnings ✔ | 0 notes ✔
</verification>

<success_criteria>
- .Rbuildignore is updated.
- Local devtools::check results: 0 errors, 0 warnings, 0 notes.
</success_criteria>

<output>
After completion, create .planning/phases/19-r-cmd-check-optimization/19-01-SUMMARY.md
</output>
