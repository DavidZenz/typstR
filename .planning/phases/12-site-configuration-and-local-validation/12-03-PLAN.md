---
phase: 12-site-configuration-and-local-validation
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified: []
autonomous: false
requirements: [SITE-02, SITE-03, SITE-04]

must_haves:
  truths:
    - "pkgdown::check_pkgdown() returns zero warnings"
    - "pkgdown::build_site() completes without errors"
    - "The generated site in docs/ is visually correct and all links resolve"
  artifacts:
    - path: "docs/index.html"
      provides: "Entry point to the generated documentation site"
  key_links:
    - from: "docs/index.html"
      to: "docs/reference/index.html"
      via: "Reference nav link"
---

<objective>
Validate the site configuration and execute a full local build to ensure the documentation site is correct and warning-free.

Purpose: Final quality gate for the documentation site before deployment.
Output: Validated local site in docs/.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-site-configuration-and-local-validation/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pkgdown validation</name>
  <files>_pkgdown.yml</files>
  <action>
    Run `pkgdown::check_pkgdown()` in R.
    - If any warnings appear (e.g., unmatched functions or unlisted articles), fix `_pkgdown.yml` until zero warnings remain.
    - If S3 methods (e.g., `print.typstR_*`) appear as unmapped, ensure they are marked `@keywords internal` in the R source (Phase 11 should have done this, but verify if check_pkgdown flags them).
  </action>
  <verify>
    <automated>Rscript -e "if(length(pkgdown::check_pkgdown()) > 0) stop('Warnings found')"</automated>
  </verify>
  <done>pkgdown::check_pkgdown() returns zero warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Build site locally</name>
  <files>docs/</files>
  <action>
    Run `pkgdown::build_site(preview = FALSE)` in R to generate the site in `docs/`.
  </action>
  <verify>
    <automated>test -f docs/index.html</automated>
  </verify>
  <done>Site built successfully in docs/.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Manual review of built site</name>
  <what-built>A full local pkgdown site in the `docs/` directory.</what-built>
  <how-to-verify>
    1. Open `docs/index.html` in a web browser.
    2. Verify the navbar contains: Home, Get Started, Reference, Articles, Changelog.
    3. Click "Get Started" and ensure it leads to the Getting Started article.
    4. Click "Reference" and verify the 5 groups (Scaffolding, Metadata helpers, Publication helpers, Rendering, Validation and diagnostics).
    5. Verify no "dev" badge is visible next to the version number.
    6. Click "Articles" and verify the order (Getting Started, Working Papers, Customizing Branding).
    7. Click "Changelog" and ensure it loads.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build → Output | Local build generates site in docs/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | docs/ | accept | Local preview of public-intended documentation |
| T-12-03-02 | Integrity | Site Links | mitigate | Manual verification of link resolution |
</threat_model>

<verification>
Zero warnings from check_pkgdown and successful human verification of the site.
</verification>

<success_criteria>
Documentation site is warning-free, correctly configured, and visually verified.
</success_criteria>

<output>
After completion, create .planning/phases/12-site-configuration-and-local-validation/12-03-SUMMARY.md
</output>
