---
phase: 12-site-configuration-and-local-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [DESCRIPTION, .gitignore, .Rbuildignore, _pkgdown.yml]
autonomous: true
requirements: [SHIP-02]

must_haves:
  truths:
    - "pkgdown is added to Suggests in DESCRIPTION"
    - "Package URLs are updated to include the documentation site"
    - "The docs/ directory is ignored by git"
    - ".Rbuildignore is updated to exclude pkgdown artifacts from the package tarball"
  artifacts:
    - path: "_pkgdown.yml"
      provides: "Initial site configuration scaffold"
    - path: "DESCRIPTION"
      provides: "Updated package metadata"
    - path: ".gitignore"
      provides: "Build artifact exclusion"
    - path: ".Rbuildignore"
      provides: "CRAN package build exclusion"
  key_links:
    - from: "DESCRIPTION"
      to: "https://davidzenz.github.io/typstR/"
      via: "URL field"
---

<objective>
Initialize the pkgdown environment and align package metadata with the documentation site.

Purpose: Set up the scaffolding and housekeeping files required for a clean pkgdown build.
Output: Initial _pkgdown.yml, updated DESCRIPTION, updated .gitignore, and updated .Rbuildignore.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-site-configuration-and-local-validation/12-CONTEXT.md
@.planning/phases/12-site-configuration-and-local-validation/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold pkgdown and update ignore files</name>
  <files>_pkgdown.yml, .gitignore, .Rbuildignore</files>
  <action>
    1. Run `usethis::use_pkgdown()` in R to create the initial `_pkgdown.yml`.
    2. Add `docs/` to `.gitignore` to ensure built site artifacts are not committed (as we will use GitHub Actions for deployment from main).
  </action>
  <verify>
    <automated>test -f _pkgdown.yml && grep "docs/" .gitignore && grep "_pkgdown.yml" .Rbuildignore</automated>
  </verify>
  <done>_pkgdown.yml exists and docs/ is ignored, and .Rbuildignore is configured.</done>
</task>

<task type="auto">
  <name>Task 2: Update DESCRIPTION metadata</name>
  <files>DESCRIPTION</files>
  <action>
    Update `DESCRIPTION` to include pkgdown requirements:
    1. Set `URL`: `https://davidzenz.github.io/typstR/, https://github.com/DavidZenz/typstR`
    2. Add `pkgdown (>= 2.0.0)` to `Suggests`.
  </action>
  <verify>
    <automated>grep "https://davidzenz.github.io/typstR/" DESCRIPTION && grep "pkgdown" DESCRIPTION</automated>
  </verify>
  <done>DESCRIPTION contains the site URL and pkgdown in Suggests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tooling → Filesystem | usethis/pkgdown modify package configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Information Disclosure | .gitignore | mitigate | Ensure docs/ is ignored to prevent accidental commit of local build artifacts |
</threat_model>

<verification>
Check for existence of _pkgdown.yml and verify DESCRIPTION fields.
</verification>

<success_criteria>
Housekeeping complete: pkgdown scaffolded and package metadata aligned.
</success_criteria>

<output>
After completion, create .planning/phases/12-site-configuration-and-local-validation/12-01-SUMMARY.md
</output>
