---
phase: 12-site-configuration-and-local-validation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: [_pkgdown.yml]
autonomous: true
requirements: [SITE-02, SITE-03, SITE-04, SITE-05, SHIP-01]

must_haves:
  truths:
    - "The site URL is correctly set to https://davidzenz.github.io/typstR/"
    - "Bootstrap 5 template is selected"
    - "Development mode is set to release to suppress the dev badge"
    - "Navbar contains all required links: Home, Get Started, Reference, Articles, Changelog"
    - "Reference index is grouped into the 5 specified categories"
    - "Articles are ordered: getting-started, working-papers, customizing-branding"
  artifacts:
    - path: "_pkgdown.yml"
      provides: "Detailed site configuration and navigation structure"
  key_links:
    - from: "navbar"
      to: "articles/getting-started.html"
      via: "Get Started entry"
---

<objective>
Configure the _pkgdown.yml file to define the site's structure, navigation, and content organization.

Purpose: Translate the site requirements into a valid pkgdown configuration that drives the final site build.
Output: Fully configured _pkgdown.yml.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-site-configuration-and-local-validation/12-01-SUMMARY.md
@.planning/phases/12-site-configuration-and-local-validation/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Base site and navbar configuration</name>
  <files>_pkgdown.yml</files>
  <action>
    Edit `_pkgdown.yml` to set the following:
    1. `url: https://davidzenz.github.io/typstR/`
    2. `template: bootstrap: 5`
    3. `development: mode: release`
    4. Configure `navbar`:
       - `structure`:
         - `left`: [home, intro, reference, articles, news]
       - `components`:
         - `intro`:
           - `text`: "Get Started"
           - `href`: "articles/getting-started.html"
  </action>
  <verify>
    <automated>grep "mode: release" _pkgdown.yml && grep "href: articles/getting-started.html" _pkgdown.yml</automated>
  </verify>
  <done>Base configuration and navbar entries are set.</done>
</task>

<task type="auto">
  <name>Task 2: Reference grouping</name>
  <files>_pkgdown.yml</files>
  <action>
    Group the reference index in `_pkgdown.yml` into 5 categories:
    1. **Scaffolding**: `create_article`, `create_policy_brief`, `create_working_paper`
    2. **Metadata helpers**: `affiliation`, `author`, `manuscript_meta`, `funding`, `keywords`, `jel_codes`, `report_number`, `appendix_title`, `code_availability`, `data_availability`
    3. **Publication helpers**: `fig_note`, `tab_note`
    4. **Rendering**: `render_pub`, `render_working_paper`
    5. **Validation and diagnostics**: `validate_render_environment`
  </action>
  <verify>
    <automated>grep "Scaffolding" _pkgdown.yml && grep "validate_render_environment" _pkgdown.yml</automated>
  </verify>
  <done>Reference index is correctly grouped.</done>
</task>

<task type="auto">
  <name>Task 3: Articles ordering</name>
  <files>_pkgdown.yml</files>
  <action>
    List articles in `_pkgdown.yml` in the following order:
    1. `getting-started`
    2. `working-papers`
    3. `customizing-branding`
  </action>
  <verify>
    <automated>grep -A 3 "articles:" _pkgdown.yml | grep "getting-started" && grep -A 3 "articles:" _pkgdown.yml | grep "customizing-branding"</automated>
  </verify>
  <done>Articles order is defined in _pkgdown.yml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configuration → Build | _pkgdown.yml drives the pkgdown build engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Information Disclosure | navbar | accept | Public navigation entries are intentional |
</threat_model>

<verification>
Review _pkgdown.yml for accuracy against the specified configuration.
</verification>

<success_criteria>
_pkgdown.yml contains all required configuration for URL, template, navbar, reference groups, and articles.
</success_criteria>

<output>
After completion, create .planning/phases/12-site-configuration-and-local-validation/12-02-SUMMARY.md
</output>
