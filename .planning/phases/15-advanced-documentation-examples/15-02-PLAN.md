---
phase: 15-advanced-documentation-examples
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [_pkgdown.yml]
autonomous: true
requirements: [EXT-01]
must_haves:
  truths:
    - "The advanced examples vignette is listed in _pkgdown.yml"
    - "The pkgdown site builds without errors related to the new article"
  artifacts:
    - path: "_pkgdown.yml"
      provides: "Site navigation configuration"
  key_links:
    - from: "_pkgdown.yml"
      to: "vignettes/advanced-examples.Rmd"
      via: "articles section"
      pattern: "advanced-examples"
---

<objective>
Wire the new "Advanced Examples" vignette into the pkgdown site navigation and validate the site build.

Purpose: Ensure the new content is discoverable and correctly rendered.
Output: Updated _pkgdown.yml, validated local site build.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@_pkgdown.yml
@.planning/phases/15-advanced-documentation-examples/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update _pkgdown.yml</name>
  <files>_pkgdown.yml</files>
  <action>
    Add `advanced-examples` to the `articles` section in `_pkgdown.yml`.
    It should be placed under the existing articles (getting-started, working-papers, customizing-branding).
  </action>
  <verify>
    <automated>grep "advanced-examples" _pkgdown.yml</automated>
  </verify>
  <done>_pkgdown.yml updated with the new article.</done>
</task>

<task type="auto">
  <name>Task 2: Validate site build</name>
  <files>_pkgdown.yml</files>
  <action>
    Run validation commands to ensure the site build is healthy:
    1. Run `Rscript -e 'pkgdown::check_pkgdown()'` to check for unmatched articles or configuration errors.
    2. Run `Rscript -e 'pkgdown::build_site(preview = FALSE)'` to verify that the new article renders correctly in the local build.
  </action>
  <verify>
    <automated>Rscript -e 'pkgdown::check_pkgdown()'</automated>
  </verify>
  <done>Site build validated without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A - Documentation and local build only.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | I | _pkgdown.yml | accept | Configuration change, validated by build tool. |
</threat_model>

<verification>
Verify that `pkgdown::build_site()` completes successfully and that `docs/articles/advanced-examples.html` exists.
</verification>

<success_criteria>
"Advanced Examples" is reachable and valid in the local build.
</success_criteria>

<output>
After completion, create .planning/phases/15-advanced-documentation-examples/15-02-SUMMARY.md
</output>
