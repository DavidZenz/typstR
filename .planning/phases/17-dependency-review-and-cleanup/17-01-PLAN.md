---
phase: 17-dependency-review-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
autonomous: true
requirements:
  - CRAN-02
must_haves:
  truths:
    - yaml dependency is imported rather than suggested
    - Package passes CRAN checks without dependency errors
  artifacts:
    - path: DESCRIPTION
      provides: Package metadata
      contains: yaml in Imports section
  key_links: []
---

<objective>
Update `DESCRIPTION` file to move `yaml` from `Suggests` to `Imports`.
Purpose: `yaml` is used in core package logic (`R/validation_environment.R`), so it must be a hard dependency to avoid runtime errors on user machines when not explicitly installed.
Output: Updated `DESCRIPTION` file and verification of clean `devtools::check()`.
</objective>

<context>
@.planning/ROADMAP.md
@DESCRIPTION
@R/validation_environment.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DESCRIPTION to import yaml</name>
  <files>DESCRIPTION</files>
  <action>
    Modify the `DESCRIPTION` file:
    - Remove `yaml` from the `Suggests:` section.
    - Add `yaml,` to the `Imports:` section, maintaining alphabetical order if possible.
  </action>
  <verify>
    <automated>grep -A 5 "Imports:" DESCRIPTION | grep "yaml"</automated>
  </verify>
  <done>The `DESCRIPTION` file contains `yaml` in `Imports` and not in `Suggests`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify package dependencies</name>
  <files>DESCRIPTION</files>
  <action>
    Run `devtools::check(document = FALSE)` locally to verify no other dependency warnings (e.g., "Namespace dependencies not required" or "Packages in Depends/Imports not imported from"). Since this is just a local check to confirm the DESCRIPTION change, running it in the shell is sufficient. Fix any minor DESCRIPTION formatting issues that the check might complain about (e.g., trailing commas, indentation).
  </action>
  <verify>
    <automated>Rscript -e "devtools::check(document = FALSE, error_on = 'warning')"</automated>
  </verify>
  <done>Package checks complete without dependency warnings related to `yaml`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package -> CRAN | The package metadata must correctly specify requirements to prevent runtime issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Denial of Service | DESCRIPTION | mitigate | Explicitly specify `yaml` as Import to ensure it is installed, preventing runtime failures. |
</threat_model>

<verification>
`devtools::check()` completes without dependency errors or warnings.
</verification>

<success_criteria>
`yaml` is in `Imports`.
`devtools::check()` passes without dependency-related warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/17-dependency-review-and-cleanup/17-01-SUMMARY.md`
</output>