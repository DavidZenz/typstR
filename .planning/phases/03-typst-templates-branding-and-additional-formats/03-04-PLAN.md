---
phase: 03-typst-templates-branding-and-additional-formats
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - R/create_article.R
  - R/create_policy_brief.R
  - inst/templates/article/template.qmd
  - inst/templates/article/_quarto.yml
  - inst/templates/article/references.bib
  - inst/templates/policy-brief/template.qmd
  - inst/templates/policy-brief/_quarto.yml
  - inst/templates/policy-brief/references.bib
  - NAMESPACE
  - DESCRIPTION
autonomous: true
requirements:
  - SCAF-02
  - SCAF-03

must_haves:
  truths:
    - "create_article('my-article') creates a project directory with template.qmd, _quarto.yml, references.bib, and _extensions/typstR/"
    - "create_policy_brief('my-brief') creates a project directory with template.qmd, _quarto.yml, references.bib, and _extensions/typstR/"
    - "Article template.qmd has format-variant: article and no report-number field"
    - "Policy brief template.qmd has format-variant: brief and no JEL codes"
    - "Both functions are exported and documented"
  artifacts:
    - path: "R/create_article.R"
      provides: "create_article() scaffolding function"
      contains: "create_article"
    - path: "R/create_policy_brief.R"
      provides: "create_policy_brief() scaffolding function"
      contains: "create_policy_brief"
    - path: "inst/templates/article/template.qmd"
      provides: "Article template with format-variant: article"
      contains: "format-variant: article"
    - path: "inst/templates/policy-brief/template.qmd"
      provides: "Policy brief template with format-variant: brief"
      contains: "format-variant: brief"
  key_links:
    - from: "R/create_article.R"
      to: "inst/templates/article/"
      via: 'system.file("templates/article")'
      pattern: 'system.file.*templates/article'
    - from: "R/create_policy_brief.R"
      to: "inst/templates/policy-brief/"
      via: 'system.file("templates/policy-brief")'
      pattern: 'system.file.*templates/policy-brief'
---

<objective>
Create scaffolding functions and template files for article and policy-brief formats.

Purpose: Enable users to scaffold article and policy-brief projects with a single R function call, matching the create_working_paper() experience. Each creates a project with format-appropriate YAML defaults.

Output: Two new R functions (create_article, create_policy_brief), two template directories with .qmd files, updated NAMESPACE and DESCRIPTION.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-CONTEXT.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-01-SUMMARY.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-03-SUMMARY.md

<interfaces>
<!-- Existing scaffolding pattern to replicate -->
From R/create_working_paper.R:
```r
create_working_paper <- function(path, title = NULL, open = interactive()) {
  # Guard: error if directory exists
  if (fs::dir_exists(path)) { cli::cli_abort(...) }
  fs::dir_create(path)
  # Copy from inst/templates/workingpaper/
  template_src <- system.file("templates/workingpaper", package = "typstR", mustWork = TRUE)
  template_files <- fs::dir_ls(template_src, all = TRUE, recurse = FALSE)
  fs::file_copy(template_files, fs::path(path, fs::path_file(template_files)))
  # Copy extension
  ext_src <- system.file("quarto/extensions/typstR", package = "typstR", mustWork = TRUE)
  ext_dest <- fs::path(path, "_extensions", "typstR")
  fs::dir_create(fs::path(path, "_extensions"))
  fs::dir_copy(ext_src, ext_dest)
  # Optional title substitution
  # cli success summary
  invisible(path)
}
```

From inst/templates/workingpaper/template.qmd (YAML structure to adapt):
```yaml
title: "Working Paper Title"
author: [...]
affiliations: [...]
abstract: |
  Replace this placeholder...
bibliography: references.bib
format:
  typstR-typst: default
typstR:
  keywords: [...]
  jel: [...]
  acknowledgements: |
  report-number: "WP 001"
  funding: |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template directories for article and policy-brief</name>
  <files>
    inst/templates/article/template.qmd,
    inst/templates/article/_quarto.yml,
    inst/templates/article/references.bib,
    inst/templates/policy-brief/template.qmd,
    inst/templates/policy-brief/_quarto.yml,
    inst/templates/policy-brief/references.bib
  </files>
  <read_first>
    inst/templates/workingpaper/template.qmd,
    inst/templates/workingpaper/_quarto.yml,
    inst/templates/workingpaper/references.bib,
    .planning/phases/03-typst-templates-branding-and-additional-formats/03-CONTEXT.md
  </read_first>
  <action>
Create `inst/templates/article/` and `inst/templates/policy-brief/` directories with format-appropriate template files. Copy the working paper templates and adapt for each format.

**inst/templates/article/template.qmd:**
```yaml
---
title: "Article Title"
author:
  - name: "Author One"
    email: "author.one@example.org"
    corresponding: true
    affiliation:
      - "1"
  - name: "Author Two"
    affiliation:
      - "1"
      - "2"
affiliations:
  - id: "1"
    name: "Institute Name"
  - id: "2"
    name: "University Name"
abstract: |
  Replace this placeholder with your abstract. This paper examines...
bibliography: references.bib
format:
  typstR-typst: default
typstR:
  format-variant: article
  keywords:
    - keyword one
    - keyword two
  jel:
    - C10
    - F13
  acknowledgements: |
    The authors thank...
  funding: |
    This research was supported by...
---

## Introduction

Introduce the research question here. @smith2020 provide relevant background.

## Data and Methods

Describe data sources and empirical strategy.

## Results

Present main findings.

## Conclusion

Summarize contributions and implications.

## References
```

Key differences from working paper: `format-variant: article`, NO `report-number` field, title placeholder says "Article Title".

**inst/templates/article/_quarto.yml:** Same as workingpaper: `project:\n  type: default`

**inst/templates/article/references.bib:** Copy from workingpaper (same placeholder .bib file).

**inst/templates/policy-brief/template.qmd:**
```yaml
---
title: "Policy Brief Title"
author:
  - name: "Author One"
    email: "author.one@example.org"
    corresponding: true
    affiliation:
      - "1"
affiliations:
  - id: "1"
    name: "Institute Name"
abstract: |
  Replace this placeholder with a concise summary of the policy brief.
bibliography: references.bib
format:
  typstR-typst: default
typstR:
  format-variant: brief
  keywords:
    - keyword one
    - keyword two
  report-number: "PB 001"
  funding: |
    This research was supported by...
---

## Key Findings

Summarize the most important findings for a policy audience.

## Background

Provide brief context for the policy question.

## Evidence

Present key evidence supporting the findings.

## Policy Implications

Describe actionable policy recommendations.

## References
```

Key differences from working paper: `format-variant: brief`, NO `jel` field, NO `acknowledgements`, simplified author block (single affiliation), title placeholder says "Policy Brief Title", sections oriented toward policy audience (Key Findings, Evidence, Policy Implications).

**inst/templates/policy-brief/_quarto.yml:** Same as workingpaper.

**inst/templates/policy-brief/references.bib:** Copy from workingpaper.
  </action>
  <verify>
    <automated>test -f inst/templates/article/template.qmd && test -f inst/templates/article/_quarto.yml && test -f inst/templates/article/references.bib && test -f inst/templates/policy-brief/template.qmd && test -f inst/templates/policy-brief/_quarto.yml && test -f inst/templates/policy-brief/references.bib && grep -q "format-variant: article" inst/templates/article/template.qmd && grep -q "format-variant: brief" inst/templates/policy-brief/template.qmd && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "format-variant: article" inst/templates/article/template.qmd
    - grep -q "Article Title" inst/templates/article/template.qmd
    - grep -q "typstR-typst" inst/templates/article/template.qmd
    - grep -qv "report-number" inst/templates/article/template.qmd (no report-number in article)
    - grep -q "format-variant: brief" inst/templates/policy-brief/template.qmd
    - grep -q "Policy Brief Title" inst/templates/policy-brief/template.qmd
    - grep -qv "jel" inst/templates/policy-brief/template.qmd (no JEL in brief)
    - test -f inst/templates/article/_quarto.yml
    - test -f inst/templates/policy-brief/references.bib
  </acceptance_criteria>
  <done>Article and policy-brief template directories exist with format-specific template.qmd, _quarto.yml, and references.bib. Article template has format-variant: article, no report-number. Brief template has format-variant: brief, no JEL codes, policy-oriented section structure.</done>
</task>

<task type="auto">
  <name>Task 2: Create create_article() and create_policy_brief() R functions</name>
  <files>
    R/create_article.R,
    R/create_policy_brief.R
  </files>
  <read_first>
    R/create_working_paper.R,
    DESCRIPTION,
    NAMESPACE
  </read_first>
  <action>
Create two new R source files following the exact pattern of create_working_paper.R. These are direct adaptations with minimal changes.

**R/create_article.R:**
```r
#' Create an article project
#'
#' Scaffolds a new article project directory with a Quarto document,
#' bibliography, project configuration, and the typstR Quarto extension.
#' The article format is identical to the working paper layout but does not
#' include a report number block or disclaimer page by default. It supports
#' anonymized mode via `typstR: anonymized: true` in the YAML front matter.
#'
#' @param path Path to the new project directory. Must not already exist.
#' @param title Optional title to pre-fill in the template YAML.
#' @param open Whether to open the project directory. Defaults to `TRUE`
#'   in interactive sessions.
#' @return The project path, invisibly.
#' @export
create_article <- function(path, title = NULL, open = interactive()) {
  if (fs::dir_exists(path)) {
    cli::cli_abort(c(
      "Directory {.path {path}} already exists.",
      "i" = "Choose a different path or remove the existing directory."
    ))
  }

  fs::dir_create(path)

  template_src <- system.file("templates/article", package = "typstR",
                               mustWork = TRUE)
  template_files <- fs::dir_ls(template_src, all = TRUE, recurse = FALSE)
  fs::file_copy(template_files, fs::path(path, fs::path_file(template_files)))

  ext_src <- system.file("quarto/extensions/typstR", package = "typstR",
                          mustWork = TRUE)
  ext_dest <- fs::path(path, "_extensions", "typstR")
  fs::dir_create(fs::path(path, "_extensions"))
  fs::dir_copy(ext_src, ext_dest)

  if (!is.null(title)) {
    qmd_path <- fs::path(path, "template.qmd")
    lines <- readLines(qmd_path)
    lines <- sub('title: "Article Title"',
                 paste0('title: "', title, '"'),
                 lines, fixed = TRUE)
    writeLines(lines, qmd_path)
  }

  cli::cli_alert_success("Created article project at {.path {path}}")
  cli::cli_bullets(c(
    "*" = "{.file template.qmd}",
    "*" = "{.file _quarto.yml}",
    "*" = "{.file references.bib}",
    "*" = "{.file _extensions/typstR/}"
  ))

  invisible(path)
}
```

**R/create_policy_brief.R:**
Same pattern but:
- Function name: `create_policy_brief`
- template_src: `system.file("templates/policy-brief", ...)`
- Title placeholder to replace: `'title: "Policy Brief Title"'`
- Success message: `"Created policy brief project at {.path {path}}"`
- Roxygen: describes policy brief format (shorter, "Summary" instead of "Abstract", no JEL codes, policy-oriented sections)

After creating both files, run `devtools::document()` to regenerate NAMESPACE with the new exports. This will add `export(create_article)` and `export(create_policy_brief)` to NAMESPACE.

IMPORTANT: The title substitution patterns must match exactly what's in the template.qmd files. Article uses `"Article Title"`, brief uses `"Policy Brief Title"`.
  </action>
  <verify>
    <automated>grep -q "create_article" R/create_article.R && grep -q "@export" R/create_article.R && grep -q 'system.file.*templates/article' R/create_article.R && grep -q "create_policy_brief" R/create_policy_brief.R && grep -q "@export" R/create_policy_brief.R && grep -q 'system.file.*templates/policy-brief' R/create_policy_brief.R && grep -q "export(create_article)" NAMESPACE && grep -q "export(create_policy_brief)" NAMESPACE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "create_article" R/create_article.R
    - grep -q "@export" R/create_article.R
    - grep -q 'system.file.*templates/article' R/create_article.R
    - grep -q "fs::dir_exists" R/create_article.R
    - grep -q "cli::cli_abort" R/create_article.R
    - grep -q "create_policy_brief" R/create_policy_brief.R
    - grep -q "@export" R/create_policy_brief.R
    - grep -q 'system.file.*templates/policy-brief' R/create_policy_brief.R
    - grep -q "fs::dir_exists" R/create_policy_brief.R
  </acceptance_criteria>
  <done>create_article() and create_policy_brief() are exported R functions following the create_working_paper() pattern. Each copies format-specific templates from inst/templates/ and the shared extension from inst/quarto/extensions/typstR/. NAMESPACE updated with new exports via devtools::document().</done>
</task>

</tasks>

<verification>
1. Article and policy-brief template directories exist with correct files
2. Template .qmd files have format-appropriate YAML (format-variant, field presence/absence)
3. R functions follow create_working_paper() pattern exactly
4. Both functions are @export annotated
5. NAMESPACE includes new exports after devtools::document()
6. R CMD check has no new warnings from the added files
</verification>

<success_criteria>
- create_article("my-article") creates project with template.qmd (format-variant: article), _quarto.yml, references.bib, _extensions/typstR/
- create_policy_brief("my-brief") creates project with template.qmd (format-variant: brief), _quarto.yml, references.bib, _extensions/typstR/
- Both functions handle existing directory error, optional title, cli success summary
- Article template has no report-number; brief template has no JEL codes
</success_criteria>

<output>
After completion, create `.planning/phases/03-typst-templates-branding-and-additional-formats/03-04-SUMMARY.md`
</output>
