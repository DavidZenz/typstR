---
phase: 03-typst-templates-branding-and-additional-formats
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - inst/templates/workingpaper/template.qmd
autonomous: false
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-04
  - TMPL-05
  - TMPL-06
  - TMPL-07
  - TMPL-08
  - TMPL-09
  - TMPL-10
  - TMPL-11
  - TMPL-12
  - TMPL-13
  - SCAF-02
  - SCAF-03

must_haves:
  truths:
    - "Working paper renders to PDF with complete title page, authors with superscripts, abstract, keywords, JEL, body, acknowledgements, bibliography"
    - "Article renders to PDF without report number; anonymized mode strips authors and acknowledgements"
    - "Policy brief renders with Summary label, no JEL, no ORCID"
    - "Branding hooks (logo, accent-color, font, footer, margins) change the PDF when set in YAML"
    - "Disclaimer page appears when enabled"
    - "All three formats can be scaffolded and rendered end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Smoke-test all three formats end-to-end and verify branding hooks, then update working paper template.qmd if needed.

Purpose: Verify the full Phase 3 deliverable works correctly before marking the phase complete. Catch integration issues between modules, branding hooks, and format variants.

Output: Verified renders of all three formats, any bug fixes applied, updated template.qmd with branding example fields.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-01-SUMMARY.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-02-SUMMARY.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-03-SUMMARY.md
@.planning/phases/03-typst-templates-branding-and-additional-formats/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-test all three format renders and fix any issues</name>
  <files>
    inst/templates/workingpaper/template.qmd
  </files>
  <read_first>
    inst/quarto/extensions/typstR/formats/workingpaper.typ,
    inst/quarto/extensions/typstR/typst-show.typ,
    inst/quarto/extensions/typstR/_extension.yml,
    inst/templates/workingpaper/template.qmd,
    inst/templates/article/template.qmd,
    inst/templates/policy-brief/template.qmd
  </read_first>
  <action>
Run end-to-end smoke tests for all three format variants. For each test, create a temporary project using the scaffolding functions, render to PDF, and verify no errors.

**Test 1: Working paper render**
```r
library(typstR)
tmp <- tempfile("wp-test-")
create_working_paper(tmp)
render_pub(tmp, quiet = TRUE, open = FALSE)
# Verify PDF exists
stopifnot(file.exists(file.path(tmp, "template.pdf")))
```

**Test 2: Article render**
```r
tmp2 <- tempfile("art-test-")
create_article(tmp2)
render_pub(tmp2, quiet = TRUE, open = FALSE)
stopifnot(file.exists(file.path(tmp2, "template.pdf")))
```

**Test 3: Policy brief render**
```r
tmp3 <- tempfile("brief-test-")
create_policy_brief(tmp3)
render_pub(tmp3, quiet = TRUE, open = FALSE)
stopifnot(file.exists(file.path(tmp3, "template.pdf")))
```

**Test 4: Branding hooks**
Create a temporary working paper with branding YAML fields added to template.qmd:
```yaml
typstR:
  accent-color: "#0A5DA6"
  footer: "Draft — Do Not Distribute"
  primary-font: "Linux Libertine"
```
Render and verify no errors. (Logo test requires an actual image file — skip in automated test or use a placeholder.)

**Test 5: Anonymized article**
Create a temporary article project, edit template.qmd to add `anonymized: true` under `typstR:`, render and verify no errors.

**Test 6: Disclaimer page**
Create a temporary working paper, add `disclaimer-page: true` to typstR: block, render and verify no errors.

If any test fails, debug the Typst compilation error, fix the relevant .typ files, and re-run. Common issues:
- Import path errors (relative paths in #import must be correct for the extension's location)
- Missing commas in Typst function parameter lists
- Typst set rule scoping issues (set rules after content)
- Pandoc template syntax errors (missing newlines after $for()$)

After all tests pass, update the working paper template.qmd to include commented-out branding fields as documentation:
```yaml
typstR:
  keywords: [...]
  jel: [...]
  # Branding (uncomment to customize):
  # logo: "logo.png"
  # accent-color: "#0A5DA6"
  # primary-font: "Linux Libertine"
  # footer: "Working Paper Series"
  # disclaimer-page: true
```

Also run `R CMD check --no-manual --no-vignettes` to verify no new warnings.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'devtools::load_all(); tmp <- tempfile("wp-"); create_working_paper(tmp); cat("WP scaffold OK\n")' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Rscript -e 'devtools::load_all(); tmp <- tempfile("wp-"); create_working_paper(tmp); cat("OK")' runs without error
    - Rscript -e 'devtools::load_all(); tmp <- tempfile("art-"); create_article(tmp); cat("OK")' runs without error
    - Rscript -e 'devtools::load_all(); tmp <- tempfile("bp-"); create_policy_brief(tmp); cat("OK")' runs without error
  </acceptance_criteria>
  <done>All three format scaffolding functions work. Quarto renders produce PDFs without errors for all variants. Branding hooks, anonymized mode, and disclaimer page render without Typst compilation errors. R CMD check passes. Working paper template.qmd includes commented branding field examples.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all three format PDF outputs</name>
  <files>none</files>
  <action>
Present rendered PDFs from all three formats to the user for visual inspection. The automated smoke tests (Task 1) verified no compilation errors; this checkpoint verifies the visual quality and correctness of the output.
  </action>
  <verify>User confirms visual inspection passes</verify>
  <done>User has approved the visual quality of working paper, article, and policy brief PDFs, including branding hooks, anonymized mode, and disclaimer page.</done>
  <what-built>Complete Phase 3 Typst template system: modular .typ architecture, 9 branding hooks, 3 format variants (working paper, article, policy brief), anonymized mode, disclaimer page, affiliation superscripts, email unescaping, scaffolding functions for all formats.</what-built>
  <how-to-verify>
    1. In R console: `library(typstR)` (or `devtools::load_all()`)
    2. Create and render a working paper:
       ```r
       create_working_paper("test-wp", title = "My Working Paper")
       render_working_paper("test-wp")
       ```
       Verify PDF shows: title page, authors with numbered superscripts, affiliations, abstract, keywords, JEL, body, acknowledgements. Email should display without backslash.
    3. Create and render an article:
       ```r
       create_article("test-art", title = "My Article")
       render_pub("test-art")
       ```
       Verify PDF: no report number, otherwise similar to working paper.
    4. Create and render a policy brief:
       ```r
       create_policy_brief("test-brief", title = "My Brief")
       render_pub("test-brief")
       ```
       Verify PDF: "Summary" label (not "Abstract"), no JEL codes, no ORCID.
    5. Test branding: Edit test-wp/template.qmd, add under typstR:
       ```yaml
       accent-color: "#0A5DA6"
       footer: "Draft - Do Not Distribute"
       disclaimer-page: true
       ```
       Re-render. Verify headings are blue, footer appears, disclaimer page at end.
    6. Test anonymized: Edit test-art/template.qmd, add `anonymized: true` under typstR:. Re-render. Verify no authors, affiliations, or acknowledgements in PDF.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All three formats scaffold without errors
2. All three formats render to PDF without errors
3. Branding hooks change the PDF output when YAML values are set
4. Anonymized mode strips author info from article PDF
5. Disclaimer page appears when enabled
6. Human visual inspection confirms layout quality
</verification>

<success_criteria>
- End-to-end: create_working_paper -> render produces polished PDF with all sections
- End-to-end: create_article -> render produces article PDF without report number
- End-to-end: create_policy_brief -> render produces brief PDF with "Summary" label
- Branding hooks visually change the output
- Anonymized mode strips identifying information
- Human approves visual quality of all three formats
</success_criteria>

<output>
After completion, create `.planning/phases/03-typst-templates-branding-and-additional-formats/03-05-SUMMARY.md`
</output>
