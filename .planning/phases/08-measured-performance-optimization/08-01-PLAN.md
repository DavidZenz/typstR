---
phase: 08-measured-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - tests/testthat/helper-performance.R
  - tests/testthat/test-performance-micro.R
  - tests/testthat/derive-v1-baseline.R
  - tests/testthat/perf-v1-baseline.yml
  - tests/testthat/perf-baseline.yml
  - tests/testthat/perf-scenario-map.yml
  - tests/testthat/test-performance-baseline-contract.R
  - tests/testthat/test-performance-regression.R
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "Performance work starts from measured hotspot scenarios with explicit v1.0 baseline derivation and ingestion (D-01, D-02)."
    - "Benchmark suites are deterministic, CRAN-safe, and Quarto-optional with explicit skip guards (D-04, D-05, D-06)."
    - "Regression guardrails detect no-backslide against current baseline while scenario mapping keeps v1.0 comparison provable for gain checks (D-09, D-10)."
  artifacts:
    - path: "tests/testthat/helper-performance.R"
      provides: "Shared deterministic fixture/benchmark helpers with bench + Quarto guard seams"
    - path: "tests/testthat/test-performance-micro.R"
      provides: "Hotspot micro-bench scenarios for validation/render-helper and scaffold-helper call paths"
    - path: "tests/testthat/derive-v1-baseline.R"
      provides: "Reproducible script that derives v1.0 medians for mapped scenarios"
    - path: "tests/testthat/perf-v1-baseline.yml"
      provides: "Committed v1.0 baseline medians keyed by scenario id"
    - path: "tests/testthat/perf-baseline.yml"
      provides: "Committed pre-optimization (current branch) medians keyed by scenario id"
    - path: "tests/testthat/perf-scenario-map.yml"
      provides: "Scenario map linking micro/regression tests to both v1.0 and current baseline keys plus tolerance/gain policy"
    - path: "tests/testthat/test-performance-baseline-contract.R"
      provides: "Executable checks proving scenario mapping and baseline ingestion consistency"
    - path: "tests/testthat/test-performance-regression.R"
      provides: "No-backslide checks comparing current medians to current-baseline+tolerance contracts"
  key_links:
    - from: "tests/testthat/test-performance-micro.R"
      to: "R/validation_environment.R"
      via: "bench scenarios calling collect_environment_checks()/validate_render_environment()"
      pattern: "collect_environment_checks|validate_render_environment"
    - from: "tests/testthat/test-performance-micro.R"
      to: "R/create_working_paper.R|R/create_article.R|R/create_policy_brief.R"
      via: "helper-driven scaffold hotspot scenarios"
      pattern: "create_working_paper|create_article|create_policy_brief"
    - from: "tests/testthat/derive-v1-baseline.R"
      to: "git tag v1.0"
      via: "derivation script emitting scenario medians into perf-v1-baseline.yml"
      pattern: "v1.0|perf-v1-baseline.yml"
    - from: "tests/testthat/test-performance-baseline-contract.R"
      to: "tests/testthat/perf-scenario-map.yml|tests/testthat/perf-v1-baseline.yml|tests/testthat/perf-baseline.yml"
      via: "assertions that every mapped scenario resolves to both baselines"
      pattern: "scenario_id|v1_baseline_key|current_baseline_key"
    - from: "tests/testthat/test-performance-regression.R"
      to: "tests/testthat/perf-baseline.yml|tests/testthat/perf-scenario-map.yml"
      via: "scenario id -> current baseline median -> slowdown tolerance lookup"
      pattern: "baseline_median_ms|slowdown_tolerance"
---

<objective>
Create deterministic performance measurement harness, v1.0 baseline derivation artifacts, and regression contracts before any optimization edits.

Purpose: PERF-01 requires provable "vs v1.0" evidence plus stable no-backslide gates; without explicit baseline derivation+mapping artifacts, gain claims are not auditable.
Output: New performance helper/test artifacts, committed baseline datasets, and executable baseline/mapping checks consumed by later gain tests.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-measured-performance-optimization/08-CONTEXT.md
@.planning/phases/08-measured-performance-optimization/08-RESEARCH.md
@.planning/phases/08-measured-performance-optimization/08-VALIDATION.md
@.planning/phases/06-pre-render-environment-validation/06-02-SUMMARY.md
@.planning/phases/07-first-run-onboarding-reliability/07-02-SUMMARY.md
@DESCRIPTION
@R/validation_environment.R
@R/render.R
@R/create_working_paper.R
@R/create_article.R
@R/create_policy_brief.R
@tests/testthat/helper-validation.R
@tests/testthat/test-validation-environment.R
@tests/testthat/test-yaml-integration.R

<interfaces>
From R/validation_environment.R:
```r
validate_render_environment <- function(path = ".")
collect_environment_checks <- function(path)
```

From R/render.R:
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE, open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE, open = interactive())
```

From create helpers:
```r
create_working_paper <- function(path, title = NULL, open = interactive())
create_article <- function(path, title = NULL, open = interactive())
create_policy_brief <- function(path, title = NULL, open = interactive())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic micro-benchmark harness for selected hotspots per D-01..D-06</name>
  <files>
    DESCRIPTION
    tests/testthat/helper-performance.R
    tests/testthat/test-performance-micro.R
  </files>
  <read_first>
    DESCRIPTION,
    tests/testthat/helper-validation.R,
    tests/testthat/test-validation-environment.R,
    R/validation_environment.R,
    R/render.R,
    R/create_working_paper.R,
    R/create_article.R,
    R/create_policy_brief.R,
    .planning/phases/08-measured-performance-optimization/08-CONTEXT.md,
    .planning/phases/08-measured-performance-optimization/08-RESEARCH.md,
    .planning/phases/08-measured-performance-optimization/08-VALIDATION.md
  </read_first>
  <action>
Add `bench` to `DESCRIPTION` Suggests only (D-06) and create `tests/testthat/helper-performance.R` with deterministic helper utilities:
- `.perf_skip_if_bench_missing()` using `testthat::skip_if_not_installed("bench")`
- `.perf_skip_if_no_quarto()` using existing Quarto guard pattern
- fixture builders for isolated temp projects and extension copies
- benchmark wrappers that always call `bench::mark(..., iterations = 60, min_iterations = 30, check = FALSE)`.

Create `tests/testthat/test-performance-micro.R` with exactly three hotspot scenarios (D-01, D-02, D-04):
1) `collect_environment_checks()` with deterministic probe stubs,
2) `validate_render_environment()` success path with stable fixture,
3) helper scaffold path (`create_working_paper()` baseline, and matrix assertions for article/policy brief call symmetry).

For each scenario, assert finite medians and bounded variability (`p95 / p50 <= 1.35`) so runs are low-noise (D-05). Keep all tests CRAN-safe and Quarto-optional through explicit skip guards.

Add smoke-focused micro checks in the same file with `test_that("smoke: ...")` names, reduced iteration budgets, and identical scenario IDs so per-task verification can assert fast Nyquist feedback (<30s) while full benchmark loops stay in wave/phase verification.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR &amp;&amp; Rscript -e 'elapsed <- system.time(testthat::test_file("tests/testthat/test-performance-micro.R", filter = "^smoke:"))[["elapsed"]]; if (elapsed &gt;= 30) stop(sprintf("smoke verify exceeded 30s: %.2f", elapsed))'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bench::mark" tests/testthat/test-performance-micro.R` shows benchmark calls for all declared scenarios.
    - `grep -n "skip_if_not_installed\("bench"\)" tests/testthat/helper-performance.R` returns at least one match.
    - `grep -n "p95" tests/testthat/test-performance-micro.R` confirms explicit noise-bound assertion logic.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-performance-micro.R")'` passes or skips only on explicit bench/Quarto guards.
  </acceptance_criteria>
  <done>Micro-benchmark harness exists, is deterministic, and produces reproducible hotspot timing data suitable for baseline capture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive/ingest v1.0 baseline artifacts and wire executable baseline comparison contracts per D-01, D-09, D-10</name>
  <files>
    tests/testthat/derive-v1-baseline.R
    tests/testthat/perf-v1-baseline.yml
    tests/testthat/perf-baseline.yml
    tests/testthat/perf-scenario-map.yml
    tests/testthat/test-performance-baseline-contract.R
    tests/testthat/test-performance-regression.R
  </files>
  <read_first>
    tests/testthat/test-performance-micro.R,
    tests/testthat/helper-performance.R,
    tests/testthat/test-render-guards.R,
    tests/testthat/test-validation-environment.R,
    .planning/phases/08-measured-performance-optimization/08-CONTEXT.md,
    .planning/phases/08-measured-performance-optimization/08-RESEARCH.md,
    .planning/phases/08-measured-performance-optimization/08-VALIDATION.md
  </read_first>
  <action>
Create `tests/testthat/perf-scenario-map.yml` defining canonical scenario IDs and explicit mapping fields:
- `scenario_id`
- `v1_baseline_key`
- `current_baseline_key`
- `slowdown_tolerance`
- `gain_target_ratio` (used in Plan 08-02 gain assertions).

Add `tests/testthat/derive-v1-baseline.R` that reruns mapped scenarios against git tag `v1.0` and emits `tests/testthat/perf-v1-baseline.yml` with `source_tag: v1.0`, measurement timestamp, and median metrics. Commit `perf-v1-baseline.yml` as the ingested artifact for roadmap "vs v1.0 baseline" proof.

Create/refresh `tests/testthat/perf-baseline.yml` for the current (pre-optimization) branch medians using the same scenario IDs so comparisons are one-to-one.

Create `tests/testthat/test-performance-baseline-contract.R` asserting every mapped scenario resolves in both baseline artifacts with numeric medians and policy fields. Update `tests/testthat/test-performance-regression.R` to enforce no-backslide against `perf-baseline.yml` using the mapped scenario policy values.

Add smoke-only contract checks in `tests/testthat/test-performance-baseline-contract.R` named `smoke: ...` that validate map/key integrity without rerunning heavy benchmark loops, so per-task verify remains fast while full regression runs stay in wave/phase verification.

Do not implement optimization edits in this task; this task only establishes auditable baseline derivation/ingestion and executable comparison wiring.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR &amp;&amp; Rscript -e 'elapsed <- system.time(testthat::test_file("tests/testthat/test-performance-baseline-contract.R", filter = "^smoke:"))[["elapsed"]]; if (elapsed &gt;= 30) stop(sprintf("smoke verify exceeded 30s: %.2f", elapsed))'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/perf-v1-baseline.yml` exists and records `source_tag: v1.0` plus numeric medians for each mapped scenario.
    - `tests/testthat/perf-scenario-map.yml` exists and includes `scenario_id`, `v1_baseline_key`, `current_baseline_key`, `slowdown_tolerance`, and `gain_target_ratio` for every scenario.
    - `grep -n "v1_baseline_key\|current_baseline_key" tests/testthat/test-performance-baseline-contract.R` confirms executable mapping assertions exist.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-performance-baseline-contract.R")'` passes, proving baseline ingestion + scenario mapping integrity.
  </acceptance_criteria>
  <done>v1.0 baseline derivation artifacts, current baseline artifacts, and executable comparison contracts are in place so PERF-01 gain claims are provable.</done>
</task>

</tasks>

<verification>
Wave/phase verification (full suites, expected to run longer than per-task smoke checks):
- `Rscript -e 'testthat::test_file("tests/testthat/test-performance-micro.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-performance-baseline-contract.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-performance-regression.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R"); testthat::test_file("tests/testthat/test-render-guards.R")'`
</verification>

<success_criteria>
- Baseline performance scenarios are deterministic, low-noise, and reproducible across runs.
- Committed artifacts provide traceable v1.0 baseline derivation/ingestion plus one-to-one scenario mapping to current baseline scenarios.
- Regression checks fail on calibrated slowdowns without breaking no-Quarto safety, and gain policy fields are ready for Plan 08-02 executable gain assertions.
- Per-task smoke verification remains under 30 seconds while full suites are preserved at wave/phase level.
</success_criteria>

<output>
After completion, create `.planning/phases/08-measured-performance-optimization/08-01-SUMMARY.md`
</output>
