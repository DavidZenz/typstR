---
phase: 08-measured-performance-optimization
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - R/validation_environment.R
  - R/render.R
  - R/scaffold_helpers.R
  - R/create_working_paper.R
  - R/create_article.R
  - R/create_policy_brief.R
  - tests/testthat/test-validation-environment.R
  - tests/testthat/test-render-guards.R
  - tests/testthat/test-scaffolding.R
  - tests/testthat/test-performance-gain.R
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "Selected helper/render hotspots show measurable runtime improvement versus mapped v1.0 baseline scenarios via executable gain assertions (PERF-01, D-01)."
    - "Optimized validation/render paths preserve diagnostics class, code set, and deterministic ordering contracts from Phases 5-7 (D-07, D-08)."
    - "Optimization removes redundant in-call work without introducing long-lived caches or stale-correctness risk (D-03)."
  artifacts:
    - path: "R/validation_environment.R"
      provides: "In-call deduplicated environment probe/preflight flow with unchanged external validation contract"
    - path: "R/scaffold_helpers.R"
      provides: "Shared internal scaffold copy/title-rewrite helper used by all create_* wrappers"
    - path: "R/create_working_paper.R"
      provides: "Working-paper scaffolder routed through shared helper without contract drift"
    - path: "R/create_article.R"
      provides: "Article scaffolder routed through shared helper without contract drift"
    - path: "R/create_policy_brief.R"
      provides: "Policy-brief scaffolder routed through shared helper without contract drift"
    - path: "tests/testthat/test-performance-regression.R"
      provides: "No-backslide checks proving optimized code remains within calibrated slowdown caps"
    - path: "tests/testthat/test-performance-gain.R"
      provides: "Executable gain checks proving selected optimized scenarios beat mapped v1.0 baselines"
  key_links:
    - from: "R/render.R"
      to: "R/validation_environment.R"
      via: "render_pub preflight call into shared validator before resolve_input()/render"
      pattern: "validate_render_environment"
    - from: "R/create_working_paper.R|R/create_article.R|R/create_policy_brief.R"
      to: "R/scaffold_helpers.R"
      via: "one shared internal scaffold implementation"
      pattern: "scaffold_"
    - from: "tests/testthat/test-validation-environment.R"
      to: "R/validation_environment.R|R/render.R"
      via: "standalone-vs-render diagnostic equivalence assertions"
      pattern: "render_pub\(\)|validate_render_environment"
    - from: "tests/testthat/test-performance-regression.R"
      to: "tests/testthat/perf-baseline.yml|tests/testthat/perf-scenario-map.yml"
      via: "current baseline median + tolerance enforcement"
      pattern: "baseline_median_ms|slowdown_tolerance"
    - from: "tests/testthat/test-performance-gain.R"
      to: "tests/testthat/perf-v1-baseline.yml|tests/testthat/perf-scenario-map.yml"
      via: "mapped scenario gain assertions against v1.0 medians"
      pattern: "v1_baseline_key|gain_target_ratio"
---

<objective>
Implement measured hotspot optimizations and prove speed gains without changing diagnostics/output semantics.

Purpose: PERF-01 is complete only when optimized hotspots show measurable improvement against v1.0 baseline mappings while Phase 5-7 behavior contracts remain intact.
Output: Optimized hotspot code paths plus updated semantic/performance tests demonstrating measurable gains, no-backslide, and no contract drift.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-measured-performance-optimization/08-CONTEXT.md
@.planning/phases/08-measured-performance-optimization/08-RESEARCH.md
@.planning/phases/08-measured-performance-optimization/08-VALIDATION.md
@.planning/phases/08-measured-performance-optimization/08-01-PLAN.md
@.planning/phases/05-structured-diagnostics-foundation/05-02-SUMMARY.md
@.planning/phases/06-pre-render-environment-validation/06-02-SUMMARY.md
@.planning/phases/07-first-run-onboarding-reliability/07-02-SUMMARY.md
@R/diagnostics.R
@R/validation_environment.R
@R/render.R
@R/create_working_paper.R
@R/create_article.R
@R/create_policy_brief.R
@R/scaffold_helpers.R
@tests/testthat/test-validation-environment.R
@tests/testthat/test-render-guards.R
@tests/testthat/test-scaffolding.R
@tests/testthat/test-yaml-integration.R
@tests/testthat/perf-scenario-map.yml
@tests/testthat/perf-v1-baseline.yml
@tests/testthat/perf-baseline.yml
@tests/testthat/test-performance-regression.R

<interfaces>
Diagnostics contract (must remain unchanged):
```r
emit_diagnostics_error(diagnostics, message, hint)
sort_diagnostics(diagnostics)
DIAGNOSTIC_CODEBOOK[c("env_quarto_unavailable", "env_typst_unavailable", "env_quarto_floor_incompatible", "env_extension_missing")]
```

Validation/render interface:
```r
validate_render_environment(path = ".")
collect_environment_checks(path)
render_pub(input = NULL, output_format = NULL, quiet = FALSE, open = interactive())
```

Scaffold helper interface:
```r
create_working_paper(path, title = NULL, open = interactive())
create_article(path, title = NULL, open = interactive())
create_policy_brief(path, title = NULL, open = interactive())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optimize validation/render hotspots and add executable gain assertions per D-01, D-03, D-07, D-08</name>
  <files>
    R/validation_environment.R
    R/render.R
    tests/testthat/test-validation-environment.R
    tests/testthat/test-render-guards.R
    tests/testthat/test-performance-gain.R
  </files>
  <read_first>
    R/validation_environment.R,
    R/render.R,
    R/diagnostics.R,
    tests/testthat/test-validation-environment.R,
    tests/testthat/test-render-guards.R,
    tests/testthat/test-performance-regression.R,
    tests/testthat/perf-scenario-map.yml,
    tests/testthat/perf-v1-baseline.yml,
    tests/testthat/perf-baseline.yml,
    .planning/phases/08-measured-performance-optimization/08-CONTEXT.md,
    .planning/phases/06-pre-render-environment-validation/06-CONTEXT.md,
    .planning/phases/05-structured-diagnostics-foundation/05-CONTEXT.md
  </read_first>
  <action>
Use measured scenarios from Plan 08-01 artifacts to optimize only preflight/render hotspots (D-01, D-02): remove duplicate per-call work in `collect_environment_checks()` and simplify `render_pub()` preflight handoff only where benchmarks prove material gain.

Hard constraints:
- Keep the public contract unchanged (`typstR_validation_report` on success, `typstR_diagnostics_error` on failure).
- Preserve diagnostic code vectors and deterministic ordering exactly as in Phases 5-7 (D-07, D-08).
- Do NOT add package-level or cross-call caches; only in-call deduplication is allowed (D-03).

Create `tests/testthat/test-performance-gain.R` and add explicit executable assertions for mapped validation/render scenarios:
- Load `perf-scenario-map.yml` + `perf-v1-baseline.yml`.
- For each optimized env scenario, assert `current_median_ms <= v1_baseline_ms * gain_target_ratio`.
- Keep Quarto-dependent scenarios guard-skipped (same guard posture as existing integration tests).

Add smoke-tagged gain checks (`test_that("smoke: validation ...")`, `test_that("smoke: render ...")`) with reduced iterations so per-task Nyquist feedback stays sub-30s; keep full gain + no-backslide suites in wave/phase verification.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR &amp;&amp; Rscript -e 'elapsed <- system.time(testthat::test_file("tests/testthat/test-performance-gain.R", filter = "^smoke: (validation|render)"))[["elapsed"]]; if (elapsed &gt;= 30) stop(sprintf("smoke verify exceeded 30s: %.2f", elapsed))'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "current_median_ms <= v1_baseline_ms \* gain_target_ratio" tests/testthat/test-performance-gain.R` shows executable gain assertions.
    - `grep -n "typstR_diagnostics_error" tests/testthat/test-validation-environment.R` still shows standalone/render equivalence coverage.
    - `grep -n "DIAG-ENV-001\|DIAG-ENV-002\|DIAG-ENV-003\|DIAG-ENV-004" tests/testthat/test-validation-environment.R` confirms stable diagnostic code assertions remain explicit.
    - `grep -n "cache\|memoise" R/validation_environment.R` returns no new long-lived cache implementation.
  </acceptance_criteria>
  <done>Validation/render hotspots are measurably faster versus mapped v1.0 baselines while diagnostics/output contracts remain behaviorally equivalent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deduplicate scaffold helper internals and enforce scaffold gain + semantic parity per D-02, D-03, D-07</name>
  <files>
    R/scaffold_helpers.R
    R/create_working_paper.R
    R/create_article.R
    R/create_policy_brief.R
    tests/testthat/test-scaffolding.R
    tests/testthat/test-performance-gain.R
  </files>
  <read_first>
    R/scaffold_helpers.R,
    R/create_working_paper.R,
    R/create_article.R,
    R/create_policy_brief.R,
    tests/testthat/test-scaffolding.R,
    tests/testthat/test-yaml-integration.R,
    tests/testthat/test-performance-gain.R,
    tests/testthat/test-performance-regression.R,
    tests/testthat/perf-scenario-map.yml,
    tests/testthat/perf-v1-baseline.yml,
    .planning/phases/07-first-run-onboarding-reliability/07-CONTEXT.md,
    .planning/phases/07-first-run-onboarding-reliability/07-VERIFICATION.md,
    .planning/phases/08-measured-performance-optimization/08-CONTEXT.md
  </read_first>
  <action>
Extract shared scaffold filesystem/title-rewrite logic into `R/scaffold_helpers.R` and route all three `create_*` wrappers through the same internal helper to remove duplicated work and drift risk (D-02, D-03).

Preserve existing user-visible behavior exactly:
- no-overwrite directory guard semantics,
- file list copied into project root,
- extension copy at `_extensions/typstR`,
- title override behavior,
- current CLI success/bullets format.

Extend `tests/testthat/test-performance-gain.R` with scaffold scenario assertions using mapped v1.0 baseline keys (`scaffold_working_paper` and any additional mapped scaffold scenario). Assert `current_median_ms <= v1_baseline_ms * gain_target_ratio` for each optimized scaffold scenario. Keep integration scenario Quarto-guarded and preserve existing semantic reliability tests.

Add smoke-tagged scaffold gain checks (`test_that("smoke: scaffold ...")`) with reduced iterations for sub-30s per-task verification. Do not modify `tests/testthat/test-performance-regression.R` in this plan; keep no-backslide tolerance policy unchanged and validate it at wave/phase verification.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR &amp;&amp; Rscript -e 'elapsed <- system.time(testthat::test_file("tests/testthat/test-performance-gain.R", filter = "^smoke: scaffold"))[["elapsed"]]; if (elapsed &gt;= 30) stop(sprintf("smoke verify exceeded 30s: %.2f", elapsed))'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "scaffold_" R/create_working_paper.R R/create_article.R R/create_policy_brief.R` shows all wrappers delegate to shared helper(s).
    - `Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R")'` passes, confirming ONB-01 scaffold contract preserved.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-performance-gain.R")'` passes with explicit v1.0 gain assertions for scaffold scenarios.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-performance-regression.R")'` passes with current-baseline no-backslide checks still enforced.
  </acceptance_criteria>
  <done>Scaffold helper hotspot optimizations deliver measurable gains versus v1.0 baselines while preserving onboarding semantic contracts.</done>
</task>

</tasks>

<verification>
Wave/phase verification (full suites, expected to run longer than per-task smoke checks):
- `Rscript -e 'testthat::test_file("tests/testthat/test-performance-gain.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-performance-regression.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R"); testthat::test_file("tests/testthat/test-render-guards.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R"); testthat::test_file("tests/testthat/test-yaml-integration.R")'`
</verification>

<success_criteria>
- Mapped PERF-01 hotspot scenarios pass executable gain assertions versus committed v1.0 baseline medians.
- Diagnostics payload and ordering semantics from Phases 5-7 remain unchanged under equivalent failing/success inputs.
- No long-lived cache layer is introduced; optimization relies on measured in-call deduplication only.
- No-backslide regression checks versus current baseline remain enforced in parallel with gain assertions.
- Per-task smoke verification remains under 30 seconds while full suites are preserved at wave/phase level.
</success_criteria>

<output>
After completion, create `.planning/phases/08-measured-performance-optimization/08-02-SUMMARY.md`
</output>
