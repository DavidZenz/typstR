---
phase: 05-structured-diagnostics-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/diagnostics.R
  - tests/testthat/test-diagnostics-contract.R
  - tests/testthat/test-diagnostics-codebook.R
  - tests/testthat/test-diagnostics-ordering.R
autonomous: true
requirements:
  - DIAG-01
must_haves:
  truths:
    - "Diagnostics emitted by Phase 05 foundations always include code, severity, location, and hint fields (D-01)."
    - "Diagnostic codes follow DIAG-<AREA>-<NNN> and remain stable for each issue class (D-06, D-07, D-08)."
    - "Diagnostics ordering is deterministic (errors first, then warnings, then location, then code) across repeated runs (D-10, D-11)."
  artifacts:
    - path: "R/diagnostics.R"
      provides: "Canonical diagnostics constructors, codebook, ordering helper, and condition emitter"
    - path: "tests/testthat/test-diagnostics-contract.R"
      provides: "Schema and required-field contract tests for diagnostics entries"
    - path: "tests/testthat/test-diagnostics-codebook.R"
      provides: "Stable-code and format enforcement tests"
    - path: "tests/testthat/test-diagnostics-ordering.R"
      provides: "Deterministic sorting and multi-diagnostic aggregation tests"
  key_links:
    - from: "tests/testthat/test-diagnostics-contract.R"
      to: "R/diagnostics.R"
      via: "constructor contract assertions"
      pattern: "new_diagnostic|expect_named"
    - from: "tests/testthat/test-diagnostics-codebook.R"
      to: "R/diagnostics.R"
      via: "codebook immutability and format checks"
      pattern: "DIAG-[A-Z]+-[0-9]{3}"
    - from: "tests/testthat/test-diagnostics-ordering.R"
      to: "R/diagnostics.R"
      via: "ordering and classed-condition payload assertions"
      pattern: "sort_diagnostics|typstR_diagnostics_error"
---

<objective>
Create the diagnostics contract substrate for Phase 05 so every downstream validation failure can emit stable, machine-testable structured diagnostics.

Purpose: Lock schema/code/order invariants before wiring call sites, preventing brittle prose-coupled behavior and flaky diagnostics ordering.
Output: New internal diagnostics module plus contract tests for required fields, stable code format, deterministic ordering, and aggregate payload semantics.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-structured-diagnostics-foundation/05-CONTEXT.md
@.planning/phases/05-structured-diagnostics-foundation/05-RESEARCH.md
@.planning/phases/05-structured-diagnostics-foundation/05-VALIDATION.md
@.planning/phases/01-skeleton-and-pipeline/01-03-SUMMARY.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-01-SUMMARY.md
@R/render.R
@R/utils.R
@tests/testthat/test-render-guards.R

<interfaces>
From R/render.R:
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE,
                       open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE,
                                 open = interactive())
quarto_available <- function()
```

From R/utils.R:
```r
resolve_input <- function(input)
open_file <- function(path)
```

From tests/testthat/test-render-guards.R:
```r
expect_error(
  render_pub("paper.qmd", open = FALSE),
  "Quarto is not installed or not on PATH\\."
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define diagnostics schema and stable codebook per D-01 through D-08</name>
  <files>
    R/diagnostics.R
    tests/testthat/test-diagnostics-contract.R
    tests/testthat/test-diagnostics-codebook.R
  </files>
  <read_first>
    .planning/phases/05-structured-diagnostics-foundation/05-CONTEXT.md,
    .planning/phases/05-structured-diagnostics-foundation/05-RESEARCH.md,
    R/render.R,
    R/utils.R,
    tests/testthat/test-render-guards.R
  </read_first>
  <behavior>
    - Test 1: each diagnostic entry includes `code`, `severity`, `location`, and `hint` (D-01), with optional `message` and `details` fields retained when present (D-02).
    - Test 2: severity only accepts `error` and `warning` (D-04) and rejects any other value.
    - Test 3: the codebook maps current issue classes to stable `DIAG-<AREA>-<NNN>` identifiers (D-06) and tests fail on repurposed or malformed codes (D-07, D-08).
    - Test 4: location supports both file-level and field-level pointers (D-03).
  </behavior>
  <action>
Create `R/diagnostics.R` as the canonical internal diagnostics module. Implement constructor/helper functions that enforce the contract decisions: required fields (`code`, `severity`, `location`, `hint`) per D-01, optional `message`/`details` per D-02, and strict two-level severity semantics per D-04. Keep all APIs internal (no export changes in this plan).

Add a single immutable codebook object in `R/diagnostics.R` for the Phase 05 issue classes, including `DIAG-RUNTIME-001`, `DIAG-INPUT-001`, `DIAG-INPUT-002`, and `DIAG-INPUT-003` per D-06. Enforce regex format and uniqueness rules so existing codes cannot be silently repurposed (D-07, D-08).

Create `tests/testthat/test-diagnostics-contract.R` and `tests/testthat/test-diagnostics-codebook.R` to assert field presence, severity restrictions, location shape, code format, and stable code mapping. Do not rely on message-only assertions in these new tests.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-contract.R"); testthat::test_file("tests/testthat/test-diagnostics-codebook.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/diagnostics.R` exists and contains `DIAG-RUNTIME-001`, `DIAG-INPUT-001`, `DIAG-INPUT-002`, and `DIAG-INPUT-003`.
    - `R/diagnostics.R` contains a code format guard matching `^DIAG-[A-Z]+-[0-9]{3}$`.
    - `tests/testthat/test-diagnostics-contract.R` contains assertions covering `code`, `severity`, `location`, and `hint`.
    - `tests/testthat/test-diagnostics-codebook.R` contains a test that fails when a code is reassigned to a different issue key.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-contract.R"); testthat::test_file("tests/testthat/test-diagnostics-codebook.R")'` exits successfully.
  </acceptance_criteria>
  <done>Diagnostics schema and code namespace are contract-locked and test-enforced for DIAG-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic ordering and aggregate diagnostics emission per D-09 through D-11</name>
  <files>
    R/diagnostics.R
    tests/testthat/test-diagnostics-ordering.R
  </files>
  <read_first>
    R/diagnostics.R,
    tests/testthat/test-diagnostics-contract.R,
    tests/testthat/test-diagnostics-codebook.R,
    .planning/phases/05-structured-diagnostics-foundation/05-CONTEXT.md,
    .planning/phases/05-structured-diagnostics-foundation/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: diagnostics are sorted deterministically as errors first, then warnings, then by location, then by code (D-10).
    - Test 2: repeated runs over unchanged diagnostic sets produce identical order and code sequence (D-11).
    - Test 3: aggregate emission path carries a list of diagnostics on a stable condition class, enabling multi-issue reporting without fail-fast collapse (D-09).
  </behavior>
  <action>
Extend `R/diagnostics.R` with deterministic ordering and aggregate emission helpers used by downstream validation call sites. Sorting must rank `error` before `warning`, then sort by normalized location (file, field), then code, with a deterministic tie-breaker so order is stable across runs per D-10/D-11.

Add an aggregate condition emitter that accepts a list of diagnostics and raises a classed condition carrying the sorted payload (for later integration in `render_pub()` and `resolve_input()`). Ensure this helper supports multi-entry diagnostics lists per D-09 and does not collapse entries into prose-only output.

Create `tests/testthat/test-diagnostics-ordering.R` to verify ordering determinism and aggregate payload behavior. Keep tests independent of Quarto CLI availability.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-contract.R"); testthat::test_file("tests/testthat/test-diagnostics-codebook.R"); testthat::test_file("tests/testthat/test-diagnostics-ordering.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `R/diagnostics.R` contains deterministic ordering logic that explicitly ranks `error` before `warning`.
    - `tests/testthat/test-diagnostics-ordering.R` includes at least one repeated-run assertion checking identical output order.
    - `tests/testthat/test-diagnostics-ordering.R` asserts aggregate condition class `typstR_diagnostics_error` and validates attached `diagnostics` payload length > 1.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-ordering.R")'` exits successfully.
  </acceptance_criteria>
  <done>The diagnostics core now guarantees deterministic multi-issue ordering and structured aggregate emission required for DIAG-01 foundation behavior.</done>
</task>

</tasks>

<verification>
Run all diagnostics-foundation tests and confirm no dependency on Quarto CLI:
- `Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-contract.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-codebook.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-diagnostics-ordering.R")'`
</verification>

<success_criteria>
- A canonical internal diagnostics module exists and enforces D-01 through D-08.
- Deterministic ordering and aggregate emission satisfy D-09 through D-11.
- New diagnostics tests pass locally without requiring Quarto CLI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-structured-diagnostics-foundation/05-01-SUMMARY.md`
</output>
