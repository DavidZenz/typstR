---
phase: 05-structured-diagnostics-foundation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - R/utils.R
  - R/render.R
  - tests/testthat/test-input-diagnostics.R
  - tests/testthat/test-render-guards.R
autonomous: true
requirements:
  - DIAG-01
must_haves:
  truths:
    - "Input-resolution and Quarto-preflight validation failures emit structured diagnostics with stable code, severity, location, and hint fields (D-01)."
    - "No-Quarto environments still behave safely: render wrappers abort truthfully with structured diagnostics instead of requiring Quarto CLI at test time."
    - "Legacy message-string expectations are migrated to payload-aware assertions without losing user-facing guidance text compatibility."
  artifacts:
    - path: "R/utils.R"
      provides: "resolve_input() diagnostics wiring for DIAG-INPUT-* issue classes"
    - path: "R/render.R"
      provides: "render preflight diagnostics wiring for DIAG-RUNTIME-001"
    - path: "tests/testthat/test-input-diagnostics.R"
      provides: "Contract tests for structured diagnostics emitted from resolve_input()"
    - path: "tests/testthat/test-render-guards.R"
      provides: "No-Quarto-safe class+payload assertions with backward-compatible message checks"
  key_links:
    - from: "R/utils.R"
      to: "R/diagnostics.R"
      via: "resolve_input() diagnostic construction and aggregate emission"
      pattern: "DIAG-INPUT-001|DIAG-INPUT-002|DIAG-INPUT-003"
    - from: "R/render.R"
      to: "R/diagnostics.R"
      via: "quarto_unavailable preflight diagnostic emission"
      pattern: "DIAG-RUNTIME-001"
    - from: "tests/testthat/test-render-guards.R"
      to: "R/render.R"
      via: "mocked quarto_available() no-CLI guard assertions"
      pattern: "class\s*=\s*\"typstR_diagnostics_error\""
---

<objective>
Wire structured diagnostics into existing validation choke points (`resolve_input()` and `render_pub()` preflight) without regressing no-Quarto-safe behavior.

Purpose: Convert current prose-only aborts into stable classed diagnostics while preserving existing user-facing guidance and testability in CRAN-like environments.
Output: Updated render/input paths plus migrated tests that assert condition class and payload contract instead of relying only on message regexes.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-structured-diagnostics-foundation/05-CONTEXT.md
@.planning/phases/05-structured-diagnostics-foundation/05-RESEARCH.md
@.planning/phases/05-structured-diagnostics-foundation/05-VALIDATION.md
@.planning/phases/05-structured-diagnostics-foundation/05-01-SUMMARY.md
@R/diagnostics.R
@R/utils.R
@R/render.R
@tests/testthat/test-render-guards.R

<interfaces>
From R/diagnostics.R (created in 05-01):
```r
new_diagnostic <- function(code, severity, location, hint, message = NULL, details = NULL)
sort_diagnostics <- function(diags)
abort_diagnostics <- function(diags, message = NULL)
DIAGNOSTIC_CODEBOOK
```

From R/utils.R:
```r
resolve_input <- function(input)
```

From R/render.R:
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE,
                       open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE,
                                 open = interactive())
quarto_available <- function()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor resolve_input() to emit structured DIAG-INPUT diagnostics per D-01 and D-06</name>
  <files>
    R/utils.R
    tests/testthat/test-input-diagnostics.R
  </files>
  <read_first>
    R/diagnostics.R,
    R/utils.R,
    tests/testthat/test-render-guards.R,
    .planning/phases/05-structured-diagnostics-foundation/05-CONTEXT.md,
    .planning/phases/05-structured-diagnostics-foundation/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: no `.qmd` in directory raises `typstR_diagnostics_error` with diagnostic code `DIAG-INPUT-001`, severity `error`, and populated `location` + `hint`.
    - Test 2: multiple `.qmd` files raise `DIAG-INPUT-002` with deterministic payload structure.
    - Test 3: missing explicit input file raises `DIAG-INPUT-003` with stable code and actionable hint.
    - Test 4: top-level user-facing message strings remain compatible with existing guidance text while payload assertions become primary.
  </behavior>
  <action>
Refactor `resolve_input()` so each existing failure branch constructs diagnostics entries via `R/diagnostics.R` instead of calling `cli::cli_abort(...)` directly. Map existing issue classes to stable codes per D-06: no `.qmd` -> `DIAG-INPUT-001`, ambiguous directory -> `DIAG-INPUT-002`, missing file path -> `DIAG-INPUT-003`.

Preserve current user-facing messaging intent (message and hint text) for backward compatibility with existing expectations, but make the structured payload the primary contract under DIAG-01. Keep success-path behavior unchanged: successful resolution still returns canonical `fs::path_real(input)`.

Create `tests/testthat/test-input-diagnostics.R` with class + payload assertions, including required fields and code stability checks. Include at least one compatibility assertion on message text so migration from message-only tests is explicit and controlled.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-input-diagnostics.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-input-diagnostics.R` exists and contains assertions for `DIAG-INPUT-001`, `DIAG-INPUT-002`, and `DIAG-INPUT-003`.
    - `tests/testthat/test-input-diagnostics.R` uses class assertions for `typstR_diagnostics_error`.
    - `R/utils.R` no longer has direct `cli::cli_abort(` calls in resolve-input failure branches.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-input-diagnostics.R")'` exits successfully.
  </acceptance_criteria>
  <done>Input-resolution failure paths now satisfy DIAG-01 as structured, stable diagnostics without changing success-path behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor render preflight and migrate guard tests to payload-first assertions per D-04, D-05, and edge-case constraints</name>
  <files>
    R/render.R
    tests/testthat/test-render-guards.R
  </files>
  <read_first>
    R/diagnostics.R,
    R/render.R,
    tests/testthat/test-render-guards.R,
    tests/testthat/test-yaml-integration.R,
    .planning/phases/05-structured-diagnostics-foundation/05-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: when `quarto_available()` is `FALSE`, `render_pub()` raises `typstR_diagnostics_error` with `DIAG-RUNTIME-001`, severity `error`, and required DIAG-01 fields.
    - Test 2: `render_working_paper()` preserves the same no-Quarto behavior and diagnostic code stability.
    - Test 3: guard tests assert class and payload first, while retaining message-fragment compatibility checks to handle existing message-string test migration.
    - Test 4: no-Quarto-safe behavior is preserved; tests do not require installed Quarto CLI.
  </behavior>
  <action>
Replace the direct Quarto preflight `cli::cli_abort` in `render_pub()` with structured diagnostic emission using `DIAG-RUNTIME-001` (severity `error`) while preserving current user-facing guidance text and hint semantics per D-05. Keep `quarto_available()` as the test seam so no-Quarto behavior remains mockable and CRAN-safe.

Migrate `tests/testthat/test-render-guards.R` from message-only assertions to class+payload assertions as primary verification. Keep the existing `Quarto is not installed or not on PATH.` message assertion as a secondary compatibility check during this phase so legacy expectations do not silently disappear.

Do not alter guarded behavior in `tests/testthat/test-yaml-integration.R`; render-dependent tests must continue to skip cleanly when Quarto is unavailable.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-render-guards.R"); testthat::test_file("tests/testthat/test-input-diagnostics.R"); testthat::test_file("tests/testthat/test-yaml-integration.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-render-guards.R` contains class assertions for `typstR_diagnostics_error` and payload code checks for `DIAG-RUNTIME-001`.
    - `tests/testthat/test-render-guards.R` still contains the message fragment `Quarto is not installed or not on PATH.` as a compatibility assertion.
    - `R/render.R` emits `DIAG-RUNTIME-001` on the Quarto-unavailable preflight branch.
    - `tests/testthat/test-yaml-integration.R` still contains `skip_if_not(quarto::quarto_available())` guards for render-dependent tests.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-render-guards.R")'` exits successfully in no-Quarto environments.
  </acceptance_criteria>
  <done>Render guard paths emit structured diagnostics and tests verify payload contracts without regressing no-Quarto-safe behavior.</done>
</task>

</tasks>

<verification>
Run diagnostics and guard-focused regression checks:
- `Rscript -e 'testthat::test_file("tests/testthat/test-input-diagnostics.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-render-guards.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R")'`
- `Rscript -e 'testthat::test_dir("tests/testthat")'`
</verification>

<success_criteria>
- Existing validation choke points (`resolve_input()`, Quarto preflight in `render_pub()`) emit DIAG-01 structured diagnostics.
- Stable codes `DIAG-INPUT-001/002/003` and `DIAG-RUNTIME-001` are asserted in tests.
- Message-string compatibility is retained while tests migrate to payload-first assertions.
- No-Quarto-safe execution and test behavior is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-structured-diagnostics-foundation/05-02-SUMMARY.md`
</output>
