---
phase: 11-documentation-content-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - pkgdown/index.md
  - vignettes/getting-started.Rmd
  - vignettes/working-papers.Rmd
  - vignettes/customizing-branding.Rmd
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "README first screen shows value, install path, and a direct Quarto render-time note."
    - "README, pkgdown home, and getting-started all use the same working-paper install -> scaffold -> render story."
    - "working-papers and customizing-branding act as follow-on guides and link back to getting-started instead of re-teaching the intro."
    - "All three vignettes build through pkgdown with Quarto removed from PATH."
  artifacts:
    - path: "README.md"
      provides: "GitHub-facing quick proof + install + prerequisite note"
    - path: "pkgdown/index.md"
      provides: "Dedicated pkgdown home-page source and navigation hub"
    - path: "vignettes/getting-started.Rmd"
      provides: "Full canonical walkthrough"
    - path: "vignettes/working-papers.Rmd"
      provides: "Working-paper metadata deep dive with intro link-back"
    - path: "vignettes/customizing-branding.Rmd"
      provides: "Practical YAML-branding guide with intro link-back"
  key_links:
    - from: "README.md"
      to: "pkgdown/index.md"
      via: "shared create_working_paper()/render_working_paper() canonical commands"
      pattern: "create_working_paper|render_working_paper"
    - from: "pkgdown/index.md"
      to: "vignettes/getting-started.Rmd"
      via: "explicit get-started handoff"
      pattern: "getting-started"
    - from: "vignettes/working-papers.Rmd"
      to: "vignettes/getting-started.Rmd"
      via: "vignette() link-back"
      pattern: "vignette\\(\"getting-started\""
    - from: "vignettes/customizing-branding.Rmd"
      to: "vignettes/getting-started.Rmd"
      via: "vignette() link-back"
      pattern: "vignette\\(\"getting-started\""
---

<objective>
Lock the documentation content surfaces into one onboarding-first story without spilling into Phase 12 site wiring.

Purpose: satisfy the README/home/article split from D-01 through D-10 and prove the article sources remain pkgdown-safe with no Quarto on PATH.
Output: refreshed README, dedicated `pkgdown/index.md`, and all three vignettes aligned around the working-paper first-run path.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-documentation-content-foundation/11-CONTEXT.md
@.planning/phases/11-documentation-content-foundation/11-RESEARCH.md
@.planning/phases/11-documentation-content-foundation/11-PATTERNS.md
@README.md
@DESCRIPTION
@vignettes/getting-started.Rmd
@vignettes/working-papers.Rmd
@vignettes/customizing-branding.Rmd

<interfaces>
From DESCRIPTION:
```text
SystemRequirements: quarto
Imports:
    quarto (>= 1.4)
```

Canonical entrypoints used across docs:
```r
create_working_paper(path, title = NULL, open = interactive())
render_working_paper(input = NULL, quiet = FALSE, open = interactive())
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split README and pkgdown home around the locked onboarding jobs</name>
  <files>README.md, pkgdown/index.md</files>
  <action>Refactor `README.md` so the first screen implements D-01, D-02, D-04, D-05, and D-06 exactly: adoption-focused opener, install command, explicit Quarto render-time requirement note, and a very short working-paper scaffold -> render preview. Create `pkgdown/index.md` as the dedicated site home source per the locked phase choice and keep it as the polished overview/navigation hub rather than a README clone. Keep article and policy brief paths secondary follow-ons, not equal first-run branches. End with one atomic commit for this task only.</action>
  <verify>
    <automated>Rscript -e 'lines &lt;- readLines("README.md", warn = FALSE, n = 35); home &lt;- readLines("pkgdown/index.md", warn = FALSE); stopifnot(any(grepl("install_github\\(\"DavidZenz/typstR\"\\)", lines)), any(grepl("Quarto", lines)), any(grepl("create_working_paper", lines)), any(grepl("render_working_paper", lines)), any(grepl("working paper", home, ignore.case = TRUE)), any(grepl("getting-started", home, fixed = TRUE)))'</automated>
  </verify>
  <done>README first screen communicates value, install, Quarto note, and the canonical working-paper preview; `pkgdown/index.md` exists and clearly hands users to getting-started plus the two follow-on articles.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the vignette suite into one canonical journey</name>
  <files>vignettes/getting-started.Rmd, vignettes/working-papers.Rmd, vignettes/customizing-branding.Rmd</files>
  <action>Update the vignette YAML metadata and body copy so `getting-started` owns the full walkthrough per D-01 and D-03, explicitly explaining `template.qmd`, `_quarto.yml`, and `references.bib` and keeping the working-paper path canonical per D-02. Reposition `working-papers` per D-08 as the metadata/title-page deep dive and `customizing-branding` per D-09 as the practical YAML knob guide. Implement D-07 and D-10 by making the later two articles visibly follow `getting-started` and link back instead of duplicating the intro. Keep any render calls non-evaluated or otherwise Quarto-safe so DOCS-03 is proven by build output, then commit atomically after the build passes.</action>
  <verify>
  <automated>Rscript -e 'stopifnot(all(file.exists(c("vignettes/working-papers.Rmd", "vignettes/customizing-branding.Rmd")))); wp &lt;- readLines("vignettes/working-papers.Rmd", warn = FALSE); cb &lt;- readLines("vignettes/customizing-branding.Rmd", warn = FALSE); stopifnot(any(grepl("vignette\\(\"getting-started\"", wp, fixed = FALSE)), any(grepl("vignette\\(\"getting-started\"", cb, fixed = FALSE)))' &amp;&amp; R CMD INSTALL . &amp;&amp; bash -lc 'TMP=$(mktemp -d) &amp;&amp; ln -s /opt/homebrew/bin/pandoc "$TMP/pandoc" &amp;&amp; PATH="$TMP:/usr/bin:/bin" Rscript -e '"'"'stopifnot(Sys.which("quarto") == ""); pkgdown::build_articles(quiet = FALSE, lazy = FALSE, preview = FALSE)'"'"' ; STATUS=$? ; rm -rf "$TMP" ; exit $STATUS'</automated>
  </verify>
  <done>All three vignettes build through `pkgdown::build_articles()` with no Quarto in PATH, and the article corpus now reads in the intended order: getting-started -&gt; working-papers -&gt; customizing-branding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs source → user shell/project | Users copy commands and workflow expectations from README/pkgdown/vignettes into real local projects |
| vignette source → pkgdown article build | Example chunks execute on the docs build runner and must not require hidden Quarto availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | README.md, pkgdown/index.md, vignettes/getting-started.Rmd | mitigate | Keep the commands and wording aligned to `create_working_paper()` and `render_working_paper()` only, per D-02, and verify the shared command strings remain present across the three onboarding surfaces. |
| T-11-02 | D | vignettes/*.Rmd | mitigate | Keep render examples non-evaluated or Quarto-guarded and run `pkgdown::build_articles()` with Quarto removed from PATH before accepting the task. |
</threat_model>

<verification>
- `Rscript -e 'lines <- readLines("README.md", warn = FALSE, n = 35); stopifnot(any(grepl("Quarto", lines)))'`
- `grep -RIn "create_working_paper\\|render_working_paper" README.md pkgdown/index.md vignettes/getting-started.Rmd`
- `Rscript -e 'wp <- readLines("vignettes/working-papers.Rmd", warn = FALSE); cb <- readLines("vignettes/customizing-branding.Rmd", warn = FALSE); stopifnot(any(grepl("vignette\\(\"getting-started\"", wp)), any(grepl("vignette\\(\"getting-started\"", cb))))'`
- `R CMD INSTALL . && bash -lc 'TMP=$(mktemp -d) && ln -s /opt/homebrew/bin/pandoc "$TMP/pandoc" && PATH="$TMP:/usr/bin:/bin" Rscript -e '"'"'stopifnot(Sys.which("quarto") == ""); pkgdown::build_articles(quiet = FALSE, lazy = FALSE, preview = FALSE)'"'"' ; STATUS=$? ; rm -rf "$TMP" ; exit $STATUS'`
</verification>

<success_criteria>
- README first screen satisfies DOCS-01 without scrolling for install/prerequisite discovery.
- `pkgdown/index.md` exists and stays lighter than `getting-started`.
- All three vignettes build successfully in the no-Quarto PATH validation sequence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-content-foundation/11-01-SUMMARY.md`
</output>
