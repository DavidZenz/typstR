---
phase: 11-documentation-content-foundation
plan: "04"
type: execute
wave: 2
depends_on:
  - "02"
  - "03"
files_modified:
  - R/create_working_paper.R
  - R/render.R
  - R/validation_environment.R
  - R/typstR-package.R
  - R/metadata_helpers.R
  - NAMESPACE
  - man/create_working_paper.Rd
  - man/render_pub.Rd
  - man/render_working_paper.Rd
  - man/validate_render_environment.Rd
  - man/typstR-package.Rd
  - man/print.typstR_author.Rd
  - man/print.typstR_affiliation.Rd
  - man/print.typstR_meta.Rd
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "Users can read accurate scaffold, render, and validation help pages with meaningful examples."
    - "Generated Rd files exist in git, including `validate_render_environment`."
    - "The refreshed roxygen sources satisfy both `check_man()` and the stricter exported-help-page audit."
  artifacts:
    - path: "R/create_working_paper.R"
      provides: "Canonical scaffold help-page source"
    - path: "R/render.R"
      provides: "Canonical render help-page source with corrected format naming"
    - path: "R/validation_environment.R"
      provides: "Validation help-page source with safe example"
    - path: "man/validate_render_environment.Rd"
      provides: "Generated reference page for the exported validator"
  key_links:
    - from: "R/*.R roxygen"
      to: "man/*.Rd"
      via: "devtools::document()"
      pattern: "Generated by roxygen2"
    - from: "tests/testthat/test-documentation-reference.R"
      to: "NAMESPACE"
      via: "exported-help-page audit gate from Plan 02"
      pattern: "export\\("
    - from: "R/render.R"
      to: "README.md"
      via: "shared render_working_paper wording"
      pattern: "typstR-typst"
---

<objective>
Close the reference-documentation quality gap with source-driven fixes and regenerated Rd files once the audit and ignore-rule prerequisites exist.

Purpose: satisfy DOCS-04 without relying on `devtools::check_man()` alone, while keeping examples short and workflow-centered per D-11 through D-13.
Output: refreshed roxygen on the main entrypoints, regenerated committed Rd files, and a generated validator help page.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-documentation-content-foundation/11-CONTEXT.md
@.planning/phases/11-documentation-content-foundation/11-RESEARCH.md
@.planning/phases/11-documentation-content-foundation/11-PATTERNS.md
@.planning/phases/11-documentation-content-foundation/11-02-PLAN.md
@.planning/phases/11-documentation-content-foundation/11-03-PLAN.md
@R/create_working_paper.R
@R/render.R
@R/validation_environment.R
@R/typstR-package.R
@R/metadata_helpers.R
@NAMESPACE

<interfaces>
From R/create_working_paper.R:
```r
create_working_paper <- function(path, title = NULL, open = interactive())
```

From R/render.R:
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE,
                       open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE,
                                 open = interactive())
```

From R/validation_environment.R:
```r
validate_render_environment <- function(path = ".")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh roxygen sources and regenerate the committed reference pages</name>
  <files>R/create_working_paper.R, R/render.R, R/validation_environment.R, R/typstR-package.R, R/metadata_helpers.R, NAMESPACE, man/create_working_paper.Rd, man/render_pub.Rd, man/render_working_paper.Rd, man/validate_render_environment.Rd, man/typstR-package.Rd, man/print.typstR_author.Rd, man/print.typstR_affiliation.Rd, man/print.typstR_meta.Rd</files>
  <action>Update the roxygen in the high-traffic entrypoints first per D-13: keep `create_working_paper()` and `render_working_paper()` examples short, runnable, and aligned to the canonical scaffold -> edit -> render flow per D-11 and D-12; correct the stale `typstR-workingpaper` wording in `R/render.R` to the real `typstR-typst` format name; add a safe `\dontrun{}` example to `validate_render_environment()` using the existing prerequisite wording; and keep `R/typstR-package.R` aligned with the same onboarding story. If the audit from Plan 02 still fails on the existing print-method pages, add minimal one-line examples in `R/metadata_helpers.R` so the generated pages pass without turning them into mini-vignettes. Regenerate docs from source with roxygen; do not hand-edit any `.Rd` file. Commit atomically only after `check_man()` and the new audit are green.</action>
  <verify>
    <automated>Rscript -e 'devtools::document(quiet = TRUE); devtools::check_man(); testthat::test_file("tests/testthat/test-documentation-reference.R", reporter = testthat::StopReporter$new())'</automated>
  </verify>
  <done>`devtools::check_man()` is clean, the stricter audit passes, `man/validate_render_environment.Rd` exists, and the committed Rd files match the refreshed source comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roxygen source → generated Rd | Source comments become the published reference contract users see on pkgdown and in local help |
| exported API list → audit test | The audit must reflect the real exported/user-facing surface instead of a stale manual checklist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | T | R/render.R, R/validation_environment.R, man/*.Rd | mitigate | Regenerate docs from roxygen source and assert the resulting Rd pages contain the expected title/description/arguments/examples sections. |
| T-11-04 | R | tests/testthat/test-documentation-reference.R | mitigate | Gate the regeneration step on the new audit so the reference refresh cannot pass while exported pages remain incomplete. |
</threat_model>

<verification>
- `Rscript -e 'devtools::document(quiet = TRUE)'`
- `Rscript -e 'devtools::check_man()'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-documentation-reference.R", reporter = testthat::StopReporter$new())'`
</verification>

<success_criteria>
- Every exported/user-facing help page required by the audit exposes meaningful title/description/params/examples content.
- `render_working_paper` docs no longer mention the stale format name.
- `validate_render_environment` has a generated committed Rd page and a safe example.
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-content-foundation/11-04-SUMMARY.md`
</output>
