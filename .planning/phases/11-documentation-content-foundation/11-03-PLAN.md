---
phase: 11-documentation-content-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - NEWS.md
  - .gitignore
  - .Rbuildignore
autonomous: true
requirements:
  - DOCS-05
  - SHIP-04
must_haves:
  truths:
    - "Users can browse a pkgdown-readable changelog covering v1.0, v1.1, and v1.2."
    - "Generated `man/*.Rd` files are no longer ignored by git."
    - "pkgdown-only artifacts stay out of the built package tarball."
  artifacts:
    - path: "NEWS.md"
      provides: "pkgdown news source with v1.0-v1.2 headings"
    - path: ".gitignore"
      provides: "Git tracking rules that allow committed Rd files"
    - path: ".Rbuildignore"
      provides: "Tarball exclusions for pkgdown-only artifacts"
  key_links:
    - from: "NEWS.md"
      to: "pkgdown news build"
      via: "pkgdown heading parser"
      pattern: "^# typstR "
    - from: ".gitignore"
      to: "man/*.Rd"
      via: "git tracking"
      pattern: "man/\\*\\.Rd"
    - from: ".Rbuildignore"
      to: "pkgdown/index.md, docs/"
      via: "R CMD build exclusion"
      pattern: "^pkgdown$|^docs$"
---

<objective>
Add the changelog source and lock the git/build-ignore split so documentation sources are tracked without polluting release tarballs.

Purpose: satisfy DOCS-05 and SHIP-04 now, before Phase 12 site config and Phase 13 CI assume these source artifacts are stable.
Output: `NEWS.md`, tracked `man/*.Rd`, and `.Rbuildignore` rules that exclude pkgdown-only outputs/sources from package builds.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-documentation-content-foundation/11-CONTEXT.md
@.planning/phases/11-documentation-content-foundation/11-RESEARCH.md
@.planning/phases/11-documentation-content-foundation/11-PATTERNS.md
@.gitignore
@.Rbuildignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the pkgdown-readable milestone changelog source</name>
  <files>NEWS.md</files>
  <action>Create `NEWS.md` with pkgdown-friendly level-one headings in the locked Phase 11 format: `# typstR 1.0`, `# typstR 1.1`, and `# typstR 1.2`. Summarize each shipped/active milestone from the roadmap in concise user-facing bullets, keeping the v1.2 entry focused on the documentation-and-website-polish work rather than future deployment details. Commit the changelog as its own atomic task after the heading audit passes.</action>
  <verify>
    <automated>grep -n '^# typstR ' NEWS.md</automated>
  </verify>
  <done>`NEWS.md` exists, includes exactly the three required milestone headings, and reads cleanly as pkgdown changelog source.</done>
</task>

<task type="auto">
  <name>Task 2: Allow committed Rd files while excluding pkgdown artifacts from builds</name>
  <files>.gitignore, .Rbuildignore</files>
  <action>Remove the `man/*.Rd` ignore rule so generated reference files can be committed, preserving the existing plain-glob `.gitignore` style. Add anchored-regex exclusions in `.Rbuildignore` for `docs/` and `pkgdown/` so the dedicated `pkgdown/index.md` source and later built site output stay out of the CRAN/source tarball per SHIP-04. Do not add Phase 12/13 site wiring here. Verify with git-ignore and tarball inspection before committing this task atomically.</action>
  <verify>
    <automated>bash -lc 'if git check-ignore man/create_working_paper.Rd >/dev/null 2>&amp;1; then echo "man page still ignored"; exit 1; fi &amp;&amp; R CMD build . &gt;/dev/null &amp;&amp; PKG=$(ls -t typstR_*.tar.gz | head -n 1) &amp;&amp; if tar -tf "$PKG" | grep -E "(^typstR/docs/|^typstR/pkgdown/)"; then echo "pkgdown artifact leaked into tarball"; exit 1; fi'</automated>
  </verify>
  <done>`man/*.Rd` is trackable by git, `docs/` and `pkgdown/` are excluded from the built tarball, and no Phase 12 site-output files are introduced into package contents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source tree → built package tarball | Files safe for git are not always safe for CRAN/source package distribution |
| NEWS.md source → pkgdown changelog page | Heading format controls whether pkgdown exposes the changelog entries correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | I | .Rbuildignore, built tarball | mitigate | Add explicit `^docs$` and `^pkgdown$` exclusions and verify by inspecting the newest `R CMD build` tarball contents. |
| T-11-06 | T | NEWS.md | mitigate | Use the exact `# typstR X.Y` heading pattern and verify with grep so pkgdown's changelog parser receives predictable input. |
</threat_model>

<verification>
- `grep -n '^# typstR ' NEWS.md`
- `git check-ignore man/create_working_paper.Rd || true`
- `R CMD build .`
- `tar -tf "$(ls -t typstR_*.tar.gz | head -n 1)" | grep -E '(^typstR/docs/|^typstR/pkgdown/)'`
</verification>

<success_criteria>
- `NEWS.md` exists with the three required milestone headings.
- `.gitignore` no longer blocks committed Rd files.
- `.Rbuildignore` keeps pkgdown source/output artifacts out of the tarball.
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-content-foundation/11-03-SUMMARY.md`
</output>
