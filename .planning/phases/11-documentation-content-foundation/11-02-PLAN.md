---
phase: 11-documentation-content-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-documentation-reference.R
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "The repo has an automated audit that catches missing exported-help-page sections beyond `devtools::check_man()`."
  artifacts:
    - path: "tests/testthat/test-documentation-reference.R"
      provides: "Regression audit for exported help-page completeness"
  key_links:
    - from: "tests/testthat/test-documentation-reference.R"
      to: "NAMESPACE"
      via: "enumerate exported help pages"
      pattern: "export\\("
    - from: "tests/testthat/test-documentation-reference.R"
      to: "man/*.Rd"
      via: "section-level assertions on generated help pages"
      pattern: "\\\\title\\{|\\\\description\\{|\\\\examples\\{"
---

<objective>
Add the stricter exported-help-page audit before any reference refresh lands.

Purpose: satisfy the research finding that `devtools::check_man()` alone is insufficient and create a deliberate RED-state gate for the follow-on roxygen plan.
Output: one permanent `testthat` audit file that fails on the current documentation gaps.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-documentation-content-foundation/11-CONTEXT.md
@.planning/phases/11-documentation-content-foundation/11-RESEARCH.md
@.planning/phases/11-documentation-content-foundation/11-PATTERNS.md
@R/create_working_paper.R
@R/render.R
@R/validation_environment.R
@R/typstR-package.R
@R/metadata_helpers.R
@NAMESPACE

<interfaces>
From R/create_working_paper.R:
```r
create_working_paper <- function(path, title = NULL, open = interactive())
```

From R/render.R:
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE,
                       open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE,
                                 open = interactive())
```

From R/validation_environment.R:
```r
validate_render_environment <- function(path = ".")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the exported-help-page audit before refreshing docs</name>
  <files>tests/testthat/test-documentation-reference.R</files>
  <behavior>
    - Test 1: fails if an exported help page is missing `\title{}` or `\description{}`.
    - Test 2: fails if a function help page that should expose arguments/examples is missing `\arguments{}` or `\examples{}`.
    - Test 3: fails if `validate_render_environment` is exported but `man/validate_render_environment.Rd` does not exist.
  </behavior>
  <action>Create a focused `testthat` audit that reads `NAMESPACE` plus `man/*.Rd` and enforces the stricter DOCS-04 contract the research identified. Exempt `typstR-package` from parameter/example expectations only where the Rd form truly differs, but do not exempt user-facing function pages. Make the test fail on the current gaps first, then keep it as a permanent regression gate. Commit this RED-state test atomically before the roxygen fixes.</action>
  <verify>
    <automated>Rscript -e 'ok &lt;- FALSE; tryCatch(testthat::test_file("tests/testthat/test-documentation-reference.R", reporter = testthat::StopReporter$new()), error = function(e) ok &lt;&lt;- TRUE); if (!ok) quit(status = 1)'</automated>
  </verify>
  <done>The audit file exists, targets exported/help-page completeness rather than generic Rd syntax only, and fails against the current missing/stale documentation state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roxygen source → generated Rd | Source comments become the published reference contract users see on pkgdown and in local help |
| exported API list → audit test | The audit must reflect the real exported/user-facing surface instead of a stale manual checklist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | T | tests/testthat/test-documentation-reference.R | mitigate | Read `NAMESPACE` and generated `man/*.Rd` directly so the audit fails before any stale or incomplete help page can ship. |
| T-11-04 | R | tests/testthat/test-documentation-reference.R | mitigate | Derive the audit inputs from `NAMESPACE` and `man/*.Rd` so the regression check is tied to the real exported surface, not a hand-maintained list that can drift. |
</threat_model>

<verification>
- `Rscript -e 'ok <- FALSE; tryCatch(testthat::test_file("tests/testthat/test-documentation-reference.R", reporter = testthat::StopReporter$new()), error = function(e) ok <<- TRUE); if (!ok) quit(status = 1)'`
</verification>

<success_criteria>
- The stricter exported-help-page audit exists and fails on the current repo state before roxygen fixes.
- The follow-on reference-refresh plan can use this test as its gating signal.
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-content-foundation/11-02-SUMMARY.md`
</output>
