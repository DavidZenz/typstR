---
phase: 10-supported-environment-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-first-run-onboarding-reliability/07-VERIFICATION.md
  - .planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md
  - tests/testthat/test-yaml-integration.R
  - tests/testthat/test-validation-environment.R
  - R/validation_environment.R
  - R/render.R
  - R/scaffold_helpers.R
  - R/create_working_paper.R
  - R/create_article.R
  - R/create_policy_brief.R
autonomous: true
requirements:
  - ONB-01
must_haves:
  truths:
    - "On a supported setup, helper-generated working paper, article, and policy brief projects pass `validate_render_environment()` without manual repair (ONB-01)."
    - "On a supported setup, helper-generated working paper, article, and policy brief projects render successfully through package wrappers without manual template edits (ONB-01)."
    - "Phase 07 verification and validation artifacts record concrete Quarto-enabled runtime evidence instead of `human_needed` deferrals."
  artifacts:
    - path: ".planning/phases/07-first-run-onboarding-reliability/07-VERIFICATION.md"
      provides: "Passed onboarding verification with supported-environment validation/render evidence"
    - path: ".planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md"
      provides: "Validation ledger updated from deferred manual-only proof to recorded runtime evidence"
    - path: "tests/testthat/test-yaml-integration.R"
      provides: "Existing helper-driven scaffold -> validate -> render contract exercised on supported setup"
  key_links:
    - from: "tests/testthat/test-yaml-integration.R"
      to: "R/validation_environment.R"
      via: "helper-driven `validate_render_environment()` assertions for all supported formats"
      pattern: "validate_render_environment"
    - from: "tests/testthat/test-yaml-integration.R"
      to: "R/render.R"
      via: "helper-driven render assertions through package wrappers"
      pattern: "render_pub|render_working_paper"
    - from: ".planning/phases/07-first-run-onboarding-reliability/07-VERIFICATION.md"
      to: "tests/testthat/test-yaml-integration.R"
      via: "verification report cites the actual supported-environment command output"
      pattern: "test-yaml-integration.R"
---

<objective>
Close the onboarding side of the milestone audit by capturing supported-environment scaffold -> validate -> render evidence for all supported formats.

Purpose: `ONB-01` cannot be marked fully evidenced until the real helper-driven runtime path is observed on a Quarto-enabled setup.
Output: Updated Phase 07 verification/validation artifacts, plus any smallest-safe contract-surface fixes needed to make the supported-environment onboarding matrix pass truthfully.
</objective>

<execution_context>
@/Users/davidzenz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/10-supported-environment-verification-closure/10-CONTEXT.md
@.planning/phases/10-supported-environment-verification-closure/10-PATTERNS.md
@.planning/phases/07-first-run-onboarding-reliability/07-VERIFICATION.md
@.planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md
@tests/testthat/test-yaml-integration.R
@tests/testthat/test-validation-environment.R
@R/validation_environment.R
@R/render.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute supported-environment onboarding validation/render matrix and capture raw evidence</name>
  <files>.planning/phases/07-first-run-onboarding-reliability/07-VERIFICATION.md, .planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md</files>
  <read_first>.planning/phases/10-supported-environment-verification-closure/10-CONTEXT.md, .planning/phases/07-first-run-onboarding-reliability/07-VERIFICATION.md, .planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md, tests/testthat/test-yaml-integration.R</read_first>
  <action>Run the existing helper-driven onboarding contracts on a supported Quarto-enabled setup. Capture the exact command results for the validation and render portions of `tests/testthat/test-yaml-integration.R`, including evidence that working paper, article, and policy brief all execute instead of skipping. Update Phase 07 verification and validation artifacts so the previous `human_needed` boundary is replaced by concrete runtime evidence, preserving the existing document structure from `10-PATTERNS.md` rather than rewriting them into terse notes. If the contracts pass as-is, prefer documentation-only evidence updates in this task.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R")'</automated>
  </verify>
  <acceptance_criteria>
    - The Quarto-enabled onboarding matrix executes without the prior Quarto-availability skips for the supported-format validation/render assertions.
    - Phase 07 verification no longer lists onboarding runtime evidence as `human_needed` if the command passed.
    - Phase 07 validation no longer describes supported-environment onboarding proof as deferred if the command passed.
    - The evidence recorded identifies all three supported formats, not just one smoke path.
  </acceptance_criteria>
  <done>Phase 07 artifacts carry concrete supported-environment onboarding evidence instead of an audit-blocking deferral.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply smallest-safe onboarding contract repairs only if supported-environment execution exposes drift</name>
  <files>tests/testthat/test-yaml-integration.R, tests/testthat/test-validation-environment.R, R/validation_environment.R, R/render.R, R/scaffold_helpers.R, R/create_working_paper.R, R/create_article.R, R/create_policy_brief.R</files>
  <read_first>tests/testthat/test-yaml-integration.R, tests/testthat/test-validation-environment.R, R/validation_environment.R, R/render.R, R/scaffold_helpers.R, R/create_working_paper.R, R/create_article.R, R/create_policy_brief.R, .planning/phases/10-supported-environment-verification-closure/10-PATTERNS.md</read_first>
  <action>If Task 1 reveals real supported-environment onboarding failures, repair only the exercised contract surface needed to make the already-claimed Phase 07 behavior true. Keep fixes bounded to helper scaffolding, validation preflight, render wrapper dispatch, or onboarding integration assertions that are demonstrably wrong on supported setups. Do not add new onboarding features, change format scope, or weaken the runtime contract to make the audit pass. After any fix, rerun the same onboarding matrix until the evidence is clean and then update the Phase 07 artifacts with the final result.</action>
  <verify>
    <automated>Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R"); testthat::test_file("tests/testthat/test-validation-environment.R")'</automated>
  </verify>
  <acceptance_criteria>
    - Any code/test edits are directly tied to an observed supported-environment onboarding failure.
    - The helper-driven scaffold -> validate -> render path passes for working paper, article, and policy brief after the repair loop.
    - No new skips, fake mocks, or reduced assertions are introduced to hide runtime truth.
    - The final Phase 07 docs match the last green supported-environment run.
  </acceptance_criteria>
  <done>Any onboarding drift exposed by supported-environment execution is fixed narrowly, rerun, and documented truthfully.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-validation-environment.R")'`
</verification>

<success_criteria>
- Supported-environment onboarding evidence exists for validation and render across all supported formats.
- Phase 07 audit artifacts no longer rely on a Quarto-enabled human-needed caveat.
- Any fixes made are bounded to the exercised onboarding contract surface.
</success_criteria>

<output>
After completion, create `.planning/phases/10-supported-environment-verification-closure/10-01-SUMMARY.md`
</output>
