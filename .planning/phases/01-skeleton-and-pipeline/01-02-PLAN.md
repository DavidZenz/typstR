---
phase: 01-skeleton-and-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - R/create_working_paper.R
  - inst/templates/workingpaper/template.qmd
  - inst/templates/workingpaper/_quarto.yml
  - inst/templates/workingpaper/references.bib
autonomous: true
requirements:
  - FOUN-03
  - SCAF-01

must_haves:
  truths:
    - "create_working_paper('my-paper') creates a directory with template.qmd, _quarto.yml, references.bib, and _extensions/typstR/"
    - "create_working_paper() errors if the target directory already exists"
    - "create_working_paper(path, title = 'My Title') pre-fills the title in template.qmd YAML"
    - "Scaffolded template.qmd references format: typstR-workingpaper and includes rich section structure"
  artifacts:
    - path: "R/create_working_paper.R"
      provides: "Scaffolding function"
      exports: ["create_working_paper"]
    - path: "inst/templates/workingpaper/template.qmd"
      provides: "Rich starter template for working papers"
      contains: "typstR-workingpaper"
    - path: "inst/templates/workingpaper/_quarto.yml"
      provides: "Quarto project config"
      contains: "project"
    - path: "inst/templates/workingpaper/references.bib"
      provides: "Dummy bibliography for first render"
      contains: "@article"
  key_links:
    - from: "R/create_working_paper.R"
      to: "inst/quarto/extensions/typstR/"
      via: "system.file() + fs::dir_copy()"
      pattern: 'system\\.file.*quarto/extensions/typstR'
    - from: "R/create_working_paper.R"
      to: "inst/templates/workingpaper/"
      via: "system.file() + file copy"
      pattern: 'system\\.file.*templates/workingpaper'
    - from: "inst/templates/workingpaper/template.qmd"
      to: "inst/quarto/extensions/typstR/_extension.yml"
      via: "format: typstR-workingpaper reference"
      pattern: "typstR-workingpaper"
---

<objective>
Create the project scaffolding function and the template files it copies. After this plan, `create_working_paper("my-paper")` generates a complete project directory ready to render.

Purpose: This is the primary user-facing entry point -- researchers start here.
Output: R/create_working_paper.R and inst/templates/workingpaper/ with template files.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-and-pipeline/01-CONTEXT.md
@.planning/phases/01-skeleton-and-pipeline/01-RESEARCH.md
@.planning/phases/01-skeleton-and-pipeline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template files for working paper scaffold</name>
  <files>inst/templates/workingpaper/template.qmd, inst/templates/workingpaper/_quarto.yml, inst/templates/workingpaper/references.bib</files>
  <read_first>
    - .planning/phases/01-skeleton-and-pipeline/01-RESEARCH.md (exact template.qmd content, _quarto.yml content, references.bib content)
    - .planning/phases/01-skeleton-and-pipeline/01-CONTEXT.md (locked decisions on rich template content, title argument, _quarto.yml inclusion)
    - inst/quarto/extensions/typstR/_extension.yml (verify format name is typstR-workingpaper)
  </read_first>
  <action>
Create directory `inst/templates/workingpaper/` and populate with three files:

**template.qmd:**
```yaml
---
title: "Working Paper Title"
author:
  - name: "Author One"
  - name: "Author Two"
abstract: |
  Replace this placeholder with your abstract. This paper examines...
bibliography: references.bib
format:
  typstR-workingpaper: default
---
```
Then the body content (markdown, not YAML):
```markdown
## Introduction

Introduce the research question here. @smith2020 provide relevant background.

## Data and Methods

Describe data sources and empirical strategy.

## Results

Present main findings.

## Conclusion

Summarize contributions and implications.

## References
```

This is a rich starter template per locked decision. It includes placeholder title, author block, abstract, all standard academic sections, and a bibliography citation (`@smith2020`) so the bibliography renders on first build.

**_quarto.yml:**
```yaml
project:
  type: default
```

Minimal project config per locked decision. This enables proper Quarto project behavior.

**references.bib:**
```bibtex
@article{smith2020,
  title   = {An Example Working Paper},
  author  = {Smith, Jane},
  journal = {Journal of Example Studies},
  year    = {2020},
  volume  = {1},
  pages   = {1--20}
}
```

One dummy entry that matches the `@smith2020` citation in template.qmd. This ensures the bibliography renders on first build.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && test -f inst/templates/workingpaper/template.qmd && test -f inst/templates/workingpaper/_quarto.yml && test -f inst/templates/workingpaper/references.bib && grep -q "typstR-workingpaper" inst/templates/workingpaper/template.qmd && grep -q "smith2020" inst/templates/workingpaper/references.bib && echo "TEMPLATES OK"</automated>
  </verify>
  <acceptance_criteria>
    - inst/templates/workingpaper/template.qmd contains "typstR-workingpaper"
    - inst/templates/workingpaper/template.qmd contains "## Introduction"
    - inst/templates/workingpaper/template.qmd contains "@smith2020"
    - inst/templates/workingpaper/template.qmd contains 'title: "Working Paper Title"'
    - inst/templates/workingpaper/_quarto.yml contains "project:" and "type: default"
    - inst/templates/workingpaper/references.bib contains "@article{smith2020,"
  </acceptance_criteria>
  <done>Three template files exist with rich content; template.qmd references typstR-workingpaper format; references.bib has a dummy entry matching the template citation</done>
</task>

<task type="auto">
  <name>Task 2: Create create_working_paper() scaffolding function</name>
  <files>R/create_working_paper.R</files>
  <read_first>
    - .planning/phases/01-skeleton-and-pipeline/01-RESEARCH.md (Pattern 1: Extension-Copy Scaffolding, complete function example)
    - .planning/phases/01-skeleton-and-pipeline/01-CONTEXT.md (locked decisions: error on existing dir, cli-styled output, title argument, extension copied only during scaffolding)
    - DESCRIPTION (verify imports: cli, fs, rlang)
    - inst/templates/workingpaper/template.qmd (verify the title line format for gsub replacement)
    - inst/quarto/extensions/typstR/_extension.yml (verify extension path for system.file)
  </read_first>
  <action>
Create `R/create_working_paper.R` with the exported `create_working_paper()` function. Implementation:

```r
#' Create a working paper project
#'
#' Scaffolds a new working paper project directory with a Quarto document,
#' bibliography, project configuration, and the typstR Quarto extension.
#'
#' @param path Path to the new project directory. Must not already exist.
#' @param title Optional title to pre-fill in the template YAML.
#' @param open Whether to open the project directory. Defaults to `TRUE`
#'   in interactive sessions.
#' @return The project path, invisibly.
#' @export
create_working_paper <- function(path, title = NULL, open = interactive()) {
  # Guard: error if directory exists (locked decision: no overwrite, no force argument)
  if (fs::dir_exists(path)) {
    cli::cli_abort(c(
      "Directory {.path {path}} already exists.",
      "i" = "Choose a different path or remove the existing directory."
    ))
  }

  # Create project directory
  fs::dir_create(path)

  # Copy starter template files from inst/templates/workingpaper/
  template_src <- system.file("templates/workingpaper", package = "typstR",
                               mustWork = TRUE)
  template_files <- fs::dir_ls(template_src, all = TRUE, recurse = FALSE)
  fs::file_copy(template_files, fs::path(path, fs::path_file(template_files)))

  # Copy extension into _extensions/typstR/ within the new project (FOUN-03)
  ext_src <- system.file("quarto/extensions/typstR", package = "typstR",
                          mustWork = TRUE)
  ext_dest <- fs::path(path, "_extensions", "typstR")
  fs::dir_create(fs::path(path, "_extensions"))
  fs::dir_copy(ext_src, ext_dest)

  # Optionally pre-fill title in template.qmd
  if (!is.null(title)) {
    qmd_path <- fs::path(path, "template.qmd")
    lines <- readLines(qmd_path)
    lines <- sub('title: "Working Paper Title"',
                 paste0('title: "', title, '"'),
                 lines, fixed = TRUE)
    writeLines(lines, qmd_path)
  }

  # cli success summary (locked decision: usethis-style checkmarks)
  cli::cli_alert_success("Created working paper project at {.path {path}}")
  cli::cli_bullets(c(
    "*" = "{.file template.qmd}",
    "*" = "{.file _quarto.yml}",
    "*" = "{.file references.bib}",
    "*" = "{.file _extensions/typstR/}"
  ))

  invisible(path)
}
```

Key implementation details:
- Uses `system.file(..., mustWork = TRUE)` for both template and extension paths (Pitfall 6 from research)
- Copies individual template files (not dir_copy of the template dir, to avoid nesting)
- Extension copy uses `fs::dir_copy()` into `_extensions/typstR/` (the ONLY mechanism Quarto supports)
- Title replacement uses `sub()` with `fixed = TRUE` to match the exact placeholder string
- Returns path invisibly per standard R convention
- `open` parameter accepted but not wired to anything in Phase 1 (scaffolding opens project in IDE -- deferred)

Also update NAMESPACE by running roxygen2, OR manually add the export. The executor should run `devtools::document()` after creating the file.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e "devtools::load_all('.'); tmp <- withr::local_tempdir(); p <- file.path(tmp, 'test-paper'); create_working_paper(p, title='Test Title'); stopifnot(file.exists(file.path(p, 'template.qmd'))); stopifnot(file.exists(file.path(p, '_quarto.yml'))); stopifnot(file.exists(file.path(p, 'references.bib'))); stopifnot(file.exists(file.path(p, '_extensions', 'typstR', '_extension.yml'))); lines <- readLines(file.path(p, 'template.qmd')); stopifnot(any(grepl('Test Title', lines))); cat('SCAFFOLD OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/create_working_paper.R contains "#' @export" roxygen tag
    - R/create_working_paper.R contains "create_working_paper <- function(path, title = NULL, open = interactive())"
    - R/create_working_paper.R contains 'system.file("templates/workingpaper", package = "typstR", mustWork = TRUE)'
    - R/create_working_paper.R contains 'system.file("quarto/extensions/typstR", package = "typstR", mustWork = TRUE)'
    - R/create_working_paper.R contains "cli::cli_abort" for existing directory check
    - R/create_working_paper.R contains "fs::dir_copy(ext_src, ext_dest)" for extension copy
    - create_working_paper(tempdir_path) creates template.qmd, _quarto.yml, references.bib, and _extensions/typstR/_extension.yml
    - create_working_paper(existing_dir) throws an error
    - create_working_paper(path, title="Custom") replaces title in template.qmd
  </acceptance_criteria>
  <done>create_working_paper() scaffolds a complete project directory with all template files, copies extension to _extensions/typstR/, pre-fills title, and prints cli success summary; errors on existing directory</done>
</task>

</tasks>

<verification>
- `devtools::load_all(".")` succeeds with create_working_paper available
- `create_working_paper(tempdir)` creates all expected files
- `create_working_paper(existing_dir)` errors with informative message
- Title pre-fill works when title argument is provided
- Extension files are present in scaffolded project's _extensions/typstR/
</verification>

<success_criteria>
- create_working_paper() is exported and functional
- Scaffolded project has template.qmd, _quarto.yml, references.bib, _extensions/typstR/
- Extension copy works correctly from inst/ to user project
- Error handling for existing directories works
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-and-pipeline/01-02-SUMMARY.md`
</output>
