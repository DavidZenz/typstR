---
phase: 07-first-run-onboarding-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-scaffolding.R
  - inst/templates/workingpaper/template.qmd
  - inst/templates/article/template.qmd
  - inst/templates/policy-brief/template.qmd
autonomous: true
requirements:
  - ONB-01
must_haves:
  truths:
    - "Each create_* helper scaffolds a starter with a shared runnable baseline and only format-specific deltas where required (D-03)."
    - "Starter front matter includes realistic first-run defaults for typstR fields relevant to each format without requiring manual template repair (D-02)."
    - "Inline onboarding guidance appears inside template.qmd near editable fields and preserves valid YAML/renderable starter structure (D-01, D-04)."
  artifacts:
    - path: "tests/testthat/test-scaffolding.R"
      provides: "Contract tests that lock shared baseline starter expectations and format-specific deltas for all three scaffold helpers"
    - path: "inst/templates/workingpaper/template.qmd"
      provides: "Hybrid runnable working-paper starter with representative defaults and inline guidance"
    - path: "inst/templates/article/template.qmd"
      provides: "Hybrid runnable article starter aligned to shared baseline plus article-only deltas"
    - path: "inst/templates/policy-brief/template.qmd"
      provides: "Hybrid runnable policy-brief starter aligned to shared baseline plus brief-only deltas"
  key_links:
    - from: "R/create_working_paper.R"
      to: "inst/templates/workingpaper/template.qmd"
      via: "template copy into scaffolded project"
      pattern: "templates/workingpaper"
    - from: "R/create_article.R"
      to: "inst/templates/article/template.qmd"
      via: "template copy into scaffolded project"
      pattern: "templates/article"
    - from: "R/create_policy_brief.R"
      to: "inst/templates/policy-brief/template.qmd"
      via: "template copy into scaffolded project"
      pattern: "templates/policy-brief"
---

<objective>
Lock the starter-template contract so each scaffolded format is first-run runnable, realistic, and consistently structured before integration-path verification.

Purpose: ONB-01 depends on truthful starter content; if templates drift or guidance breaks YAML, downstream validation/render checks become flaky and misleading.
Output: Cross-format scaffold contract tests plus updated template.qmd files implementing D-01 through D-04.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-first-run-onboarding-reliability/07-CONTEXT.md
@.planning/phases/07-first-run-onboarding-reliability/07-RESEARCH.md
@.planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md
@.planning/phases/04-tests-documentation-and-cran-hardening/04-02-SUMMARY.md
@R/create_working_paper.R
@R/create_article.R
@R/create_policy_brief.R
@tests/testthat/test-scaffolding.R
@inst/templates/workingpaper/template.qmd
@inst/templates/article/template.qmd
@inst/templates/policy-brief/template.qmd

<interfaces>
From R/create_working_paper.R, R/create_article.R, R/create_policy_brief.R:
```r
create_working_paper <- function(path, title = NULL, open = interactive())
create_article <- function(path, title = NULL, open = interactive())
create_policy_brief <- function(path, title = NULL, open = interactive())
```

From tests/testthat/test-scaffolding.R:
```r
.with_template_lookup(fn_name, repo_root, code)
```

From template contracts:
```yaml
format:
  typstR-typst: default
typstR:
  format-variant: article|brief   # format-specific delta where required
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode first-run starter contract for all scaffold helpers per D-01..D-04</name>
  <files>
    tests/testthat/test-scaffolding.R
  </files>
  <read_first>
    tests/testthat/test-scaffolding.R,
    .planning/phases/07-first-run-onboarding-reliability/07-CONTEXT.md,
    .planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md,
    R/create_working_paper.R,
    R/create_article.R,
    R/create_policy_brief.R,
    inst/templates/workingpaper/template.qmd,
    inst/templates/article/template.qmd,
    inst/templates/policy-brief/template.qmd
  </read_first>
  <behavior>
    - Test 1: each `create_*` helper scaffolds valid starter files and preserves shared baseline front-matter structure across formats (D-03).
    - Test 2: scaffolded starters include representative typstR defaults required for first run per format (e.g., JEL/report-number where relevant) (D-02).
    - Test 3: scaffolded `template.qmd` files contain concise inline guidance comments near editable fields instead of external-only instructions (D-01, D-04).
    - Test 4: title override behavior remains intact for all scaffold helpers after template changes.
  </behavior>
  <action>
Refactor `tests/testthat/test-scaffolding.R` into a table-driven cross-format contract test that executes each helper (`create_working_paper`, `create_article`, `create_policy_brief`) and asserts both shared baseline and format-specific deltas.

Lock D-03 by asserting a common baseline set of YAML/body markers across all formats, then assert only allowed deltas: article variant metadata + JEL defaults, policy brief variant metadata + report-number defaults, working paper report-number + JEL defaults. Lock D-01/D-04 by asserting inline guidance comments exist at key edit points in scaffolded `template.qmd`.

Keep assertions anchored on scaffolded output (not raw template files) so tests exercise real helper behavior.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R")'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat/test-scaffolding.R` includes cross-format contract coverage for shared baseline + format deltas.
    - Contract tests explicitly reference inline guidance expectations in scaffolded `template.qmd` content.
    - Existing file-presence and title-override guarantees remain asserted for all three helpers.
    - `Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R")'` runs green.
  </acceptance_criteria>
  <done>Starter contract regression tests fail whenever first-run baseline, defaults, or inline guidance drift from D-01..D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update all starter templates to satisfy hybrid runnable onboarding contract per D-01..D-04</name>
  <files>
    inst/templates/workingpaper/template.qmd
    inst/templates/article/template.qmd
    inst/templates/policy-brief/template.qmd
  </files>
  <read_first>
    inst/templates/workingpaper/template.qmd,
    inst/templates/article/template.qmd,
    inst/templates/policy-brief/template.qmd,
    tests/testthat/test-scaffolding.R,
    .planning/phases/07-first-run-onboarding-reliability/07-CONTEXT.md,
    .planning/phases/07-first-run-onboarding-reliability/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: each starter remains directly renderable on supported setup without manual template edits (D-01).
    - Test 2: each format includes realistic default metadata for first run and only the format-specific fields relevant to that format (D-02, D-03).
    - Test 3: guidance comments are concise, colocated with editable fields, and do not invalidate YAML/document structure (D-04).
  </behavior>
  <action>
Edit all three `template.qmd` files to a shared hybrid runnable baseline (compact realistic content plus concise inline edit cues), then apply only required format deltas per D-03.

Implement D-02 by ensuring representative defaults are present and format-appropriate: keep JEL/report-number where relevant and avoid irrelevant fields in each format. Implement D-04 by placing short inline guidance comments directly in front matter/body near the fields users must change first.

Do not move onboarding guidance to CLI output or external docs in this phase. Keep templates valid Quarto YAML and maintain existing format markers (`format-variant`) and bibliography wiring.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R")'</automated>
  </verify>
  <acceptance_criteria>
    - All three template files contain shared baseline starter structure plus format-only deltas.
    - Inline guidance comments exist near editable front-matter fields in each template.
    - Representative typstR defaults are present per format and align with D-02 constraints.
    - `tests/testthat/test-scaffolding.R` passes after template updates.
  </acceptance_criteria>
  <done>Shipped starter templates consistently satisfy first-run onboarding contract assumptions across all supported formats.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R")'`
</verification>

<success_criteria>
- Contract tests enforce D-01..D-04 starter requirements through actual `create_*` scaffold output.
- All three shipped templates reflect one shared baseline onboarding structure with explicit format deltas.
- Starter content remains realistic and runnable without manual structural fixes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-first-run-onboarding-reliability/07-01-SUMMARY.md`
</output>
