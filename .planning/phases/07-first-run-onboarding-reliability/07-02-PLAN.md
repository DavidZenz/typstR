---
phase: 07-first-run-onboarding-reliability
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/testthat/test-yaml-integration.R
autonomous: true
requirements:
  - ONB-01
must_haves:
  truths:
    - "On a supported setup, helper-generated working paper, article, and policy brief projects pass `validate_render_environment()` before render (ONB-01)."
    - "On a supported setup, helper-generated starters for all three formats render to PDF without manual template edits (ONB-01, D-01)."
    - "Quarto-absent environments remain deterministic and safe via explicit guard skips while preserving integration contracts for supported setups."
  artifacts:
    - path: "tests/testthat/test-yaml-integration.R"
      provides: "Create-helper onboarding matrix proving scaffold -> validate -> render success across all supported formats"
  key_links:
    - from: "tests/testthat/test-yaml-integration.R"
      to: "R/validation_environment.R"
      via: "helper-driven `validate_render_environment()` assertions per format"
      pattern: "validate_render_environment"
    - from: "tests/testthat/test-yaml-integration.R"
      to: "R/render.R"
      via: "helper-driven render assertions using package render wrappers"
      pattern: "render_pub|render_working_paper"
    - from: "tests/testthat/test-yaml-integration.R"
      to: "R/create_working_paper.R|R/create_article.R|R/create_policy_brief.R"
      via: "first-run scaffolding path exercised before validation/render checks"
      pattern: "create_working_paper|create_article|create_policy_brief"
---

<objective>
Prove ONB-01 end-to-end by enforcing a helper-driven scaffold -> validate -> render matrix for all supported formats.

Purpose: onboarding reliability is only truthful if tests exercise the real user path for each format, not template-only smoke paths.
Output: Guarded integration tests that validate and render scaffolded working paper, article, and policy brief starters without manual edits.
</objective>

<execution_context>
@/Users/davidzenz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidzenz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-first-run-onboarding-reliability/07-CONTEXT.md
@.planning/phases/07-first-run-onboarding-reliability/07-RESEARCH.md
@.planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md
@.planning/phases/07-first-run-onboarding-reliability/07-01-PLAN.md
@.planning/phases/06-pre-render-environment-validation/06-02-SUMMARY.md
@R/validation_environment.R
@R/render.R
@tests/testthat/test-yaml-integration.R

<interfaces>
From R/validation_environment.R:
```r
validate_render_environment <- function(path = ".")
```

From R/render.R:
```r
render_pub <- function(input = NULL, output_format = NULL, quiet = FALSE, open = interactive())
render_working_paper <- function(input = NULL, quiet = FALSE, open = interactive())
```

From tests/testthat/test-yaml-integration.R:
```r
.skip_if_no_quarto <- function() {
  skip_if_not(.quarto_available())
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add helper-driven cross-format validation matrix per ONB-01 and D-03</name>
  <files>
    tests/testthat/test-yaml-integration.R
  </files>
  <read_first>
    tests/testthat/test-yaml-integration.R,
    R/create_working_paper.R,
    R/create_article.R,
    R/create_policy_brief.R,
    R/validation_environment.R,
    .planning/phases/07-first-run-onboarding-reliability/07-VALIDATION.md,
    .planning/phases/07-first-run-onboarding-reliability/07-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `create_working_paper()`, `create_article()`, and `create_policy_brief()` each produce a project that returns `typstR_validation_report` with `ok = TRUE` on supported setup.
    - Test 2: validation assertions run through the same helper-driven first-run path for every format, not direct template-directory smoke tests (D-03).
    - Test 3: tests remain Quarto-guarded and deterministic in unsupported environments (skip, not fail).
  </behavior>
  <action>
Refactor onboarding integration tests into a format matrix that scaffolds projects via each `create_*` helper, then runs `validate_render_environment()` per scaffold root.

Replace or demote template-directory-only validation smoke checks in favor of helper-path assertions so ONB-01 is evidenced on the actual user flow. Keep the matrix table-driven to prevent per-format drift and to reflect D-03 shared baseline behavior expectations.

Retain `.skip_if_no_quarto()` as the gate for supported setup evidence; no fallback fake validation paths are allowed.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R")'</automated>
  </verify>
  <acceptance_criteria>
    - Integration file contains helper-driven validation assertions for working paper, article, and policy brief.
    - Validation checks assert `typstR_validation_report` shape and successful status for each format.
    - Direct template-folder validation assertions are removed or clearly secondary to helper-path contracts.
    - Test command exits green (with expected Quarto-dependent skips when CLI unavailable).
  </acceptance_criteria>
  <done>Validation-first onboarding reliability is contract-tested for all supported formats through real scaffold helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add helper-driven cross-format render-success and inline-guidance regression assertions per D-01 and D-04</name>
  <files>
    tests/testthat/test-yaml-integration.R
  </files>
  <read_first>
    tests/testthat/test-yaml-integration.R,
    R/render.R,
    inst/templates/workingpaper/template.qmd,
    inst/templates/article/template.qmd,
    inst/templates/policy-brief/template.qmd,
    .planning/phases/07-first-run-onboarding-reliability/07-01-PLAN.md,
    .planning/phases/07-first-run-onboarding-reliability/07-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: each helper-generated starter renders successfully to PDF on supported setup without manual template edits (ONB-01, D-01).
    - Test 2: render assertions execute through package wrappers (`render_pub()` / `render_working_paper()`), preserving user-path fidelity.
    - Test 3: inline onboarding comments introduced in template.qmd do not break YAML parsing or render flow for any format (D-04).
  </behavior>
  <action>
Extend the same format matrix with helper-driven render assertions: scaffold each format, run the package render path, and assert expected PDF outputs exist. Keep tests unchanged-by-user-edit to prove first-run reliability.

Add explicit regression assertions that onboarding inline comments remain render-safe for each format. This can be satisfied by rendering untouched scaffold outputs and asserting no YAML/front-matter parse errors occur.

Ensure article and policy-brief checks no longer rely on direct `quarto::quarto_render("template.qmd")` from copied template folders as primary evidence; helper-path render contracts must be primary.
  </action>
  <verify>
    <automated>cd /Users/davidzenz/R/typstR && Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R")'</automated>
  </verify>
  <acceptance_criteria>
    - Integration matrix includes render-success assertions for all three helper-generated formats.
    - Tests assert expected PDF artifacts exist in scaffold directories for each format.
    - Inline guidance comment safety is covered through untouched scaffold render assertions.
    - `tests/testthat/test-yaml-integration.R` remains fully guarded by Quarto availability checks.
  </acceptance_criteria>
  <done>ONB-01 scaffold->validate->render success path is fully enforced across all supported formats with comment-safe onboarding templates.</done>
</task>

</tasks>

<verification>
- `Rscript -e 'testthat::test_file("tests/testthat/test-yaml-integration.R")'`
- `Rscript -e 'testthat::test_file("tests/testthat/test-scaffolding.R")'`
</verification>

<success_criteria>
- Integration contracts prove all three helper-generated starters validate and render successfully on supported setups.
- First-run reliability evidence is helper-driven and cross-format, not template-smoke-only.
- Guarded skip behavior remains deterministic when Quarto is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-first-run-onboarding-reliability/07-02-SUMMARY.md`
</output>
